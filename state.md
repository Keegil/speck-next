# State

**What's true now.** The method exists in the form agents load: `AGENTS.md` (the loop, the files, small changes, risky work, honest state, product speech) plus three real skills — `independent-review` with its attack playbook and experience reference, and `shape-product` with the question set that old Speck's failures paid for. The dev suite is green with every check first proven able to fail; a fresh cross-vendor agent passed governance unprompted, and another refused in writing to mark unbuilt work done. The example product ([examples/pulse](examples/pulse/state.md)) is two increments deep: its first independent review caught two promise-breaking bugs the builder missed, and its AI-first increment (`pulse innsikt`, local model on the shipped path) was redesigned twice by its own proving runs — the machine counts, the model phrases, and nothing canned ever reaches the user. The v1 harvest from old Speck is adopted: shaping questions, experience judgment, and the scar patterns, each traceable to the failure that earned it.

**What's blocked.** Nothing.

**What needs the owner.** Nothing right now — the next pieces are agent work.

**What happens next.** The independent review of the innsikt increment lands and its findings get fixed. Then the v1 confidence audit: a fresh adversarial context judges the whole kernel against the owner's goal — excellent methodology, true value-add over a no-harness agent, ready to reboot a real product greenfield. Whatever it finds gets closed before v1 is declared. Deferred past v1: the upgrader (greenfield reboots don't need it; promise 5 stays), the shelved contest, a browser-UI proof exercise (pulse is terminal-only — the experience reference is harvested from real UI products but hasn't been exercised on a browser product under this kernel yet).

**Evidence.** The files sit at the paths hosts actually read — `AGENTS.md`, `CLAUDE.md`, `.claude/skills/` (this repo, this commit). Pulse's proof runs and review reproductions: its state file and this repo's history, 2026-08-13. Next-session governance: not yet evidenced, open above.
