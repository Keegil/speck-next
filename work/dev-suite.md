# The development suite

**Outcome:** a handful of tiny scored tasks the kernel can be measured on in minutes, so a method change shows its effect the same day: at least one bug-hunt task (visible planting on a pulse-sized toy), one small-change task (paperwork must stay zero), and one honest-state task (overclaim must be refused) — each with mechanically checkable observables and a one-command runner that prints a small scorecard.

**How I'll know it works:** run it twice on the current kernel — same scores both times (it's deterministic where it claims to be) — then make one deliberate kernel regression (loosen the review skill) and watch the suite catch it. A checker that can't go red proves nothing.

**Open:** not started — next session, after the fresh-agent governance check that's already queued in state.md. Driving an agent per task needs the headless runner; smallest version may score my own session transcripts before automating.
