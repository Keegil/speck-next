# State

**What's true now.** The method exists in the form agents load: `AGENTS.md` is the method page (this repo runs under it, kernel work included), `independent-review` is a real skill, and the repo carries its own product, work, decisions, and state files. One piece is **Built**: the runtime form itself. One example product exists and is **Built** with three of four verdicts evidenced ([examples/pulse/state.md](examples/pulse/state.md)) — its independent review caught two promise-breaking bugs the builder's own proving missed, which is the method's core claim working in practice. The contract (v0.5) and the frozen contest are in place; no upgrader, no state generator, no installer yet.

**What's blocked.** Nothing.

**What needs the owner.** Nothing right now — the next pieces are agent work.

**What happens next.** Verify next session that a fresh agent is governed by `AGENTS.md` without being pointed at it. Then two pieces, either order: the development suite (work/dev-suite.md — tiny scored tasks so kernel changes are measured in minutes, adopted from the Meta-Harness pattern with the contest kept as the untouched held-out exam) and the upgrader's first slice (era detection plus a dry-run report against a real old-Speck repo snapshot). In parallel, the contest's independent bug-planting stays with a different AI, unseen by anything building this kernel. Harvesting continues by pull: the review skill carries the attack playbook; the UI/LARP pack gets written when the first UI product needs it.

**Evidence.** The files sit at the paths hosts actually read — `AGENTS.md`, `CLAUDE.md`, `.claude/skills/` (this repo, this commit). Pulse's proof runs and review reproductions: its state file and this repo's history, 2026-08-13. Next-session governance: not yet evidenced, open above.
