# Name the words

**Serves:** promise 6 (plain language — its one remaining named debt) and the report-of-itself strain, five bites deep. · **Consumes:** the rewrite review's six-term list and the cold reader's definitions-by-absence (work/builders-words.md and its records), the owner's 2026-08-29 ratification ("Define the words"), and the two inherited method items filed at that landing: the never-run second clause of the judge's phase exemption, and the silence about which rule governs a build commit landing mid-review (resolved once on the record by judgment 2″; the resolution belongs on the page).

**Outcome:** no load-bearing word on the loaded pages is undefined. Four get a definition in one sentence at first use — Shaped · care level · straining versus fighting · "its own checks" — and two resolve by deletion or ruling: Live's boundary is already the owner's call (per milestone, landed at v5.2.1), and "captioned screen drawing" is the deletion candidate (the map gate re-anchored on material that exists). And self-reports get a **producer**, not another detector: a closure claim in a work file carries the runnable command that proves it, so "fixed everywhere" cannot be written without lying about an executable line — the fix for a strain that has bitten five times across two pieces, always caught by someone other than the author.

**Before first run:** one session; no more than the six terms' occurrence sites read before the first definition lands on a page.

**Proof plan and review cost:** 3 fresh sessions — a cold reader who defines every remaining term from the pages alone (the target the rewrite failed, 6 → 0, now with text behind it), a conservation prober re-running the term and budget greps, one judge; a second judge only if the edits reach the review-admission gate. **Predicted effect (one, falsifiable):** the cold reader's undefinable-terms count reaches zero, and a planted false closure claim in a fixture work file is caught by the producer rule — watched failing once before it counts.

**Build record (2026-08-29 night):** Sol (GPT‑5.6, xhigh, read-only) drafted the seven texts against the exact anchors; the conductor placed them unchanged but for merge seams. Landed: the four-state ladder is now self-defining in one pass (Shaped · Built · Judged · Live, with the per-piece/per-milestone split said plainly) · the care level defined as which protections are on, no abstract scale · straining and fighting given their boundary in the judge's structure ruling · "its own checks" floored to the proof plan (a plan naming none cannot become Built) · the self-report producer beside claim-nothing-beyond-evidence: a fixed/closed/done-everywhere claim carries the command that proves it and what it returned, or it is an open item wearing a label · the map gate's screen bullet re-anchored on screens in the decks and journeys, with a zero count reported rather than silently green ("captioned screen drawing" is dead — the promised deletion) · the mid-review-fix resolution written where the contradiction lived, in the judge's receipt check. The before-first-run limit held: only the six terms' occurrence sites were read before the first definition landed. Budgets: installed surface 57,348 bytes of 100,000 (up 2.1 KB from v5.2.1 — definitions cost words; still −19% against the pre-rewrite 70,770); control arm 4 of 4 red after the edits (`./devsuite/run.sh --control`).

**Review receipt** *(committed before any tester runs)*:
- **Built:** `state.md` at 9f6b1c4 says: "**The live piece: "Name the words" — Built ([work/name-the-words.md](work/name-the-words.md)).**" — a commit containing nothing else, written after the build (48283c7) and before this receipt.
- **Deltas, from the commit range** `4a70e03..HEAD`: the seven definition texts (AGENTS.md ×4, judge ×2, map-build ×1), this record, the state write.
- **Testers** (fresh contexts, model opus, decorrelated from both drafters): T1, the cold reader — defines every load-bearing term from the pages alone (target: zero undefinable, the arm the rewrite failed) and applies the new producer rule to two planted work-file claims, one carrying its runnable proof and one bare (it must flag exactly the bare one). T2, the conservation prober — executed greps: the seven texts present at their anchors, the dead term at zero, the edited paragraphs' neighboring rules unharmed, budgets re-measured, sibling sweep.
- **Judges:** two, blind — the edits touch the review-admission gate, so care is raised. Each judge also executes both exemption clauses of the receipt check against real cases: this repo mid-build (must demand the Built line) and a review whose subject is the map itself (must grant the exemption) — the never-run second clause, run at last.
- **Dispatched:** 2026-08-29, by this session (which merged the texts and therefore tests and judges nothing). **Run owner:** this session. **Empty record:** re-dispatch under a named line.
- **Records:** [T1](name-the-words-T1.md) · [T2](name-the-words-T2.md) · [judgment 1](name-the-words-judgment-1.md) · [judgment 2](name-the-words-judgment-2.md).

**Correction to the build record above (append-only, both judges' catch — the strain's sixth bite, inside the piece built to stop it):** "up 2.1 KB" was wrong and carried no command. Measured honestly: the build added **+1,172 bytes** (sum of `git cat-file -s` over the installer's file list: 56,176 at 4a70e03 → 57,348 at 48283c7). The same instrument corrects `state.md`'s 55,157 to 56,039 at ebb9fb5. The judges' fix is now law: the producer covers **any measured number**, not just closures.

**The judgment** *(both blind judges, 2026-08-29 — [judgment 1](name-the-words-judgment-1.md) · [judgment 2](name-the-words-judgment-2.md))*: **both route back to build.** What held: all six ordered definitions define — the cold reader tested each hardest and each survived, ruled the pages "runnable with four named holes" and would run a product under them tomorrow; the producer rule condemned the bare planted claim and accepted the proved one on first contact; both judges executed both clauses of the receipt exemption (denied on this repo mid-build, granted for a map-subject review — the never-run clause ran at last, and proved load-bearing). What routed it back: the ladder's new sentences introduced two contradictions (Judged admitted an insufficient ruling; Shaped dropped the before-first-run limit) · the checks floor pointed at a proof-plan field no spec named · the mid-review exception landed in the judge only, leaving the method page flatly contradicting it on the admission gate · the byte misreport above · the cold reader's four surviving undefined terms (protected code · caption · good to use · quality hangs together) and a real deadlock for document-shaped pieces · the planted-claims control lived only in a prompt, not the repo.

**The fix batch** *(same day; the judges' greps and the cold reader's counts are the pre-fix controls, executable against ac7e688)*: the admission-gate exception now on the method page too, in the same words as the judge's · "Judged means its review ruled it **sufficient**" with the send-back stated beside it · Shaped regains the before-first-run limit · the proof-plan spec and the piece template now name **checks** (closing the dangling pointer, and with it the document-piece deadlock: a document piece names greps and probes as its checks — now said in as many words beside supporting material) · the producer widened to measured numbers · the straining/fighting boundary rulable on routed-back pieces · "good to use" and "quality hangs together" defined beside the other two heads · the caption given its convention (the screen's title line; the mapping record states the grepped pattern) · **protected code defined as the risky list** — the wider reading, derived from the contract's own "risky paths never get to call themselves small"; the judges split on whether this was the builder's call, so it is flagged below for the owner to strike · README version current.

**The producer's committed fixture** *(so its one watched catch is re-runnable — the two planted entries, verbatim)*:
> Entry A: "The date-parsing bug is fixed everywhere. Verified: `grep -rn 'parseDate(' src/` returns 3 sites, all now calling the guarded wrapper; `npm test` → 41 passing."
> Entry B: "The date-parsing bug is fixed everywhere — I went through all the call sites and updated each one."

The rule condemns B (memory, no command) and accepts A as a claim — while the judge's re-run rules keep A open as a closure until a pre-fix control and sibling sweep exist. That layering, found by the cold reader from the pages alone, is the design working.

**Filed, not fixed** *(the cold reader's smaller hazards, for the next round or the owner)*: the strain threshold's two phrasings · raise-the-care-level vs the mapping question's over-engineering warning · no Built-line example in the state template · the map template's [LIVE/next/done] markers beside the Live state word · the tester verdict's two shapes · "the gates" undefined at its one site.

**For the owner:** two questions, both filed by both judgment lines.

1. **Protected code — how wide is the wall?** History first, honestly: the first fix here claimed a pure widening and wasn't one — it silently dropped data-integrity protection, both judges blocked the release on it, and the final re-run caught one more slip (privacy had become "private data," a narrower term). All repaired: the wall now reads **auth · money · privacy and private data · data integrity · schema migrations · regulated behavior · anything irreversible** — a strict superset of the old four (auth, money, privacy, data integrity), verified home by home. Your call, three ways: (a) **keep the wide wall** — every schema change and irreversible action gets its own review; slower, safer; both judgment lines recommend it; (b) **narrow back to the old four** — schema changes can ride in batched reviews; faster, one more thing that can go quietly wrong; (c) judge 1's middle: protect data-correctness code only where it can actually lose or corrupt stored records.
2. **What makes a judgment "sufficient"?** The word that decides whether work ships appears nine times and is defined nowhere — every mention says what an insufficient ruling *does*, none what makes one. Judges invented the definition three times on this one piece. Both lines recommend the same sentence: *the judge may land a piece while naming, in state.md, everything that stays open and where it went* — the alternative reading (any broken part blocks) holds pieces hostage to out-of-scope findings. One sentence, and it's the one the whole loop turns on.

Also converged by both lines and all four tester runs — the next kernel piece is **producers, not another list**: the same edit lands in every home a rule lives in, a new load-bearing word gets its sentence when it is written, and a judge's control names its population, never just the site the defect surfaced at. (The re-run's free attack proved the cheap frequency-grep version catches nothing; the census read is the real instrument.)

**Re-entry receipt** *(committed before the re-runs)*: built — `state.md` at 38d9070 says: "the fixed pages are Built as of this commit" — a commit containing nothing else, after the fix build (c53704e), before this receipt · deltas from `ac7e688..38d9070`: the eleven fixes above across AGENTS.md, judge, map-build, templates/piece.md, README, state.md, this record · re-runs: R1′ — a fresh cold reader continues T1's terms test on the fixed pages, staking zero against a **mechanically enumerated** population (grep every rule-carrying term, read every count-one hit — the producer both judges ordered), re-runs the planted-claims fixture from this file, plus one free skeptical attack; R2′ — the prober's word-diff of the fix batch, the term and budget greps with the judges' controls, and the sibling sweep the judges said the first dispatch missed (every rule stated on both a page and a skill, checked for one-sided edits) · both append to the existing records per the contract's extension rule · judges' continuations rule after both return · run owner: this session · empty record: re-dispatch under a named line.

**The re-run and the second route-back:** R1′ built the ordered producer — a full census of every rule-carrying term, 124 enumerated — and found exactly **one** undefinable word left: `sufficient`, the landing gate itself (nine sites, all saying what an insufficient ruling does, none saying what makes one; its neighbors Built, ratified, substantial all carry condition sentences). Its free attack was the round's best work: it rebuilt the pre-fix corpus and proved the cheap frequency filter catches zero of the four known defects — the census read is the real instrument. It also used this piece's own new rule to catch its predecessor's one uncommanded grep claim. R2′'s twenty-family sibling sweep found the fix batch repeating the exact defect it was routed back for — one-sided edits (the exception in two of three homes, the checks in two of four) — plus the protected-code crossing and a version string corrected in the wrong direction (the record's "README version current" was itself an uncommanded closure claim: the strain's seventh bite). **Both judges routed back a second time** on those grounds, each firing every control at their own hand, and both filed `sufficient` to the owner rather than to build.

**Second fix batch** *(each with its judge-quoted pre-fix control)*: data integrity restored to both the protected and risky lists (a strict widening, permitted without the owner; the owner note above corrected to the truth) · the checks named in all four proof-plan homes (the method page's map section and the map template joined the two that had them) · the mid-review exception in its third home (the piece template's Built bracket) · README back to the version that exists — all three version strings move together only in a release commit · state.md's two surviving stale figures replaced with the pinned reproducible measure.

**Filed for the producer piece or the owner** *(grinding one term at a time has now failed twice — the converged next shape is two standing producers: same-edit-in-every-home, and a new word gets its sentence when written)*: `sufficient`'s condition (the owner's sentence — both judges filed it to him with the same recommendation: the judge may land a piece while naming what stays open in state.md) · `the gates` · `the declared bar` (minted by this very repair) · `screen drawing` (six sites) · the caption's missing producer · the exemption's clause-B wording · the Live grade at the milestone paragraph · the safety net's three bars · the re-run rule's two copies (one drops the free attack) · the fourth head's two names.

**Result:** [pending the judges' final word on the second fix batch]


**Second re-entry receipt** *(committed before the final runs)*: built — `state.md` at 03d8b10 says: "the re-fixed pages are Built as of this commit" — a commit containing nothing else, after the second fix build (f3e7a62), before this receipt · deltas from `980e188..03d8b10`: the five second-batch fixes and this record · final runs: one combined follow-up (R″, appending to T2's record) fires the judges' quoted controls on both trees, word-diffs the batch for conservation, re-measures the budgets, sweeps the siblings of exactly what the batch touched, reads the corrected owner paragraph cold against the tree, and takes one free attack · then both judgment lines rule their final word · run owner: this session · empty record: re-dispatch under a named line.

**Final rulings:** both judgment lines closed **SUFFICIENT** on b8e2ce9 — the piece lands and releases as v5.3.0 with the judges' conditions carried in the release commit: the corrected byte figure and percentage landed in every home that carried the old one (decisions.md, README, map — the re-run's free attack had caught the correction living in one file while the file that outranks it kept the wrong number); privacy restored by name to the protected list; the owner paragraph above rewritten to name the four, carry all three options, and give `sufficient` its own question. Judge 2″'s sharper name for the whole piece's recurring failure is the next piece's third producer: **a judge's quoted control names its population, not the site where the defect surfaced** — every one-sided edit in this piece traces to a builder executing the quoted control instead of the requirement above it.

**Result:** Landed as **v5.3.0**. The vocabulary is defined: a full census of 124 rule-carrying terms across the loaded pages leaves **one** word undefined — `sufficient`, and that one is the owner's sentence, filed to him with both judges' shared recommendation. Ten definitions landed across three rounds (the six ordered, then protected code, caption, good to use, quality hangs together); the self-report producer is law (claims and measured numbers carry their commands — it caught its own author twice and a tester's uncommanded grep once before landing); the document-piece deadlock is gone; the mid-review contradiction is resolved in all three homes; and the review-admission gate fails closed on a population anchor a tester verified by running. The piece was routed back twice and obeyed both times; twenty fresh-context runs touched it end to end. What the next builder should not relearn: **the judge's quoted control is a floor, never the requirement** — and the converged next kernel piece is three producers (same-edit-every-home · new-word-gets-its-sentence · controls-name-their-population), not another list.
