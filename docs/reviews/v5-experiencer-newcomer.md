# Experiencer record — the newcomer

Persona: a smart agent who has never seen Speck Next and must use these pages to drive a real product build tomorrow.

Subject: the kernel surface at commit `586a87a943dde20989142d11973b09f0f`.

I did not build this work. I read `.claude/skills/experience/SKILL.md` first, then made the requested single top-to-bottom read of `AGENTS.md`. The record below follows the order in which I encountered the surface.

## 1. The method page, read once

### Run

I ran:

```text
nl -ba AGENTS.md
```

The command exited 0. I read lines 1–57 once, from top to bottom.

### What I could act on immediately

- Lines 15–19 gave me a usable dispatcher. In the supplied scenario — a ratified `product.md` and a ratified map with one live piece — line 17 puts me in building.
- Lines 28–32 gave me the main build sequence: inspect accumulated strain and foundation triggers, shape the piece in its work file before code, build through the real surface, convene the hearing, then land the result.
- Lines 39–47 told me what each standing file means, which sources win when documents disagree, and that measured evidence beats documents.
- Lines 51 and 55 gave me checkable boundaries for small and risky work.

### Stumbles during the one-pass read

I recorded these when I hit them; I did not repair them from the later skills.

1. **Five phases, but one combined process.** At line 3 I paused on “five phases” followed immediately by “Experience and judge run as one process with two stances.” I could retain five names and one hearing, but I did not yet know whether the combined hearing was one phase boundary or two. Lines 18 and 33 later made the intended operating unit clearer: dispatch experiencers, then a judge, as one hearing.

2. **A first-session status opening has one missing value.** Line 7 says every reply opens with the phase, live piece or round, and “what changed since he last looked.” On a genuinely new session I know the first two from the files, but there may be no known prior look. I would say that plainly, but the page does not specify the cold-start form.

3. **The piece loop says six steps and contains five.** Line 27 says “six steps”; lines 28–32 enumerate only 1 through 5. I re-counted the displayed numbers before moving on. This made me wonder whether a transition — most plausibly recording `Built` before the hearing — had dropped out.

4. **The `Built` landing is not named.** Line 43 defines `Shaped → Built → Judged → Live`, while the piece loop moves directly from building (line 30) into the hearing (line 31), then lands and marks the piece done (line 32). I could not tell from the page alone exactly when I must write `Built` to `state.md` or whether that is the missing sixth step.

5. **“Land it” reads unconditionally after a hearing that can fail.** Line 32 tells me to mark the piece done and put the next piece live. Lines 33 and 43 say insufficient work must route backward and must not advance. I infer that line 32 applies only when the judge's rulings are sufficient, but the numbered step does not say that.

6. **The page requires dispatch receipts but does not locate or shape them.** Line 31 says receipts open at dispatch for experiencers and judge alike. Line 41 calls the piece work file “cradle to grave,” so I would put them there, but that is an inference from the page. I needed the skills to learn the required receipt fields.

7. **The allowance for one unjudged piece does not fit the listed order cleanly.** Line 31 allows at most one substantial piece to sit unjudged when the next starts, but line 32 starts the next piece only after the current piece's hearing. I could follow the stricter listed order; I could not tell when the allowance is intended to be used.

8. **The owner's grade has no named landing place.** Line 33 says the owner grades the felt experience and that the hearing exits only after the owner has graded. The page does not say whether that grade is appended to the piece work file, a milestone record, or only reflected in `state.md` evidence.

9. **A milestone repeats the hearing at a larger scope, but that distinction took a careful read.** Line 31 hears each substantial piece using the personas named in its proven-means. Line 33 hears the completed milestone with all four personas and adds a second judge. I could act after separating those scopes, but “the hearing” names both of them.

### Simulated drive from the method page alone

Given a ratified product and map with exactly one live piece, I would do this:

1. Open my first owner update with “building,” name the live piece, and state the current file-backed change or say this is the cold start. This follows line 7 and the dispatcher at line 17.
2. Read the “what's wearing out” section of `state.md` and the foundations and triggers in `product.md` (line 28). If one strain has bitten twice or a foundation trigger fired, stop treating the mapped piece as next: say that mapping is being re-entered, use the mapping path to re-cut the map, and leave the trace required by line 19. If it is deferred, surface that deferral to the owner. The page does not name the exact deferral field.
3. Create the live piece's one work file. State its outcome and what “proven” means for this piece, including the personas and runs its hearing needs. Commit that file before writing piece code (line 29).
4. Build while repeatedly running the actual product surface (line 30). Use drawn frames for drawn screens; make the first external-dependency execution a real round trip; apply the `craft` skill to anything the user sees. If I work around a problem, add a strain line and count to `state.md`. If I add a safety net, deliberately make it fail and retain that observation before citing it in the hearing (line 31).
5. When the implemented outcome runs, I would want to write `Built` and its evidence to `state.md` before the hearing, because line 43 defines that state. This is the point where the page leaves me unsure: no numbered step explicitly orders that write.
6. Before each experiencer starts, open a receipt in the piece work file. I infer that location from lines 31 and 41; the page alone does not give the fields. Dispatch fresh non-builder experiencers in the personas named by the piece's proven-means. Each lives the real product under the `experience` skill and returns a chronological record ending in that persona's verdict; every verdict claim must point to a lived moment (line 31).
7. Before judgment starts, open a judge receipt in the same inferred place. Dispatch a separate judge who neither built nor walked the piece. The judge reads the returned records, questions and challenges every verdict, challenges favorable ones hardest, and sends the relevant experiencer back to the product with an exact scenario whenever the record cannot answer. The judge keeps persona disagreements side by side and rules from cited record moments (lines 31 and 33). For a risky piece, dispatch a second independent judge (line 33).
8. Put the experiencer records, their follow-up runs, the judge's questions, and the judge's rulings with the piece. This landing location is inferred from the “cradle to grave” work file at line 41; the page does not state it directly. Rewrite `state.md` with the four separate rulings and evidence as required by line 43.
9. If evidence is thin, order another experiencing round. If the promise is wrong, re-enter shaping; if the piece-space is wrong, re-enter mapping; if the build is wrong, return to build. Name and link the judgment as the trace (lines 19 and 33). Do not mark the piece done.
10. If the four rulings stand on evidence, mark the piece done in `map.md`, rewrite `state.md` to reality, and put the next piece live (line 32). This conditional is my reconciliation of lines 32, 33, and 43 rather than wording the page gives directly.
11. When that landing completes a milestone, convene the larger hearing: one fresh non-builder context for each of the first-timer, real-job worker, second user, and worst day; then two independent judges on the same records. Give the owner a plain-language rendering and ask for the felt grade. Route any insufficiency backward with a trace; unlock the next milestone only after the four rulings and owner grade are recorded (line 33). I still do not know from the page alone exactly where the owner's grade is written.

## 2. The experiencer stance and `walk.md`

### Reads

I read `.claude/skills/experience/SKILL.md` in full before doing anything else. I then ran:

```text
nl -ba .claude/skills/experience/references/walk.md
```

The command exited 0 and printed all 28 lines.

### Could I follow the stance?

Yes. The stance is operational: “live it, record it, then rule as the user you are.” It tells me what to capture while running — exact commands, observable changes, examined screenshots, mechanisms behind claimed actions, felt reports — and it puts the verdict last. It also gives the check on each verdict claim: it must point back to a moment in my record, and anything I could not run becomes untested rather than a pass.

For a user interface, `walk.md` turns that stance into an ordered walk. Lines 7–18 take me from promises and a cold real build through first-screen comprehension, the full job, mechanism checks, hostile behavior, screenshot inspection, accessibility checks, promised moments, and craft, then end in three persona verdicts. Lines 22–26 separate declared rules from taste and reserve genuine aesthetic forks for the owner.

### Contradiction check

I found **no sentence in either file that tells the experiencer not to conclude**. The strongest conclusion instructions point the same way:

- Experience skill: “Then, at the end, the verdict: as this user, does it work, would you keep it, what breaks the deal.”
- `walk.md` line 18: “Close with your verdicts as this persona.”

I found **no contradiction with verdict-at-the-end**.

One terminology mapping made me pause, without reversing the stance: `walk.md` asks the UI experiencer for three verdicts — “works, feels good, crafted” (lines 3 and 18) — while `AGENTS.md` and the judge ask for four rulings — works, delivers the promise, good to use, quality hangs together. I can treat the three UI verdicts as testimony feeding the judge's four product rulings, but no page explicitly maps one set to the other.

## 3. The judge stance

### Read

I ran:

```text
nl -ba .claude/skills/judge/SKILL.md
```

The command exited 0 and printed all 23 lines.

### Could I run the hearing?

Yes. Lines 14–19 provide an executable order: hear every record and strike ungrounded claims; challenge each verdict that carries weight; send the experiencer back for an exact run where needed; preserve persona divergence; rule per promise and on four separate whole-product dimensions; rule on structure and the whole; then route insufficiency to the right phase. Line 21 adds the independent second judge for milestones and risky pieces. Line 23 orders the fix, targeted re-experience, and re-judgment loop.

### Contradiction and actionability check

I found **no leftover ban on contact with the product world that conflicts with dispatch-back power**. Line 8 draws a coherent boundary: “The judge built nothing and walked nothing — its contact with the product runs through the experiencers it steers.” The same paragraph says the judge does not walk the product itself and, when evidence is missing, sends an experiencer back with the exact question and waits for the run. The judge controls new observation without becoming the observer.

Two mechanics remain under-specified for me as a newcomer:

1. Line 15 says to put questions “to” an experiencer and dispatch “that experiencer” back. The receipt records tool, model, and session, which helps, but the skill gives no host-neutral fallback for a one-shot agent session that cannot be resumed. I would launch a fresh follow-up under the same persona and preserve the lineage, but that is my repair.
2. The skill specifies the ruling content but not the output file. `AGENTS.md` lets me infer that the hearing belongs in the piece's cradle-to-grave work file and its current claims in `state.md`; the judge skill itself does not state that landing.

Neither gap prevents the judge from hearing and ruling, but both would make two first-time conductors store or resume the same hearing differently.

## 4. README as a curious owner

### Read

I ran:

```text
nl -ba README.md
```

The command exited 0 and printed all 44 lines.

### What landed cleanly

- Lines 3 and 7–16 gave me the product promise and a concrete session before explaining the machinery.
- Lines 20–34 explained the five phases, files, honest states, hearing, small-change boundary, accumulated strain, and risky-work care in owner language.
- Line 36 gave me an upgrade command and plainly marked old-Speck conversion as not built.
- Line 38 disclosed both the size limits and that CI enforcement is still open rather than implying a machine-enforced gate.

### Stumbles

1. **The “only words” promise overreaches.** Line 26 says the four states “are the only words Speck Next asks you to learn,” but by then I had already needed phase, piece, milestone, hearing, proven, promise, and strain to follow the explanation. I understand the intended distinction — only four state labels — but the sentence made me stop and reinterpret it.
2. **“Five files” briefly looks like a literal count.** Line 24 says “five files and a page,” then one of the five is “one small work file per piece of work.” The method actually has a `work/` collection, as `AGENTS.md` line 41 explains. The owner-level compression is readable, but not literal.
3. **The status is stale against the executable.** Line 44 begins “At v4.” The current no-argument CLI run below prints `speck-next v5.0.0`, and its pin example is `#v5.0.0`. As a curious owner, I cannot tell from README whether v5 is released, under test, or simply missing from the status paragraph.
4. **The final status paragraph gets dense after a plain page.** Line 44 introduces “v3's full sequence discipline,” “dev-suite,” “demonstrated confidence,” and “designed head-to-head contest” in one paragraph. The links help, but this is where I stopped feeling like the owner in the opening example and started reading project history.

## 5. CLI and fresh install

### No arguments

I ran:

```text
node bin/speck-next.js
```

It exited 0 and printed exactly:

```text
speck-next v5.0.0 — a small kernel for building great products and proving them by running them.

  npx github:Keegil/speck-next install [dir]   place the method into a fresh git repo (default: current dir)
  npx github:Keegil/speck-next upgrade [dir]   refresh the method files in a Speck Next repo

The method itself is one page: AGENTS.md. Everything else is five skills your agent loads on demand, and six file skeletons in templates/.
Pin a version: npx -y github:Keegil/speck-next#v5.0.0 install
```

As a first-timer, I knew which command to use, what repository condition `install` expects, where the main page lives, and how to pin the version. It did not send me looking for a separate manual.

### Fresh repository and install

`./tmp-install` did not exist. The existence check printed:

```text
ABSENT
```

I created it and ran `git init` inside it. That exited 0 and printed exactly:

```text
Initialized empty Git repository in /private/tmp/claude-501/-workspace-speck/a0e0d32c-31ec-4aba-a433-2bd88364f16f/scratchpad/v5-newcomer-LhNM/repo/tmp-install/.git/
```

Then I ran:

```text
node bin/speck-next.js install ./tmp-install
```

It exited 0 and printed exactly:

```text
Installed Speck Next v5.0.0 into /private/tmp/claude-501/-workspace-speck/a0e0d32c-31ec-4aba-a433-2bd88364f16f/scratchpad/v5-newcomer-LhNM/repo/tmp-install — 19 files on disk (method files, the version marker, an empty starter map).
Next: open an agent session there and say what you want to build — shaping starts in that conversation.
```

That is a clear handoff: the command says what landed and gives one next action in product language.

### What changed where

I checked the fresh repository. `git status --short` showed these untracked roots:

```text
?? .claude/
?? AGENTS.md
?? CLAUDE.md
?? map.md
?? templates/
```

I also asked for the latest commit in the same diagnostic command. It exited 128 at that point and printed:

```text
fatal: your current branch 'main' does not have any commits yet
```

The installer therefore wrote the kernel but did not create a commit. A separate file listing showed exactly 19 installed files:

```text
./.claude/skills/craft/SKILL.md
./.claude/skills/experience/SKILL.md
./.claude/skills/experience/references/walk.md
./.claude/skills/experience/references/worst-day.md
./.claude/skills/judge/SKILL.md
./.claude/skills/map-build/SKILL.md
./.claude/skills/map-build/references/questions.md
./.claude/skills/shape-product/SKILL.md
./.claude/skills/shape-product/references/questions.md
./.claude/speck-next.json
./AGENTS.md
./CLAUDE.md
./map.md
./templates/decisions.md
./templates/map.md
./templates/piece.md
./templates/product.md
./templates/rounds.md
./templates/state.md
```

The observed count matches both the installer output and README line 38.

## Limits

- I did not drive an actual product piece, because this subject is the kernel pages and CLI rather than a ratified product with a live piece. The simulated piece sequence is untested against a real build.
- I did not dispatch and resume real experiencer or judge sessions. The receipt, follow-up-session, divergence, and routing mechanics are untested; my actionability findings come from executing the instructions on paper.
- I did not run a UI, take screenshots, exercise accessibility, or perform `walk.md`'s hostile states. The UI walk is untested.
- I did not run `upgrade`, including its dirty-tree and old-Speck refusal claims. Those README claims are untested.
- I did not run the shape or map skills. Their completion tests, substrate round, and owner-ratification mechanics are untested.
- I did not commit the fresh install. I observed only that installation left all 19 files untracked.

## Verdict — could I drive a real build tomorrow?

**Yes, I could start and carry a normal piece through a real hearing.** The dispatcher put the supplied scenario in building, and the page gave me the wear check, pre-code piece shape, run-while-building rule, persona records, challenged judgment, backward routing, and landing sequence (record 1, “What I could act on” and “Simulated drive,” steps 1–11). The two stance pages removed the old failure mode I was specifically looking for: they tell the experiencer to conclude from lived moments, and they let the judge obtain new evidence by sending the experiencer back rather than by walking the product itself (records 2 and 3, “Contradiction check”).

**I would not be stuck on what the hearing is for or what each participant returns.** The experiencer returns a chronological run ending in grounded persona verdicts; the judge hears, challenges, orders exact follow-up runs, preserves divergence, rules four dimensions, and routes insufficiency (record 2, “Could I follow the stance?”; record 3, “Could I run the hearing?”). The CLI also gets a new repository to the starting line without a manual hunt: its exact install output names the 19-file result and tells me to open an agent session and state the product intent (record 5, “Fresh repository and install”).

**I would hesitate at the state transitions and record locations, and two conductors could diverge there.** The page says six build steps but prints five, never explicitly lands `Built`, phrases “Land it” as unconditional even though a hearing may route backward, and does not name where the owner grade or hearing outputs are written (record 1, stumbles 3–8). The judge skill also assumes I can return “that experiencer” to the product without giving a host-neutral fallback for a non-resumable run (record 3, actionability gap 1). Those are not reasons I cannot build; they are places where I would make up kernel behavior tomorrow.

**The owner surface is mostly plain, but I would not trust its version status without checking the executable.** README says “At v4,” while the command I actually ran reported `v5.0.0` twice (record 4, stumble 3; record 5, “No arguments”). That mismatch does not block the build, but it weakens the README as current truth.

**My newcomer ruling: usable for a real build, not yet unambiguous enough for two fresh agents to drive identically.** The successful dispatch/build/hearing path is grounded in the executable sequence I reconstructed from lines 17 and 28–33 (record 1, simulated steps 1–11); the remaining risk is grounded in the missing sixth step, unnamed `Built` write, conditional landing ambiguity, receipt/grade destinations, and follow-up-session gap (record 1, stumbles 3–8; record 3, actionability gaps 1–2). I would proceed tomorrow, while expecting the judge to challenge those invented transitions rather than treating them as settled method.
