# State

## What's true now

The kernel is at **v4.0.0**, tagged and Judged through its own new law — five phases (shape → map → build → experience → judge) on the owner's ruling that agents sent to prove act dysfunctionally: witnesses live the product in personas and write records with no verdicts in them, and a separate judge per piece rules from records it never gathered, routing insufficient work backward by grounds. States renamed Shaped → Built → **Judged** → Live, with *proven* kept as plain speech for all-four-verdicts-on-evidence. `AGENTS.md` (~11 KB) plus five skills — `shape-product`, `map-build`, `craft`, `experience` (the witness: receipt-at-dispatch law carried over, worst-day scripts, the walk), `judge` (rules, gathers nothing, may order more walks) — plus six template skeletons. The upgrader removes the retired `independent-review` directory (copy never deletes). All of v3's discipline stands underneath.

## What's wearing out

- **The receipt law, bitten twice** (shaping campaign, then the map reviewers): receipts reconstructed at closure instead of opened at dispatch. Carried unchanged into the witness dispatch; unproven until the next real record shows a receipt that existed before the witness ran (bitten twice).
- **My own manual dispatches keep hanging on open stdin** (the suite runner is fixed; the habit isn't — bitten twice, 79 minutes and 18 minutes). The fix is mechanical (`< /dev/null`), the discipline isn't yet.

## What's blocked

Nothing.

## What needs the owner

**What should your sign-off mean for build, experience, and judge?** The judge's first ruling found the contract promising "every phase exits with the owner's ratification" while build and experience exit mechanically and judge exits on your felt grade — the words and the machine disagreed, and picking silently isn't mine to do. Options: **(a)** keep it as built — you ratify shaping and mapping, you grade the felt experience at judging, build/experience run without a sign-off beat (recommended: no new interruptions, matches how you actually steer); **(b)** add a sign-off to every phase including per-piece (maximum control, real attention cost — works against the owner-attention property); **(c)** drop ratification language below shaping/mapping entirely (cheapest, loses the judging-phase grade as a named exit condition). The contract carries an honest interim line until you rule.

## What happens next

The v4 piece is **Judged and landed** (tag v4.0.0; Pilot upgraded). The milestone's proving continues on Pilot's build — five phases, verdict-free records, separate judges, measured by the playbook's git checks and the owner's felt verdict — which also carries two of the re-judge's ordered experiences (a real four-persona UI piece; a full fresh-install lifecycle with one deliberately insufficient judgment routed backward). The third ordered experience is the queued CI piece (promise-7 controls watched red). Queued behind triggers: the v11 converter, CI limit enforcement, the promise-conservation check.

## Evidence

This repo's history, 2026-08-13 → 19. The v4 loop, four fresh contexts deep and all receipted at dispatch: first witness record ([docs/reviews/v4-witness-record.md](docs/reviews/v4-witness-record.md)) → first judgment, ruled BROKEN with seven triaged defects ([docs/reviews/v4-judge-rulings.md](docs/reviews/v4-judge-rulings.md)) → fixes → re-witness on the judgment's exact grounds ([docs/reviews/v4-rewitness-record.md](docs/reviews/v4-rewitness-record.md)) → re-judge, MAY LAND with all four verdicts Kept and structure sound ([docs/reviews/v4-rejudge-rulings.md](docs/reviews/v4-rejudge-rulings.md)). The owner's three verbatim calls in [decisions.md](decisions.md); the shape committed before the code; the dev suite control re-run 4-of-4 red after the v4 regex changes; install measured 19 files on disk / ~55 KB, always-read 17.5 KB.
