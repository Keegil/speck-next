# The right roles show up

**Serves:** all eight contract promises through selective product-role separation, honest adoption, and repair of a missed call in the current piece · **Consumes:** the ratified selective promise in `work/shaping.md`, the ratified executable seam in `work/mapping.md`, the failed universal `6.0.0-rc.1` candidate, and the rule-home census below

**Outcome:** A fresh or upgrading repository receives `6.0.0-rc.2` and can decide which independent product lenses the work actually needs. Product and Engineering are always separate on substantial work. Business and Experience join when their own conditions, evidence, expiry, a material change, or uncertainty requires them. An inactive role leaves a factual reason, not a pretend contribution. A missed call stops and repairs the current piece. This piece proves those rules and the migration deterministically; it does not claim that live role carriers finish affordably.

**Before first run:** Set at role dispatch: no more than four carriers and one contribution from each; no more than 90 minutes from the live-piece commit; no more than 120,000 aggregate host-reported tokens; no more than 30 unique repository files read before the first deterministic product run. The carrier and one-contribution limits held. The three non-Product carriers alone used at least 6,822,314 gross and 423,210 fresh tokens, so the token limit failed before Product was counted. The host evidence does not expose a trustworthy normalized file-read total, so that limit is not judged. Planning stops here; Engineering's first action after this commit is the smallest honest run against an exact rc.1 migration fixture.

Token command, reading the last host token record from each role session:

```sh
python3 - <<'PY'
import json
from pathlib import Path
base = Path.home() / ".codex/sessions/2026/09/02"
runs = {
    "Business": "rollout-2026-09-02T18-30-33-01a062f5-3a3a-7da3-bb4d-026a3a7525b9.jsonl",
    "Experience": "rollout-2026-09-02T18-30-45-01a062f5-6783-7242-9370-28d9fa14e2dc.jsonl",
    "Engineering": "rollout-2026-09-02T18-30-58-01a062f5-99e0-7712-90d0-f5c0f8506e63.jsonl",
}
totals = dict(input_tokens=0, cached_input_tokens=0, output_tokens=0, total_tokens=0, fresh_tokens=0)
for role, name in runs.items():
    usage = None
    for line in (base / name).read_text().splitlines():
        payload = json.loads(line).get("payload", {})
        info = payload.get("info") if payload.get("type") == "token_count" else None
        if isinstance(info, dict) and isinstance(info.get("total_token_usage"), dict):
            usage = info["total_token_usage"]
    values = {key: int(usage.get(key, 0)) for key in ("input_tokens", "cached_input_tokens", "output_tokens", "total_tokens")}
    values["fresh_tokens"] = values["input_tokens"] - values["cached_input_tokens"] + values["output_tokens"]
    for key, value in values.items():
        totals[key] += value
    print(role, " ".join(f"{key}={value}" for key, value in values.items()))
print("LOWER_BOUND_THREE_ROLES", " ".join(f"{key}={value}" for key, value in totals.items()))
PY
```

It returned:

```text
Business input_tokens=1293551 cached_input_tokens=1196928 output_tokens=9911 total_tokens=1303462 fresh_tokens=106534
Experience input_tokens=2687604 cached_input_tokens=2575232 output_tokens=19258 total_tokens=2706862 fresh_tokens=131630
Engineering input_tokens=2787732 cached_input_tokens=2626944 output_tokens=24258 total_tokens=2811990 fresh_tokens=185046
LOWER_BOUND_THREE_ROLES input_tokens=6768887 cached_input_tokens=6399104 output_tokens=53427 total_tokens=6822314 fresh_tokens=423210
```

**Proof plan and review cost:** Before edits, reproduce the known rc.1 migration failure on an exact generated fixture. Then run deterministic clean and broken twins for Product-plus-Engineering only, Business active, Experience active, all four active, ambiguity, expiry, false inactivity and current-piece repair, an overdue informative return, replacement-carrier lineage and exclusions, and Product/Engineering separation. Run fresh install plus v5, generated rc.1, custom-section, missing-product, dirty-worktree, failure-and-retry, and second-upgrade fixtures. Every clean subject must pass and every one-field mutation must fail with its subject count printed. `node --check`, Python compilation without cache writes, the existing separated-team control, the complete home census, exact installed/always-read/file/skill budgets, `git diff --check`, and the public-detail scan must pass. No governed Codex or Claude run, broker run, host proof, or component model probe belongs to this piece. After Built, two fresh testers use the candidate: one first-time builder routes contrasting pieces, and one mid-product upgrader resumes real work. One independent judge challenges both records and rules that it works, delivers the promise, is good to use, hangs together as a quality product, and has sound structure. Business rules separately before landing. The second milestone judge remains in Piece 9's milestone hearing unless this piece's evidence makes the work risky.

## Role call decisions

| Role | Decision | Product-specific reason | Earliest informative run |
|---|---|---|---|
| Product | Active | Always active on substantial work; this piece resolves the installed promise, migration behavior, and proof boundary. | The contrasting deterministic fixtures select exactly the required roles and reject false inactivity. |
| Business | Active | The piece changes adoption, owner attention, token and time cost, maintenance, and durable method value. | The no-model fresh/v5/rc.1/custom/missing/dirty/idempotent matrix completes with zero owner repair, bounded footprint, and honest output. |
| Experience | Active | The piece changes always-read wording, templates, role selection, upgrade output, builder handoff, comprehension, and trust. | A disposable fresh install and mid-product upgrade let a builder route contrasting pieces, leave no fake inactive-role prose, and understand what blocks landing. |
| Engineering | Active | Always active and separate from Product; this piece changes installed rules, migration, reversibility, reporting, and deterministic proof. | An exact generated rc.1 fixture first reproduces the obsolete universal contract, then crosses to rc.2 twice while preserving every unrelated byte. |

## Pre-code contributions

### Product

- **Carrier:** `/root`.
- **Direct evidence:** the owner's ratified “Lock selective separation!” and “Do it!”; `product.md`; `map.md`; `decisions.md`; the repaired Shape and Map records; the 266,484-gross-token failed governed run; and the three contributions below.
- **Conclusion:** Install one selective contract everywhere, repair both v5 and obsolete rc.1 repositories honestly, and prove repository semantics without reopening live-host cost in this piece.
- **Assumptions:** deterministic fixtures can prove routing, record, migration, reporting, and footprint behavior but cannot prove live carrier obedience or cost; Piece 9 owns that claim; old-Speck conversion and the already-running external product remain out of scope.
- **Proposed change:** make Product and Engineering the substantial-piece floor; call Business and Experience through their authored conditions and evidence; record current-piece repair and informative returns; migrate to rc.2 marker-last and byte-stably; keep host transport untouched.
- **Active decision:** active. A wrong integration either loses a needed lens or preserves the universal ceremony already disproved by cost. The selection fixture is the earliest disconfirming run.

### Business

- **Carrier:** `/root/piece8_business`.
- **Direct evidence:** the stopped governed run at 266,484 gross tokens in 110 seconds; the current major-only upgrader classification; the current universal checker; disposable fresh-install and pre-v6 fixtures; installed and always-read footprint measurements; and `capabilities.md`'s open host and field claims.
- **Conclusion:** this is a useful, low-cost adoption boundary, but the installed rc.1 candidate remains Business broken until the deterministic migration and routing evidence exists. Piece 8 cannot turn the failed host run green.
- **Assumptions:** local Node fixtures faithfully test install, migration, reporting, idempotence, selection, and current-piece repair; exact generated rc.1 text can be distinguished from owner-authored text; no installed file, skill, owner beat, or host proof is added.
- **Proposed change:** replace the universal contract in every home; migrate v5 and rc.1 to rc.2; replace only the exact generated universal paragraph; preserve custom and historical bytes; report the exact version/commit span, paths, diff, and footprint.
- **Active decision:** active. A bad selector loses a business concern; a bad upgrader leaves existing products on the costly universal cadence. The migration and selection matrix is the earliest disconfirming run.
- **Pre-code ruling:** deterministic and local implementation may proceed. This is not a `kept` ruling for the finished piece, release, or live-host execution.

### Experience

- **Carrier:** `/root/piece8_experience`.
- **Direct evidence:** the current universal `AGENTS.md`, fixed four-row piece template, universal contract and checker; disposable fresh, v5, rc.1, custom-section, and missing-product journeys; and the upgrader's current lack of a clear resume action.
- **Conclusion:** the current fresh and upgrading journeys are broken for selective separation. Builders need a visible factual call decision, full detail only from called roles, immediate repair of a missed lens, and a plain next action after upgrade.
- **Assumptions:** owner-authored bytes remain in order; a generated assessment status can be added beside a custom section without claiming historical assessment; the first informative Experience evidence is a builder journey, not syntax or a technical smoke run.
- **Proposed change:** make role-owned conditions, evidence, expiry, and material-change examples visible in product and map templates; split call decisions from active contributions in the piece; add current-piece repair, informative-return due points, and replacement exclusions; make rc.2 migration distinguish generated rc.1, custom, missing, and already-selective products; finish output with a plain `Next:` action.
- **Active decision:** active. Without the change, builders either fill departmental prose or silently skip a fired lens. Fresh-install and mid-product-upgrade journeys are the earliest disconfirming runs.

### Engineering

- **Carrier:** `/root/piece8_engineering`, distinct from Product and retained for implementation and its return.
- **Direct evidence:** the selective authority in `product.md` and the ratified Shape/Map records; universal cadence in `AGENTS.md`, Shape/Map skills, templates, contract, README, and checker; executable installer defects around major-only v6 detection, heading-based suppression, marker-before-migration, and incomplete reporting; and the commit boundary separating installed semantics from Piece 9 transport.
- **Conclusion:** replace every universal rule home with one selective contract, migrate marker-last and byte-stably to rc.2, and prove it through deterministic controls only.
- **Assumptions:** Piece 8 migration sources are v5.x and exact `6.0.0-rc.1`; custom bytes remain untouched while a canonical unassessed status is added; deterministic proof does not claim host obedience; no new installed file, skill, dependency, state, or owner beat is needed.
- **Proposed change:** update the installed method, Shape/Map prompts, product/map/piece/state templates, eight contract promises, public description and capability claim, package and installer; add a `--piece-8-controls` path to the existing separated-team checker; leave all host runner, broker, host-proof, and live fixture files untouched.
- **Active decision:** active. Otherwise an rc.1 repository silently retains the rejected contract or a false inactive decision leaves tainted Built/review evidence standing. The exact rc.1 before/after/retry fixture is the earliest disconfirming run.

## Product synthesis and Engineering handoff

One installed rule governs the work. All four distinct carriers form or reopen the product and take the first Map after Shape. Later re-maps call Product and every role whose protected concern, evidence, expiry, or ordering changed. Every substantial piece has distinct Product and Engineering carriers. Business and Experience join before the affected decision or code when their authored condition fires, evidence is missing or expired, a listed material change occurs, the answer is uncertain, or the role declares an effect. Product cannot waive those facts.

An inactive Business or Experience role records only its condition and the direct current evidence that kept it out. It has no carrier, conclusion, assumptions, or proposed change. Active roles keep the full contribution. Each active role names the earliest run that can actually test its conclusion and returns there; once that run exists, a missing return leaves the concern unresolved and blocks landing. Replacement carriers inherit the same evidence and prior contribution, and both original and replacement remain excluded from testing and judgment.

False inactivity repairs the current evidence chain first. Work stops and returns to Shape for a wrong promise, Map for a wrong piece or order, or piece setup otherwise. The missed role joins for the rest of the piece, Product re-synthesizes before Engineering resumes, and every affected Built line, receipt, tester verdict, and judgment becomes invalid. New work, a new Built line, and a new receipt are required. Unrelated evidence remains. The role is also mandatory on the next comparable piece; a repeat keeps it through the milestone.

Engineering owns the implementation listed in its contribution. Product owns only this work record and later product/map/decision/state truth. Engineering must first run the exact generated rc.1 control, then make the smallest installer-and-contract repair and rerun that same fixture twice before widening to the full matrix. The version is `6.0.0-rc.2`. The marker is written only after successful copying and migration. Missing `product.md` stays missing. Exact generated rc.1 prose is replaced; custom prose is preserved and receives one honest unassessed status without suppressing migration. Output names both ends, both commits when known, the migration outcome, working-tree paths across the complete installed surface plus `product.md`, and a plain next action. `capabilities.md` may claim only deterministic routing/migration proof; live hosts and field value stay open.

The home census used the case-insensitive families `product…business…experience…engineering`, `four roles|four separate|all four`, `every substantial piece`, `active|inactive role`, `first real run`, `product team`, `excluded contributors`, and `contributed as` across `AGENTS.md`, every skill and reference, every template, `CONTRACT.md`, `README.md`, `capabilities.md`, the installer, package, and separated-team task. Engineering changes `AGENTS.md`; the Shape and Map skills and question references; `templates/product.md`, `templates/map.md`, `templates/piece.md`, and `templates/state.md`; `CONTRACT.md`; `README.md`; `capabilities.md`; `package.json`; `bin/speck-next.js`; and `devsuite/tasks/separated-product-team/check.py`. It leaves `CLAUDE.md`, Craft, Experience and Judge and their references, `templates/decisions.md`, `templates/rounds.md`, every Product-owned record, and every Piece 9 runner/broker/host fixture untouched. The generic rounds template remains a conversation and receipt floor; the Shape and Map skills own their role-contribution fields.

**Preserved dissent:** Experience considers migration a distinct builder journey and insists on its own proof lane; Product keeps it inside Piece 8 because fresh and upgrading repositories consume the same installed selective contract, while keeping separate fixtures and a separate mid-product tester. Engineering proposed leaving the rounds template untouched; Product agrees after inspecting it because it states no universal attendance rule and delegates phase-specific records to the skills. The failed pre-run cost is not repaired by widening or reinterpreting the limit; it remains a finding for Piece 9's transport and context design.

## Informative returns

- **Product — carrier `/root`:** The contrasting routing subjects selected Product plus Engineering alone, added Business alone, added Experience alone, or called all four from the recorded conditions. Ambiguity, expired evidence, a false inactive call, a missing informative run, a missing return, replacement lineage, Product implementation, and same-carrier Product/Engineering each went red in their focused mutant. The selection rule held. The broader Product review did change the candidate seven times before this return: later re-maps had accidentally kept Engineering universal; a role could avoid returning by never creating its named run; the handled-concern miss rule had disappeared from the always-loaded page; a capability row exposed concrete fixture details; a missing-product upgrade gave an impossible next action; a clean retry told the builder to commit nonexistent changes; and the first porcelain status line lost its leading column. Engineering repaired each finding and the complete matrix passed again. **Resulting product change:** selective delivery now covers later re-maps, both halves of an informative return, both miss paths, abstract public evidence, state-aware upgrade guidance, and exact working-tree status. No Product concern remains unresolved before Built.
- **Business — carrier `/root/piece8_business`:** Eleven migration subjects covered fresh install, v5, exact generated rc.1, custom Product-team prose, missing product, assessed current product, dirty work, failed retry, an unknown version, a clean retry, and exact changed-path output. They completed without an owner question or manual repair; the 19-file / 76,998-byte fresh footprint, five skills, and 46,552-byte always-read surface remained inside the contract. The previous 266,484-gross-token governed run and this piece's 6,822,314-gross-token three-role setup lower bound remain failed evidence, not reclassified success. **Binding ruling: `kept` for Piece 8.** The selective repository and adoption boundary earns its cost and may proceed to Built and review. Live carrier execution, Piece 9, the milestone, and release remain `not judged`; this ruling does not permit any of them.
- **Experience — carrier `/root/piece8_experience`:** The first fresh-install and mid-product-upgrade walk found the core selection flow understandable but returned unresolved at `78857f9`: a repository with no `product.md` was told to run an assessment named in that missing file, and the first unstaged changed path looked staged because one status column was trimmed. At `737959a`, the same journey received one possible next action for every product state: create and ratify a missing product before Map, finish a pending product-and-map assessment, or resume the assessed product from `state.md`; a clean retry explicitly had nothing to commit, and the first porcelain line retained both columns. The selective call records, factual inactive rows, immediate false-inactive repair, and missing-run/return landing blocks also held in the executed subjects. **Result:** sufficient before Built; no Experience concern remains unresolved.
- **Engineering — carrier `/root/piece8_engineering`:** Before edits, an exact generated rc.1 fixture reported rc.1 to rc.1, left one universal paragraph and zero selective paragraphs, and said migration was not needed. After the repair, that fixture crossed to rc.2, replaced only the generated paragraph, preserved adjacent bytes, and stayed byte-stable on retry. The expanded routing and migration matrix, syntax checks, complete diff, home census, footprint, and leak control all passed at `737959a`. Experience's running journey changed Engineering's earlier conclusion and produced the state-aware `Next:` table plus exact porcelain output. **Result:** implementation held; no Engineering concern remains unresolved before Built.

The final Product readback ran these commands after all returns:

```sh
python3 devsuite/tasks/separated-product-team/check.py --piece-8-controls .
./devsuite/run.sh --control separated-product-team
node --check bin/speck-next.js
python3 -c 'from pathlib import Path; compile(Path("devsuite/tasks/separated-product-team/check.py").read_text(), "check.py", "exec")'
git diff --check ddb62f1..HEAD
node -e 'const p=require("./package.json"); if(p.version!=="6.0.0-rc.2") process.exit(1); console.log(p.version)'
find AGENTS.md CLAUDE.md .claude/skills templates -type f -print0 | sort -z | xargs -0 wc -c
wc -c AGENTS.md product.md map.md state.md
```

They returned `role-control subjects=26 clean=13 mutants=13`, `migration subjects=11`, and `Piece 8 controls: PASS`; the legacy control reported `1 of 1 tasks went red`; syntax, compilation, and diff checks returned zero; the package printed `6.0.0-rc.2`; the installed-method source was 17 files / 76,664 bytes; and the four always-read files totaled 46,552 bytes. A disposable Git repository then ran `node bin/speck-next.js install <fixture>` and returned 19 non-Git files / 76,998 bytes, marker `6.0.0-rc.2`, and no invented `product.md`. The added-line public-detail matcher was first watched matching its planted fixture, then returned zero matches over `capabilities.md` and zero over the complete installed-surface change.

The closing home census repeated the pre-code search families. Changed homes are `AGENTS.md`; Shape and Map skills plus their question references; product, map, piece, and state templates; `CONTRACT.md`; `README.md`; `capabilities.md`; `package.json`; the installer; and the separated-team checker. Inspected and intentionally untouched homes are `CLAUDE.md`; Craft; Experience and Judge plus their references; decisions and rounds templates; Product-owned records; and every Piece 9 runner, broker, host-proof, and live-fixture file. The only remaining exact universal paragraph is the rc.1 migration fingerprint and its control, never an installed rule.

## Review receipt

**Built line and commit:** `state.md` at `946b85a` says:

> **Built — Piece 8 “The right roles show up”:** product commits `10acad3`, `8bf17be`, `78857f9`, and `737959a` cover exactly `AGENTS.md`, `.claude/skills/shape-product/SKILL.md`, `.claude/skills/shape-product/references/questions.md`, `.claude/skills/map-build/SKILL.md`, `.claude/skills/map-build/references/questions.md`, `templates/product.md`, `templates/map.md`, `templates/piece.md`, `templates/state.md`, `CONTRACT.md`, `README.md`, `capabilities.md`, `package.json`, `bin/speck-next.js`, and `devsuite/tasks/separated-product-team/check.py`. The proof-plan commands and returned results recorded in `work/right-roles-show-up.md` pass selective routing and focused broken twins, current-piece repair, informative-run and return blocks, handled-miss escalation, replacement and exclusion, fresh install, eleven honest migration journeys, syntax, complete diff, footprint, and the public-detail boundary; the existing same-context control remains red. No product implementation commit follows `737959a`.

**Opened:** 2026-09-02, after `946b85a` and before either tester was dispatched.

**Candidate:** the exact product implementation ending at `737959a`; later commits `c88dc4a`, `946b85a`, and this receipt change records only role evidence, state, and the review request.

**Fresh tester 1 — first-time builder:** receives `product.md`, the Piece 8 promise and routing cases from `map.md`, and the installed candidate—not the product-role conclusions. They install into a disposable fresh repository, use the loaded method and templates to set up contrasting substantial changes, and decide whether Product/Engineering stay separate, Business/Experience appear only when facts call them, inactive roles leave no invented contribution, uncertainty cannot be waived, and the owner gets one understandable recommendation. They report what they actually ran and saw, the worst confusion, and a verdict.

**Fresh tester 2 — mid-product upgrader:** receives the same product promise plus supported v5, exact generated rc.1, custom-section, missing-product, dirty-work, and clean-retry journeys—not the product-role conclusions. They execute disposable upgrades, follow the printed next action, inspect preserved and changed bytes, and decide whether an existing builder can resume without fabricated history, manual surgery, or a false commit instruction. They report what they actually ran and saw, the worst trust failure, and a verdict.

**Judge:** after both tester records are committed, one fresh judge who built and tested none of this challenges their evidence and the candidate. The judge separately rules whether Piece 8 works, delivers the promise, is good to use, hangs together as a quality product, and has sound structure. A sufficient ruling must name every open item and its destination.

**Excluded contributors and prior readers:** `/root`, `/root/piece8_business`, `/root/piece8_experience`, `/root/piece8_engineering`, `/root/piece8_migration_audit`, `/root/piece8_contract_audit`, `/root/bounded_transport_review`, every earlier Shape or Map contributor/tester/judge, and any honest replacement carrier. Neither fresh tester nor the judge may come from this set, and the judge cannot be either tester.

## Result

The Piece 8 candidate was Built at `946b85a` from product commits `10acad3`, `8bf17be`, `78857f9`, and `737959a`; review opened at `a737eaa`. The first-time-builder verdict was sufficient and the mid-product-upgrader sent it back. The independent judge upheld and strengthened the send-back at round 1: the upgrade assessment has no executable completion/resume transition, quoted historical status can trap a completed assessment as pending, and checkout provenance is presented as though it identifies the installed surface. Piece 8 remains live and returns to build. Host affordability remains Piece 9's unresolved concern and cannot be inferred from Piece 8.

## Fresh testing — round 1

### First-time builder — `/root/piece8_fresh_builder`

**Candidate used:** product implementation through `737959a`; `git diff --exit-code 737959a..HEAD -- AGENTS.md CLAUDE.md bin templates .claude/skills` returned zero before the walk.

**What happened:** `node bin/speck-next.js install <fresh-git-fixture>` installed rc.2 with 19 files. Reading only the installed method and templates, the tester created four contrasting piece setups. The internal change called Product and Engineering; the builder-visible handoff added Experience; the adoption, cost, and journey change called all four; and an ambiguous consequence also called the affected roles. Every setup kept Product and Engineering separate, used one Product synthesis, required a named informative run and return, and left inactive roles with evidence but no carrier or contribution prose. A deliberate invalid setup tried to dismiss uncertain shared-cache consequences with Product's opinion. The installed rule stopped at setup, called both affected roles, withdrew the synthesis, and required a new synthesis before Engineering; because no build existed, no later evidence needed invalidation.

**Worst confusion:** the installer printed the source checkout `a737eaa`, while the last product-changing commit was `737959a`. `git diff --exit-code` proved the installed surface identical, so the tester treated this as provenance wording rather than a functional failure and left it for the judge.

**Verdict: SUFFICIENT.** Selective call decisions were understandable and enforceable from the installed product. Live carrier execution remains Piece 9.

### Mid-product upgrader — `/root/piece8_midproduct_upgrader`

**Candidate used:** a disposable clone detached at `737959a`.

**What held:** Real v5.4.1 and exact generated rc.1 repositories crossed to rc.2; rc.1's generated universal paragraph was replaced exactly once; custom Product-team bytes remained an exact prefix beside one generated status; missing `product.md` stayed missing and routed to Shape; dirty state/work hashes survived; retry was byte-stable; printed scoped status and diff exactly matched Git; the first status entry retained both porcelain columns; and a clean current retry said there was nothing to commit before resuming from `state.md`. An adversarial `product.md` directory left a visibly partial method copy but kept the old marker, so retry honesty held while failure presentation remained rough.

**Blocking experience:** after an ordinary supported upgrade, the generated status and `Next:` line require a product-and-current-map assessment but the installed surface never says which record receives the four carrier contributions, where Product writes the synthesis, what explicit edit completes `Unassessed`, or how the existing live piece resumes. The tester ran `rg -n -i 'upgrade status|product-and-current-map|assess the existing product|unassessed|assessment.*(complete|finish|close|record|replace|remove)|clear.*assessment|migration' AGENTS.md .claude/skills templates product.md`; only the generated paragraph was actionable. Leaving it repeats “finish” forever; deleting it silently changes the upgrader to “resume.”

**Verdict: SEND BACK.** Define the assessment record, its required evidence and synthesis, the explicit status transition, and the return to the existing live piece; then rerun this journey.

## Judgment — round 1

**Judge:** `/root/piece8_judge_round1`, disjoint from both testers and every role, builder, prior reader, and auditor.

**Candidate challenged:** product through `737959a`; Built line `946b85a`; receipt `a737eaa`; tester records `5135238`.

**What the judge ran and found:** A disposable supported v5 upgrade reproduced the printed instruction to run the pending product-and-current-map assessment. A search across installed `AGENTS.md`, skills, templates, `product.md`, and `state.md` found no instruction naming its record, Product synthesis, completion edit, or no-reopen resume path beyond the generated status paragraph. The judge then completed an assessment in natural historical prose while retaining the old status as a quote; the installer's raw exact-sentence search still reported the assessment pending. A fresh install from `5135238` and one from `737959a` produced identical installed method bytes apart from the marker while reporting different commit values, so that field identifies the source checkout rather than the last installed-surface change.

**Tester challenges:** The first-time builder's routing evidence held, but their provenance note is material to exact traceability. The mid-product upgrader's send-back held and the historical-quote attack widened it from missing guidance to a brittle status mechanism.

**Rulings:** works — insufficient · delivers the promise — insufficient · good to use — insufficient · quality hangs together — insufficient · sound structure — insufficient.

**Route back:** Piece 8 build. The product promise and two-piece order still hold. Before Engineering edits, Product integrates the delta with Business, Experience, and Engineering because adoption, handoff, trust, migration safety, and provenance changed after those roles had marked their concerns handled. Their old returns and Business ruling describe `737959a`, not the repaired candidate. This first handled-concern miss makes the affected roles mandatory at the next comparable piece's key decisions and runs; Piece 9 already calls both Business and Experience.

**Required fix and re-run:** Name the upgrade assessment record, the four carrier contributions and Product synthesis it receives, the explicit completion status/action, the wrong-promise and wrong-piece/order reopen paths, and the no-reopen return to the existing live piece. Replace the raw paragraph-presence gate with explicit state that quoted history cannot hold open. Report checkout provenance honestly or separately identify installed-surface provenance. After the fix, rerun the mid-product tester's complete supported-upgrade population; their grep must find executable assessment/resume instructions outside the generated status. Rerun the first-time builder's full contrasting routes and uncertainty-stop mutant if any installed always-read page, template, or output changes. Add the judge's skeptical attack: completed natural historical prose that quotes or paraphrases old status must remain complete. Then write a new Built line, open a new receipt, use fresh testers, and re-judge from a fresh context.

**Open items:** assessment completion/resume and provenance return to this Piece 8 fix batch. Live-host affordability remains Piece 9.

## Repair setup after judgment round 1

### Active role deltas

**Product — carrier `/root`:** The promise and piece boundary hold; the defect is a missing installed transition. Product accepts the judge's three findings and the distinct role consequences below. The repair must make one upgraded-product assessment executable and explicit without backfilling history, creating an owner decision on the no-reopen path, or weakening selective calls. Product remains active through the repaired migration matrix.

**Business — carrier `/root/piece8_business`:** Direct comparison proved `737959a..a82ee0a` changed no product surface. A completed-assessment fixture retaining the old pending paragraph as history still printed the pending instruction, and two identical non-marker installations reported different bare commits. The resulting permanent loop and ambiguous provenance break adoption, owner attention, and durable trust. The unrepaired Piece 8 candidate is Business `broken`. Engineering may make the narrow repair after synthesis; Business returns and rules again only after the full no-model migration matrix passes. The failed host and Piece 8 setup limits remain failed.

**Experience — carrier `/root/piece8_experience`:** The prior sufficient return is explicitly disproved. An upgraded builder needs one visible lifecycle: a canonical block in `product.md`; a named `work/product-team-assessment.md`; four distinct contributions and one Product synthesis there; one explicit route to Shape, Map, or the existing live piece; and a committed completion status that deletion cannot impersonate. Wrong promise reopens Shape, wrong piece/order reopens Map, and no reopen names and resumes the existing live piece from `state.md`. The installed page must explain the transition, and output must point to it. Experience returns after a builder completes all three routes from installed instructions alone while quoted and paraphrased history stays inert.

**Engineering — carrier `/root/piece8_engineering`:** Whole-file paragraph search is the root defect. Engineering proposes one exact top-level assessment section whose status and record fields are parsed only within that section; a marker pointer distinguishes an upgraded product whose lifecycle must exist from a current product that never needed migration. Pending names the record. Complete accepts exactly Shape reopened, Map reopened, or a named live piece resumed from `state.md`, and requires the record to exist. Missing, duplicate, deleted, or malformed required state refuses visibly before the marker changes. Legacy generated prose remains only a migration fingerprint. Marker and output label the source checkout and add a SHA-256 over the sorted installed method surface; copied bytes must match that digest before the marker is written last. No dependency, installed file, template, skill, or Piece 9 path is added.

### Product synthesis and handoff

The assessment becomes a real, single-source state transition:

```md
## Speck Next upgrade assessment

**Speck Next upgrade assessment:** pending
**Record:** `work/product-team-assessment.md`
```

Installed `AGENTS.md` gets one short “Finish an upgrade” passage. Product creates the named record and writes the existing product, current map, state, and live piece it read; distinct Product, Business, Experience, and Engineering carriers; each role's direct evidence, conclusion, assumptions, proposed change, and active decision; one Product synthesis; and exactly one route. Product then commits the record, `product.md`, and `state.md` together, plus the reopening decision when required. Completion changes only the canonical status to one of:

```md
**Speck Next upgrade assessment:** complete — Shape reopened
**Speck Next upgrade assessment:** complete — Map reopened
**Speck Next upgrade assessment:** complete — resumed [live piece] from state.md
```

The record link remains. Completion is never inferred from removing words. The parser accepts exactly one unquoted top-level assessment heading, one canonical status, and one canonical record field before the next top-level heading. A completed state requires the named record. Once the marker says this migration lifecycle was opened, a missing, duplicate, or malformed block fails honestly rather than assuming completion. Quoted or paraphrased historical prose elsewhere is inert. A current fresh product with no lifecycle pointer remains current; the rejected rc.2 generated paragraph is repaired into the canonical block because rc.2 has not been released.

The marker writes `sourceCheckout`, `methodSurfaceSha256`, and the assessment-record pointer instead of presenting a bare `commit` as method identity. Legacy `commit` is read as a source-checkout fallback. Console output labels both source checkouts and both method-surface digests; a legacy digest says `not recorded`. The digest covers the sorted kernel-owned method surface and is verified after copy and before the marker is written last. The candidate stays `6.0.0-rc.2`.

Engineering owns `AGENTS.md`, `CONTRACT.md`, `README.md`, `capabilities.md`, `bin/speck-next.js`, and `devsuite/tasks/separated-product-team/check.py`. Product owns this record and later state truth. Skills, templates, `package.json`, Product records, and every Piece 9 runner, broker, host-proof, and live fixture remain untouched unless the pre-edit census proves a direct conflicting rule.

Before edits, Engineering runs and records red controls for: no executable “Finish an upgrade”/record/completion/routes in the installed surface; completed natural prose retaining the pending sentence still reported pending; missing/duplicate/malformed/deleted canonical state not failing closed; missing contribution, duplicate carrier, missing Product synthesis, and zero or two routes; and two source checkouts with identical method bytes reporting different unlabeled commits and no shared digest. The fixed population adds pending plus all three completion routes, quote and paraphrase attacks, record/block corruption, rejected-rc.2 repair, same-surface/different-checkout provenance, and every existing eleven migration journeys. Then rerun all 26 routing subjects, the legacy control, syntax, compilation, complete diff, budgets, home census, and the positive-control-backed public-detail scan. No host model or Piece 9 path runs.

The pre-edit census used `upgrade|migration|unassessed|product-team assessment`, `source commit|source checkout|method surface|marker`, `resume current|reopen Shape|reopen Map`, and `SELECTIVE_STATUS|versionWithCommit` across `AGENTS.md`, `CLAUDE.md`, every skill and reference, every template, `CONTRACT.md`, `README.md`, `capabilities.md`, installer, package, and checker. It returned the six owned homes above plus unrelated steady-state review, state, and migration wording. The six change; `CLAUDE.md`, all skills/references, all templates, package, and Product/Piece 9 records stay untouched for the reasons above.

**Preserved dissent:** Engineering adds a method-surface digest; Business would accept merely labeling the checkout. Product keeps the digest because it answers the tester's exact confusion without misnaming a Git commit, uses the standard library, and makes dirty-source or record-only checkout differences visible. It remains provenance, never a new acceptance state.

**Repair status:** Product synthesis is complete and committed before Engineering code. Business, Experience, and Engineering are active; the old Business ruling and Experience return are invalid for the repaired candidate. Their earliest informative runs are the complete deterministic migration matrix, the installed three-route builder journey, and the v5 no-reopen lifecycle plus provenance controls respectively.

## Repair informative returns

**Engineering — carrier `/root/piece8_engineering`:** At the named v5 no-reopen and provenance run, pending pointed to the explicit four-role/Product-synthesis record; Shape, Map, and the named live-piece completion routes all executed; exact quotes and paraphrases stayed inert; missing, duplicate, malformed, deleted, and recordless completed state failed before marker change; and two source checkouts with identical method bytes shared one digest. Engineering's earlier “held” conclusion is replaced by this evidence on `99a0f38`. No Engineering concern remains before the new Built line.

**Experience — carrier `/root/piece8_experience`:** The previous sufficient return on `737959a` remains disproved. On the repair, a disposable v5 product with a ratified product, current map, state, and live piece received the pending block and plain link to “Finish an upgrade.” Using only the installed page, Experience wrote the named record once, cloned it across the three routes, and saw clean retries continue Shape, continue Map, and resume the named live piece from `state.md`. Exact quoted and paraphrased rejected status remained in every product without reopening pending. A record-only source checkout changed while the method digest stayed identical and both labels were understandable. First-time selective routing still held. The lack of a record template caused one translation step but every required field was present and the record completed on the first pass. **Result: sufficient; no Experience concern remains before Built.**

**Business — carrier `/root/piece8_business`:** The 26 routing, eight assessment, and 21 migration subjects passed independently. All five incomplete assessment-record mutants went red; all corrupt lifecycle states failed without changing the previous marker; historical/custom/dirty bytes, retry stability, missing-product honesty, reporting, and the same-context control held. Two checkouts installed byte-identical non-marker surfaces with the same SHA-256 while their source checkout labels differed. The prior permanent-loop and ambiguous-provenance failures are closed. The failed 266,484-token governed run and Piece 8's failed setup limit remain failed and cannot support Piece 9. **Binding ruling: `kept` for Piece 8 candidate `99a0f38`, provided the required subtractive Built-state commit keeps the always-read sum at or below 50,000 bytes.** Piece 9, the milestone, and release remain `not judged`.

**Product — carrier `/root`:** Product read the exact six-file diff and reran the product boundary. The scoped parser ignores blockquotes and fences, completion retains rather than deletes its record link, the marker pointer makes later deletion fail, current fresh products do not inherit migration work, and the surface digest explains identical installed methods across record-only checkouts. The three role returns changed one Product action: with only 428 bytes of headroom before state changes, the new Built line must replace stale round-one state rather than append another chronicle. No product implementation change is required. Product remains active through the subtractive state measurement and fresh review.

The final Product commands were:

```sh
python3 devsuite/tasks/separated-product-team/check.py --piece-8-controls .
./devsuite/run.sh --control separated-product-team
node --check bin/speck-next.js
python3 -c 'from pathlib import Path; compile(Path("devsuite/tasks/separated-product-team/check.py").read_text(), "check.py", "exec")'
git diff --check c89886b..99a0f38
find AGENTS.md CLAUDE.md .claude/skills templates -type f -print0 | sort -z | xargs -0 wc -c
wc -c AGENTS.md product.md map.md state.md
```

They returned `role-control subjects=26 clean=13 mutants=13`, `assessment-control subjects=8 clean=3 mutants=5`, `migration subjects=21`, and `Piece 8 controls: PASS`; the existing control reported `1 of 1 tasks went red`; syntax, compilation, and diff checks returned zero; the method source was 17 files / 77,572 bytes with five skills; and the current four-file always-read sum was 49,572 bytes. The added-line leak matcher was watched matching its planted string, then returned zero over `capabilities.md` and zero over the complete public installed/doc surface.

**False inactive repair:** not triggered in Piece 8's actual work; the false-inactive controls still repair the current piece and invalidate only dependent evidence.

**Handled-concern miss escalation:** triggered once for Business and Experience. Both had marked adoption/handoff handled on `737959a`; fresh upgrade and judgment showed the consequential assessment-loop and provenance miss. Both therefore remain involved at Piece 9's key decisions and informative runs, which its existing Map calls already require. A repeat would keep the repeated role through the v6 milestone.

**Repair result:** implementation commit `99a0f38` changes exactly `AGENTS.md`, `CONTRACT.md`, `README.md`, `capabilities.md`, `bin/speck-next.js`, and the separated-team checker. The repaired candidate runs and all active roles have returned. Product replaced stale round-one state with the exact new Built boundary; `wc -c AGENTS.md product.md map.md state.md` returned 48,747 bytes, and the state-only Built commit is `940b8a5`. Review round 1 remains preserved as the failed control.

## Review receipt — round 2

**Built line and commit:** `state.md` at `940b8a5` says:

> **Built — Piece 8 “The right roles show up,” repaired candidate:** product commits `10acad3`, `8bf17be`, `78857f9`, `737959a`, and `99a0f38` cover exactly `AGENTS.md`, `.claude/skills/shape-product/SKILL.md`, `.claude/skills/shape-product/references/questions.md`, `.claude/skills/map-build/SKILL.md`, `.claude/skills/map-build/references/questions.md`, `templates/product.md`, `templates/map.md`, `templates/piece.md`, `templates/state.md`, `CONTRACT.md`, `README.md`, `capabilities.md`, `package.json`, `bin/speck-next.js`, and `devsuite/tasks/separated-product-team/check.py`. The commands and returns in `work/right-roles-show-up.md` pass the full routing, assessment, migration, preservation, provenance, failure, syntax, diff, footprint, and public-detail populations while the same-context control stays red. `wc -c AGENTS.md product.md map.md state.md` returned **48,747 bytes**, within 50,000. No product implementation commit follows `99a0f38`.

**Opened:** 2026-09-02, after `940b8a5` and before either round-2 tester was dispatched.

**Candidate:** exact product implementation ending at `99a0f38`; later commits `4401087`, `940b8a5`, and this receipt change only role evidence, state, and review records.

**Fresh tester 1 — first-time builder:** a new carrier receives the product promise, installed candidate, and round-1 judge requirements, never the role conclusions. Because `AGENTS.md` changed, they rerun the complete fresh install, four contrasting call decisions, Product/Engineering separation, factual inactive rows, one Product synthesis, named-run and return landing blocks, and the uncertainty-stop mutant. They also judge whether source checkout and method digest now make provenance understandable, then add one skeptical attack not named by the builder.

**Fresh tester 2 — mid-product upgrader:** a different new carrier receives supported v5, rc.1, rejected rc.2, custom, missing-product, dirty, retry, and corrupt-state journeys plus the round-1 grep floor, never the role conclusions. They follow only installed instructions through pending and all three completion routes, retain exact quoted and paraphrased old status, confirm completion cannot be deleted or fabricated, compare same-method/different-checkout provenance, and add one skeptical attack of their own. They report lived output, worst trust/comprehension failure, and a verdict.

**Judge:** after both round-2 records are committed, a third new carrier challenges them and reruns the round-1 requirement over its whole population plus the retained-history attack and each tester's skeptical attack. They separately rule works, delivers the promise, good to use, quality hangs together, and structure; every open item gets a destination.

**Excluded:** every carrier named in the round-1 receipt, `/root/piece8_fresh_builder`, `/root/piece8_midproduct_upgrader`, `/root/piece8_judge_round1`, and every Product, Business, Experience, Engineering, specialist, builder, or auditor carrier. The two round-2 testers and judge must be distinct from one another and this full set.

## Fresh testing — round 2

### First-time builder — `/root/piece8_retest_builder`

**Candidate used:** a read-only disposable clone detached at exact product candidate `99a0f38`. The source remained at receipt commit `1767e62` and was not edited.

**What happened:** Four fresh installations each reported rc.2, source checkout `99a0f38`, the same installed-method SHA-256, and 19 installed files. Four distinct fresh carriers classified an internal substantial change as Product plus Engineering, a builder-visible recovery change as Product plus Engineering plus Experience, an adoption-and-journey change as all four, and an ambiguous consequence as all four with product code stopped. Inactive Business and Experience decisions cited their product-specific condition and current evidence without inventing a carrier, conclusion, assumptions, or proposed change. Product and Engineering remained distinct. Every active concern named an informative run, and the installed method blocked landing when that run or return was missing.

**Round-one requirement and skeptical attack:** The tester found the source-checkout label and installed-method digest understandable, with only minor friction because the digest population is not defined inline. In the uncertainty-stop mutant, Product could not waive missing evidence and Engineering refused product code. As an independent attack, syntax passed for all four hypothetical runtimes, but no role accepted syntax as evidence for adoption, journey, freshness, or cost. A consequential disagreement about whether baseline evidence was needed remained for Product synthesis rather than being flattened into permission.

**Verdict: SUFFICIENT.** Selective routing, separation, inactive-role honesty, synthesis, informative-run returns, false-inactive repair, and provenance held for first-time use. Live carrier execution remains Piece 9.

### Mid-product upgrader — `/root/piece8_retest_upgrader`

**Candidate used:** exact product candidate `99a0f38` in disposable supported v5, rc.1, rejected rc.2, custom, missing-product, dirty, retry, corrupt-state, and completion-route fixtures. The source repository was not edited.

**What held:** Supported v5 and rc.1 repositories received one canonical pending block while owner-authored and quoted history stayed inert. Exact rejected-rc.2 repair preserved the product prefix byte-for-byte. Shape, Map, and named-live-piece completion routes finished and clean retries continued the correct route. Missing product routed to Shape; dirty owner work survived; successful status and diff output matched Git. Fabricated completion without its record, duplicate blocks, and malformed status all refused and kept the old marker.

**Blocking skeptical attack:** The tester first created the rejected rc.2 state through the supported v5 upgrade at `737959a`, then deleted its generated assessment section. Upgrading that fixture with `99a0f38` exited zero, said the current repository had never opened the one-time assessment, told the builder to resume from `state.md`, and advanced the marker. Deleting the outstanding evidence therefore impersonated non-applicability despite the installed promise that deleted required state fails. The tester also found that corrupt-state refusals occur after method files have already been copied: `product.md` and the old marker stayed unchanged, but the installed surface and working-tree status did not. The refusal is retryable but not byte-atomic.

**Verdict: SEND BACK.** A legacy rc.2 marker without the new lifecycle pointer is ambiguous: it can mean an unaffected current product or a rejected migration whose block was deleted. It must not be inferred safe. The upgrader must detect that ambiguity before changing installed bytes, preserve the prior marker and tree on refusal, and keep legitimate current products distinguishable. The fresh judge must decide whether this preflight requirement applies to every corrupt assessment state.

## Judgment — round 2

**Judge:** `/root/piece8_judge_round2`, disjoint from both round-2 testers and every excluded role, builder, tester, judge, and auditor carrier.

**Candidate challenged:** exact product candidate `99a0f38`; Built line `940b8a5`; receipt `1767e62`; round-2 tester records `b345795`.

**What held:** The Built and receipt chronology is valid and covers the exact product-file union. The complete deterministic population still passed on a disposable clone: 26 routing subjects, eight assessment-record subjects, and 21 migration subjects. Fresh routing, Product/Engineering separation, factual inactive rows, one Product synthesis, named informative runs and returns, uncertainty stops, all three completion routes, retained quoted history, and source-checkout versus installed-method provenance held in the evidence the judge challenged.

**What failed:** The judge independently recreated a v5 repository upgraded by the rejected rc.2 installer, deleted that installer's generated assessment section, and ran `99a0f38`. The command exited zero, claimed the repository never opened the assessment, told the builder to resume work, and advanced the marker. A healthy current rc.2 product produced the same classification. The damaged legacy state and legitimate current state are therefore indistinguishable to the candidate.

The judge also seeded an unsupported assessment-record pointer and a corrupt pending assessment with uncommitted dirt in an installed method file. Both commands refused and kept the old marker, but only after copying the method surface. The first left new installed files behind; the second overwrote the uncommitted method edit. “The version marker was not changed” therefore concealed a changed repository and, in the second case, lost user work.

**Rulings:** works — insufficient · delivers the promise — insufficient · good to use — insufficient · quality hangs together — insufficient · sound structure — insufficient.

**Route back:** Piece 8 build. Shape, Map, selective routing, the assessment lifecycle, and the two-piece boundary stand. The repair belongs to the upgrader's classification and preflight boundary.

**Required repair and complete re-run:** An old rc.2 marker without enough provenance to distinguish a healthy current product from deleted rejected-migration state must refuse before touching any byte unless direct evidence proves the safe classification. Every assessment-state refusal must validate before copying, retiring, creating, or overwriting installed files. Add controls for the exact rejected-rc.2 deletion journey; an unsupported assessment-record pointer; missing, duplicate, malformed, and deleted canonical blocks; and completed assessment without its record. Each refusal control asserts both the marker and the complete installed surface plus unrelated dirt are byte-identical. Re-run the full Piece 8 population, with every round-one control, retained-history attack, fresh routing route, and skeptical attack as floors, then add one new attack. Write a new Built line, open a new receipt, use fresh testers, and re-judge.

**Open items:** legacy rc.2 ambiguity and refusal atomicity return to this Piece 8 fix. Live-host affordability remains Piece 9. Business and Experience have now each suffered a second consequential miss after marking their concern handled; both stay continuously involved through the v6 milestone until milestone evidence supports relaxing them. Engineering's migration-safety conclusion was also disproved again; Engineering remains the separate implementing carrier and returns on the repaired refusal runs.

## Repair setup after judgment round 2

### Active role deltas

**Product — carrier `/root`:** The selective promise, assessment lifecycle, routes, and two-piece order still hold. The defect is one trust boundary: absence is treated as proof of safety, and that classification happens after mutation. Product keeps the working evidence from `99a0f38`, accepts both round-2 findings, and requires the existing marker field to state applicability explicitly. Product remains active through the paired refusal/success run and complete deterministic population.

**Business — carrier `/root/piece8_business`:** A deleted rejected-rc.2 assessment and a legitimate current product produced the same success classification and resume instruction. A malformed lifecycle with an uncommitted sentinel in `AGENTS.md` refused but erased the sentinel and changed the installed-method hash. That breaks adoption, owner attention, migration trust, and durable value. Because rc.2 is unreleased, refusing an ambiguous candidate costs less than silently skipping product judgment or destroying local work. Candidate `99a0f38` is Business `broken`; Engineering may make only this narrow repair after Product synthesis. Business returns on whole-target byte evidence. Piece 9 still owns host affordability.

**Experience — carrier `/root/piece8_experience`:** The previous sufficient return is withdrawn. The builder can trust neither “resume” nor “refusing” while missing evidence means safe and validation follows writes. Experience requires a read-only classification before mutation, an explicit required-or-not-required marker disposition, a specific ambiguity message that says Speck will not guess, no resume instruction on failure, and exact confirmation that nothing—including local method edits—changed. The paired journey is one ambiguous old rc.2 refusal beside one explicitly safe current success. This carrier stays active through the v6 milestone and remains excluded from testing and judgment.

**Engineering — carrier `/root/piece8_engineering`:** The upgrader collapses every v6 marker into `current`; a missing/falsy record pointer plus no recognized status becomes “never opened.” It copies the method, retires a skill, and creates the starter map before parsing assessment state. Existing corrupt-state controls compare only marker bytes. Engineering proposes a pure preflight planner before all target writes, followed by the existing successful copy/apply/marker-last sequence. It also found that the on-disk completion fixture omits Experience while passing because the runtime checks only record existence; the fixture must contain and assert the complete four-role record contract. Engineering owns the six existing homes named below after this synthesis.

### Product synthesis and handoff

The repaired marker makes the existing `upgradeAssessmentRecord` field total:

- `null` means this fixed installer knows no one-time migration assessment applies;
- `work/product-team-assessment.md` means the assessment applies and the canonical block must exist;
- a missing field on a current rc.2 product is unknown legacy state, never evidence that assessment was unnecessary.

A fixed fresh install and every successful upgrade write one of the first two values. A missing product can safely route to Shape and write `null`, because no product work resumes and Shape itself supplies the separated assessment. A current product with a missing field may proceed only when a surviving canonical block or exact generated migration status proves the required path. Otherwise it refuses before touching the target. This includes both old `commit`-only markers and newer candidate markers: `99a0f38` can itself launder deleted legacy state into new provenance fields, so `sourceCheckout` plus `methodSurfaceSha256` does not prove that assessment was never required.

That last sentence preserves the consequential dissent. Engineering and Business would accept the two provenance fields as the healthy-generation boundary. Product instead takes Experience's explicit disposition because the judged failure already demonstrates a route that produces those fields after an assessment was deleted. rc.2 is unreleased, so refusing any fieldless current candidate is the honest and reversible cost. This adds no method state, phase, file, skill, dependency, or owner beat; it completes an existing marker field.

Before the first target mutation, one read-only plan validates: marker field presence and exact value; product absence/file kind; the unique unquoted canonical section; exact status and record fields; the named completed record; duplicate generated rc.1 or rejected-rc.2 fingerprints; and the unknown-current classification above. It returns the planned product bytes, assessment route, record value, and message without writing. Only a successful plan may copy and verify the method surface, retire the old skill, create a missing map, apply planned product bytes, and write the marker last. Known assessment refusal says the concrete defect, says nothing in the repository changed, gives a repair-and-retry action, and never tells the builder to resume. Arbitrary filesystem failure remains a retryable marker-last boundary; this repair does not claim a multi-file transaction against concurrent or I/O failure.

The checker first watches a complete-target snapshot detect a planted byte change. It then proves the pre-fix candidate red for: the exact `737959a` rejected-rc.2 deletion; a fieldless current product; unsupported or empty record values; pointer plus missing product; product as a directory; missing, duplicate, or malformed canonical block/status/record field; completed state without its named record; and duplicate generated fingerprints. Every refusal plants installed-method dirt, tracked unrelated state dirt, and untracked work, then compares marker bytes, all non-`.git` path kinds and bytes, and porcelain-v1-z output before and after. The healthy twin is created by a fixed install carrying explicit `null` and must upgrade normally. The actual completion-route record must contain Product, Business, Experience, and Engineering with distinct carriers, one Product synthesis, and exactly one route.

After those controls turn green, rerun all 26 routing subjects, all eight assessment subjects, every prior 21 migration journey rebuilt where necessary from an explicit fixed marker, every round-one and round-two attack, the same-context control, syntax, compilation, full diff, footprint, changed-home census, and positive-control-backed public-detail scan. No governed host/model or Piece 9 path runs.

Engineering owns `AGENTS.md`, `CONTRACT.md`, `README.md`, `capabilities.md`, `bin/speck-next.js`, and `devsuite/tasks/separated-product-team/check.py`. The first three describe explicit marker applicability and truthful untouched refusal; capabilities carries only abstract measured evidence. Skills, templates, package, Product records, and every Piece 9 file remain untouched unless the pre-edit census proves a direct conflict. The candidate remains `6.0.0-rc.2`.

**Earliest informative runs:** Engineering runs the paired ambiguous-refusal/explicit-safe-success subjects first, then the table-driven refusal population. Experience reads the exact paired output and before/after bytes. Business rules only after that population and the complete Piece 8 matrix pass within the 50,000-byte always-read ceiling. Product reads the diff and integrates their returns before a new Built line.

**Continuous involvement:** Business and Experience have each reached the repeat condition. They remain active for this repair, Piece 9's decisions and informative runs, and the v6 milestone hearing until measured milestone evidence supports relaxing them. Engineering remains separately carried on every substantial implementation and returns on this repair's first informative run.

**Repair status:** Product synthesis is complete and committed before Engineering code. Candidate `99a0f38` remains broken; no Built, review, landing, Piece 9, or release progress is permitted until the repair runs and every active role returns.

## Repair delta before Built

### First refusal-repair run

**Engineering — carrier `/root/piece8_engineering`:** Implementation `9273500` made `upgradeAssessmentRecord` explicit as `null` or the canonical record path and moved every known assessment classification into a pure preflight before target writes. The paired ambiguous/explicit-safe run held; all invalid states preserved the marker, every non-Git path kind and byte, unrelated dirt, and porcelain-v1-z; and the real completion fixture now contains all four distinct role carriers, one Product synthesis, and one route. The complete population passed: 26 routing, eight assessment, and 36 migration/refusal subjects; the same-context control stayed red. Fresh footprint was 19 files / 78,511 bytes, five skills, and 48,967 always-read bytes. Engineering found no remaining data-safety concern in that repair.

**Business — carrier `/root/piece8_business`:** Independent snapshots confirmed the same byte-atomic refusal and healthy explicit-null success. Business initially ruled `kept` for `9273500`, conditional on the later Built-state byte sum. A legitimate fresh product from the rejected rc.2 era then disproved that ruling: neither deleted assessment evidence nor an explicit disposition had ever existed in its history, so the printed restore action was impossible. Business withdrew `kept`, ruled `9273500` `broken`, and authorized only an executable conservative recovery. Automatic assessment spends the failed four-role cost without consent; permanent refusal transfers marker surgery to the builder; one supported retry is the smaller adoption cost.

**Experience — carrier `/root/piece8_experience`:** The atomicity journey held, but the same legitimate fieldless product was stranded by an impossible repair instruction. Experience kept Piece 8 blocked and required the refusal itself to name an executable path. The accepted path is an explicit `--open-assessment` retry that preserves historical product bytes, creates one pending canonical block and pointer, names the record, contains no resume wording, and later reaches the ordinary Shape, Map, or exact-live-piece completion route.

### Product synthesis and second handoff

The ordinary command remains the safety boundary. An ambiguous current rc.2 product still refuses before any write. Its final lines say that nothing—including local work—changed and print the exact supported retry:

```text
Next: run the upgrade again with --open-assessment to conservatively open the one-time assessment. Product work will not resume until that assessment records its route.
```

`speck-next upgrade [dir] --open-assessment` accepts the exact flag once, before or after its single optional directory. Unknown or duplicate options, extra paths, and the flag with `install` refuse before target access. The flag does not mean “ignore validation.” The preflight first rejects every malformed pointer, missing or non-file product, canonical-state defect, missing completed record, duplicate generated fingerprint, or orphan `work/product-team-assessment.md`. It is eligible only when the repository is current rc.2, `upgradeAssessmentRecord` is absent, `product.md` is a regular file, and no canonical block, canonical record, or generated migration status survives.

For that exact ambiguity, the flag deliberately chooses the expensive safe side. In memory it appends one canonical pending block, selects the canonical record path for the marker, and prepares the specific migration message. Only after the plan succeeds may the normal copy, verification, retirement, map, product, and marker-last writes run. It never writes `null` and never resumes product work. A second ordinary upgrade is byte-stable; a second flagged attempt refuses because the repository is no longer ambiguous. Valid explicit-null, valid required, pre-v6, rc.1, missing-product, and generated-status subjects use their ordinary path and cannot be forced through this recovery.

This resolves the only remaining consequential dissent. Automatic conservative migration would be simpler but would summon four roles without an explicit choice on every legitimate ambiguous candidate; permanent refusal is safe but unusable. The narrow retry makes the conservative cost deliberate and executable without adding an owner decision or manual file surgery. It adds one CLI flag, not a method state, installed file, skill, dependency, or Piece 9 behavior.

Before edits, Engineering runs the legitimate fieldless subject with the proposed flag and records the current failure and unchanged bytes. After the fix, the required first pair is ordinary atomic refusal followed verbatim by flagged pending success on both a commit-only legacy marker and a `99a0f38`-provenance-laundered marker. Each preserves the original product prefix and unrelated dirt; the marker points to the record; output contains no resume; completing the no-reopen record later resumes the exact live piece. A table then rejects flag use with every ineligible or corrupt state unchanged, checks both flag positions and argument errors, and proves ordinary retry stability. All 26/8/36 prior subjects and every earlier skeptical attack remain floors, followed by control-red, syntax, compilation, diff, budgets, census, and the public-detail scan.

Engineering owns the same six homes: `AGENTS.md`, `CONTRACT.md`, `README.md`, `capabilities.md`, `bin/speck-next.js`, and the separated-team checker. Product records, skills, templates, package, and Piece 9 remain untouched. The candidate stays rc.2. Business, Experience, and Engineering return on the refusal → flagged pending → completed-route journey before Product may write a new Built line.

## Final recovery informative returns

**Engineering — carrier `/root/piece8_engineering`:** Pre-fix, `9273500` ignored the proposed fourth argument and could only preserve a stranded product. Implementation `a20a537` adds strict argument parsing and a pure, narrowly eligible recovery branch. Both commit-only legacy and `99a0f38`-provenance-laundered subjects refused unchanged, accepted the flag before or after the directory, preserved the product prefix and unrelated dirt, wrote one pending block and canonical pointer, rejected a second flagged attempt, stayed byte-stable on ordinary retry, and completed to the exact named live-piece route. Every corrupt, determined, missing, generated, orphan, linked, wrong-version, duplicate-option, unknown-option, and extra-path case refused before target access or mutation. Engineering permits Built and leaves host transport to Piece 9.

**Experience — carrier `/root/piece8_experience`:** A legitimate fieldless product whose history contained neither assessment evidence nor disposition followed the printed command verbatim. Ordinary upgrade refused with the complete snapshot, marker, and porcelain bytes identical. The flagged retry preserved its original product bytes, opened exactly one pending assessment, named `work/product-team-assessment.md`, and contained no resume instruction. Completing the no-reopen route then said exactly to resume the named live piece. The laundered history followed the same chain; explicit-null rejected the flag untouched and resumed normally. Experience found no remaining Piece 8 journey change. This carrier remains continuously involved through Piece 9 and the milestone and excluded from fresh review.

**Business — carrier `/root/piece8_business`:** The same recovery ran from a real fresh rejected-era install through ordinary refusal, flagged pending state, four-carrier completion record, and exact resume with no manual product or marker edit. Supported healthy, migration, custom, dirty, retry, and completion paths held; the flag could not override any decided or malformed state. Fresh installation measured 19 files / 78,696 bytes and five skills. The public-detail matcher hit all ten planted names and zero candidate additions. The recovery adds one deliberate retry only on the ambiguous prerelease path, avoids automatic four-role cost, and needs no owner decision. **Binding ruling: `kept` for Piece 8 candidate `a20a537`**, permitting a subtractive Built commit and fresh review only. Piece 9, the milestone, tag, and release remain `not judged`.

**Product — carrier `/root`:** Product read the exact six-file diff and reran the complete committed population. `python3 devsuite/tasks/separated-product-team/check.py --piece-8-controls .` returned 26 routing subjects (13 clean, 13 mutants), eight assessment subjects (three clean, five mutants), 84 migration/refusal/recovery/argument subjects, and `PASS`. `./devsuite/run.sh --control separated-product-team` returned one of one controls red as required. `node --check bin/speck-next.js` and `git diff --check aa88611..a20a537` returned zero. The implementation changes exactly the six assigned homes; Product/Piece 9 files are untouched. The fresh marker carries explicit `null`, source checkout `a20a537`, and method-surface SHA-256 `27866d1356b19f5d2e3a2911965841f3b2fa9b8d96342df4ce0417f9d08b44fc`.

**What changed or held:** The run changed no further Product decision. Ordinary ambiguity remains fail-closed; recovery is deliberate, executable, and unable to bypass other evidence. Selective routing, four-role upgrade assessment, all three routes, provenance, preservation, and Piece 8/Piece 9 separation held. No Product implementation change remains before Built.

**Final deterministic boundary:** source method 17 files / 78,226 bytes; fresh install 19 files / 78,696 bytes; five skills; always-read before the final state rewrite 49,221 bytes. The new Built line replaces stale state rather than appending history, then the four-file sum is measured again. No governed host run or field-value claim is part of this result.

## Review receipt — round 3

**Built line and commit:** `state.md` at `f01337f` says:

> **Built — Piece 8 “The right roles show up,” final repaired candidate:** product commits `10acad3`, `8bf17be`, `78857f9`, `737959a`, `99a0f38`, `9273500`, and `a20a537` cover exactly `AGENTS.md`, `.claude/skills/shape-product/SKILL.md`, `.claude/skills/shape-product/references/questions.md`, `.claude/skills/map-build/SKILL.md`, `.claude/skills/map-build/references/questions.md`, `templates/product.md`, `templates/map.md`, `templates/piece.md`, `templates/state.md`, `CONTRACT.md`, `README.md`, `capabilities.md`, `package.json`, `bin/speck-next.js`, and `devsuite/tasks/separated-product-team/check.py`. The commands and active-role returns in `work/right-roles-show-up.md` pass 26 routing, eight assessment, and 84 migration/refusal/recovery/argument subjects; the same-context control stays red. `wc -c AGENTS.md product.md map.md state.md` returned **49,308 bytes**, within 50,000. No product implementation commit follows `a20a537`.

**Opened:** 2026-09-02, after `f01337f` and before either round-3 tester was dispatched.

**Candidate:** exact product implementation ending at `a20a537`; later commits `37f83a7`, `f01337f`, and this receipt change only role evidence, state, and review records.

**Fresh tester 1 — first-time builder:** a new carrier receives the product promise, installed candidate, and the required attacks from both prior judgments, never the role conclusions. They rerun a fresh installation, four contrasting call decisions, Product/Engineering separation, factual inactive entries, one Product synthesis, named-run and return landing blocks, uncertainty stop, and provenance comprehension. They confirm a healthy fresh marker records explicit `null`, ordinary later upgrade stays one-command, and `--open-assessment` cannot be used to force or bypass its state. They add one skeptical attack not named above.

**Fresh tester 2 — mid-product upgrader:** another new carrier receives the supported v5, rc.1, rejected rc.2, custom, missing-product, dirty, retry, corrupt-state, pending, and all completion routes plus both earlier judges' exact attacks, never role conclusions. They independently create both a legitimate commit-only fieldless product and a deletion laundered through `99a0f38`; ordinary upgrade must refuse before all target bytes, print an executable recovery, and never resume. Following the printed flag must preserve history/unrelated dirt, open one pending record, reject repeat or incompatible use, and later reach the exact named route. They compare exact target snapshots and output, exercise bad arguments before target access, and add one skeptical attack.

**Judge:** after both round-3 records are committed, a third fresh carrier challenges every tester claim and reruns both prior judgments' complete requirements plus each new skeptical attack. They separately rule works, delivers the promise, good to use, quality hangs together, and sound structure; every open item gets a destination.

**Excluded:** every carrier in the first two receipts and judgments; `/root/piece8_retest_builder`, `/root/piece8_retest_upgrader`, `/root/piece8_judge_round2`; every Product, Business, Experience, Engineering, specialist, builder, and auditor carrier; and any honest replacement. The two round-3 testers and judge are distinct from one another and this whole set.

## Fresh testing — round 3

### First-time builder — carrier `/root/piece8_round3_builder`

**Running product:** a disposable kernel clone pinned to product implementation `a20a537`, plus fresh product repositories installed from it. The tester received `product.md`, `map.md`, the Built line, and the committed round-3 receipt, not the product-role conclusions.

The tester used the installed piece template to set up four substantial changes: an internal cache change called distinct Product and Engineering only; user-facing empty-state copy added Experience; a pure operating-cost change added Business; and a cross-cutting alert called all four. The records kept inactive roles factual, separated Product from Engineering, put one Product synthesis before implementation, and exposed the named-run, missing-return, false-inactivity, handled-miss, and contributor-exclusion blocks. Their free skeptical attack tried to classify user-facing pricing copy as Business-only; the recorded Experience condition fired, so the product could not launder that change through the narrower call set.

On the actual CLI, fresh install wrote 19 files and an explicit `upgradeAssessmentRecord: null`. After a minimal product started, ordinary upgrade gave the one-command `resume current work from state.md` route; `--open-assessment` could not override that decided state. A v5 product opened one pending assessment and, after a valid four-carrier record, resumed the exact named live piece. A fieldless rc.2 product refused untouched, printed the recovery command, opened exactly one pending assessment only when that command was followed, and rejected a second flagged attempt. The support sweep returned 26 routing, eight assessment, and 84 migration subjects with `PASS`.

**Verdict: SUFFICIENT.** The first-time path called the right lenses without fake departmental prose and remained understandable through install, normal resume, pending assessment, and conservative recovery. No finding needs a destination.

### Mid-product upgrader — carrier `/root/piece8_round3_upgrader`

**Running product:** independently constructed v5, rc.1, rejected-rc.2, current-rc.2, dirty, custom, missing-product, pending, completed, malformed, and recovery fixtures against implementation `a20a537`. The tester received the same cold review surface and no role conclusions.

For a legitimate fieldless rc.2 product, ordinary upgrade exited 1 and printed both `Nothing in the repository changed.` and the exact `--open-assessment` route. The full non-Git digest stayed `da0abaf6f205df1e2b1de61dce4eea8d50f0d71b8d18229647c98c75e06a7f64`, the marker hash stayed `0749b8e904bf96bf0c7f7254bda616d4e9e8fc9130653cfdef6cf767da25a79d`, and porcelain-v1-z bytes were identical. Following the printed flag preserved unrelated dirt, appended exactly one pending block, wrote the canonical record pointer, emitted no resume instruction, and rejected repeat use. Valid four-carrier records then produced the exact Shape, Map, and `Piece alpha` routes.

The tester separately constructed the deleted-assessment history laundered through `99a0f38`; ordinary upgrade again preserved the whole snapshot, marker, and porcelain bytes before offering the same recovery. v5, exact rc.1, rejected rc.2, custom prose, missing product, explicit null, symlink, missing completed record, unknown version, bad arguments, marker-last retry, and same-surface/different-checkout provenance all behaved as stated. Identical installed bytes from source checkouts `9a1c0af` and `a20a537` shared method digest `27866d1356b19f5d2e3a2911965841f3b2fa9b8d96342df4ce0417f9d08b44fc` while retaining distinct source labels. Their free skeptical attack placed the rejected status inside a blockquote; quoted history remained inert and the explicit recovery still opened one canonical pending block. The support sweep again returned 26/8/84 and `PASS`.

**Verdict: SUFFICIENT.** The upgrader preserves history and local work, refuses unknown state without pretending it is complete, and gives every supported state an executable next action. No finding needs a destination.

## Judgment — round 3

**Judge — carrier `/root/piece8_round3_judge`:** The judge received the two committed user records at `846da65`, the receipt committed earlier at `9a1c0af`, the exact Built line at `f01337f`, and product implementation ending at `a20a537`. `git rev-list a20a537..HEAD -- <the 15 product files>` returned no later product commit. The judge had built, contributed to, and tested none of the piece.

The judge reran the earlier deletion, ambiguity, atomic-refusal, recovery, blockquote, and routing requirements. The Built boundary, receipt order, carrier exclusions, quoted-history defense, and 26 routing / eight assessment / 84 migration support population held. A new running attack found a missing Markdown boundary: `activeMarkdownLines()` ignores fenced and blockquoted text but not HTML comments. In one fieldless current rc.2 fixture, the rejected rc.2 status existed only inside `<!-- ... -->`; ordinary upgrade exited 0, rewrote the product to pending, and advanced the marker instead of refusing unchanged. In a second fixture, a complete canonical block existed only inside an HTML comment while its record file existed; ordinary upgrade exited 0 and printed the exact live-piece resume. Hidden history therefore became live assessment evidence.

**Rulings:** works — insufficient · delivers the promise — insufficient · good to use — insufficient · quality hangs together — insufficient · sound structure — insufficient.

**Route: Build.** Make HTML-commented generated statuses and canonical assessment blocks inert like blockquoted history. First preserve both running failures as controls: commented rejected status must refuse unchanged and print the exact `--open-assessment` action; commented complete block plus record must refuse unchanged and never resume. Re-run every prior judgment population, the ordinary and flagged recovery, both round-3 skeptical attacks, and the 26/8/84 floor. Then all active roles return, Product writes a new Built line, a new receipt names fresh testers, and a fresh judge rules the whole piece again.

## Repair setup after judgment round 3

### Four active contributions before code

**Product — carrier `/root` — active.** Product read the owner's selective-separation decision, `product.md`, the ratified two-piece map, current state, the round-3 running judgment, both fresh user records, and the three other roles' independent reproductions. Conclusion: this is one missing current-versus-history boundary inside Piece 8, not a wrong promise or piece cut. Assumptions: HTML comments intentionally hide their contents; conservative refusal is preferable to treating a mixed hand-authored line as lifecycle state; Piece 9 remains untouched. Proposed change: make the shared assessment classifier recognize comments, state the boundary in the three installed homes that already define inert history, and preserve every previous control.

**Business — carrier `/root/piece8_business` — active.** Business independently reproduced both candidate failures: a commented rejected status silently imposed the assessment cost, and a commented completed block resumed retired work. It measured the current always-read set at 48,582/50,000 bytes and kept the old governed-cost failures intact. Conclusion: hidden prose currently controls adoption, owner attention, and durable value. Assumptions: standard comments are inert and deterministic fixtures make no host-cost claim. Proposed change: one shared comment-aware classifier, byte-preserved comments, explicit installed wording, both judge fixtures plus a fence/blockquote boundary attack, and the complete existing population. Binding ruling: **`broken` for `a20a537`; narrow repair only is permitted.**

**Experience — carrier `/root/piece8_experience` — active.** Experience reproduced both unsafe successes and a multiline comment whose fence-like contents suppressed later current evidence under a naive ordering. Clean twins showed that comment delimiters inside a fence or blockquote must not start a top-level comment. Conclusion: builders reasonably understand `<!-- ... -->` as hidden history; invisible text must never open, complete, delete, or resume an assessment. Assumptions: comments may span lines, do not nest, close at the first `-->`, and an unclosed comment makes the remainder inert. Proposed change: name blockquotes, fences, and comments as inactive containers; keep the existing recovery; rename “unquoted copies” to “current copies”; test outer-comment precedence, inert examples, clean current blocks after close, and unclosed comments. Earliest informative run is that mixed-container journey. This carrier remains continuously involved through Piece 9 and the milestone.

**Engineering — carrier `/root/piece8_engineering` — active.** Engineering reproduced the commented status changing full digest `b6049763…` to `b90991bb…` and marker `47e4f53f…` to `4939d6cb…`, and the commented complete block changing full digest `6c006db5…` to `66360dab…` before printing `resume Piece alpha`. Root cause: `activeMarkdownLines()` tracks fences and leading blockquotes only; both generated-status removal and canonical parsing trust its result. Assumptions: no general HTML parser or dependency is needed; first-close comment semantics and fail-closed unmatched comments are sufficient. Proposed change: one left-to-right shared classifier, then exact failure controls, a container-boundary table, and every prior gate. Engineering owns implementation and remains separate from Product.

### Product synthesis and Engineering handoff

Assessment evidence is current only when its complete line lives outside a blockquote, a fenced code block, and a top-level `<!-- ... -->` comment. A live comment opener makes its entire line and following lines inactive through the first close; a line touched by a real comment remains inactive even if visible text sits beside the delimiter. Current evidence may resume on the next clean line. While a fence is open, comment-looking bytes are code. A blockquoted comment opener is quoted history and does not open a top-level comment. While a real comment is open, quote and fence markers are comment text and cannot escape it. A stray closer is plain text. An unclosed live comment refuses before any target byte changes, including flagged recovery, because appending a block would leave it hidden.

This preserves the consequential edge choice. Business asked to preserve active text after comments; Product interprets that as the next clean line after the close, not a partial mixed line. Parsing visible fragments on a comment-touched lifecycle line would add ambiguity for no product value. The cost is a safe refusal for unusual mixed-line authoring; the ordinary repair path remains explicit.

Engineering first records the two judge subjects red at `a20a537`. The repaired first pair must show: commented rejected status → ordinary whole-target atomic refusal with the exact `--open-assessment` instruction, then flagged recovery preserving the original comment prefix and opening exactly one current pending block; commented complete block plus record → ordinary and flagged atomic refusal with no resume. The skeptical table covers same-line and multiline comments, multiple closed comments followed by current state, comment delimiters inside fences and blockquotes, fence/quote markers inside comments, nested opener content, stray closer, and unclosed comment under ordinary and flagged calls. The blockquoted-status and pricing-copy attacks remain floors. Every existing 26 routing, eight assessment, and 84 migration/refusal/recovery/argument subject remains; no old subject may be replaced.

The rule lands coherently in `AGENTS.md`, `CONTRACT.md`, and `README.md`; `bin/speck-next.js` and the separated-team checker carry behavior and controls; `capabilities.md` changes only if measured evidence changes. No skill, template, package/version, Product record, or Piece 9 runner/broker/host file changes. After the exact pair and full population pass, Engineering, Experience, Business, and Product return before a new Built line. This synthesis is committed before code; Product hands implementation to `/root/piece8_engineering` now.

### Inline-code finding and second Engineering handoff

The first implementation, `cd1b45f`, made both judge subjects and the full 26/8/95 population green. Product did not accept it as Built. The required free skeptical attack showed that a valid literal `` `<!--` `` before a current assessment was mistaken for an unclosed top-level comment. The refusal preserved every byte, but its instruction to close a nonexistent comment stranded a valid product.

**Product — carrier `/root` — active.** Product read the committed diff, reran the 26/8/95 population, inspected the shared scanner, and checked the primary CommonMark code-span rules: equal-length maximal backtick runs may span line endings; unmatched openers are literal. Conclusion: `cd1b45f` fixed hidden comments but implemented raw-token detection rather than the current-Markdown boundary it promises. Assumptions: only a proven span can shield comment-looking bytes; block parsing bounds inline parsing; safety requires unmatched markup never to hide state. Proposed change: preserve `cd1b45f`, add inline-code recognition to the same classifier, and keep Piece 8 in Build.

**Business — carrier `/root/piece8_business` — active.** Business ran the ordinary and flagged inline-code fixture. Both refused atomically with the unclosed-comment message while the live pending block remained present. Conclusion: the product is safe but operationally stranded and asks for a manual edit to valid documentation. All earlier costs, public-detail scans, and 26/8/95 floors held. Binding ruling: **`broken` for `cd1b45f`; only the narrow inline-code repair is permitted.**

**Experience — carrier `/root/piece8_experience` — active.** Experience independently saw a marker-required completed assessment after `` `<!--` `` refuse instead of resuming `Piece alpha`. The installed words say top-level comments, so the running behavior contradicts what a builder reads. Conclusion: balanced inline-code examples are visible literal syntax, never hidden lifecycle state. Proposed change: add a live-current and an ambiguous-recovery twin plus a multi-backtick case; keep all comment journeys intact. Experience returns on that pair before Built.

**Engineering — carrier `/root/piece8_engineering` — active.** Engineering reproduced the false refusal with tree digest `2fcfa2dc…`, marker `cb1fff02…`, and empty-porcelain digest `e3b0c442…` unchanged. Both assessment consumers share the defective raw `indexOf("<!--")` boundary. Conclusion: recognize proven code spans once in the shared classifier. Assumptions: an unescaped maximal backtick run opens only when an equal-length maximal closer exists before the current inline region ends; an unmatched run is literal; backticks inside an already-open comment are inert. Proposed change: one bounded lookahead, mark every line touched by a proven multiline span inactive, then preserve the complete earlier population.

Product resolves the precise precedence as follows. On plain current text, whichever valid construct starts first owns its contents. A real comment opener seen first owns bytes through its first close. An unescaped maximal backtick run seen first shields comment-looking bytes only after a matching maximal run of exactly the same length is proven. The proof may cross soft line endings, but stops at a blank line or a new block: blockquote, fence, heading, list, indented code, or non-comment HTML block. If no closer is proven, the backticks are literal and scanning continues from them, so a later real comment can still open. An unpaired backslash escapes a prospective opener; backslashes inside a proven span do not escape its closer. Shorter or longer runs do not close it.

Every line touched by a proven multiline span is inactive as assessment evidence, even when one interior line exactly matches generated or canonical status text. Text after its closer is still scanned for a later real comment, though that mixed line itself remains inactive. Comments remain outer containers when already open; blockquotes and fences remain outer containers before inline scanning.

The pre-fix positive/negative pair is mandatory: balanced `` `<!--` `` before a current assessment changes from false refusal to the exact pending or completed route, while an unmatched opener before a real comment remains an atomic ambiguity refusal. The widened table covers one-, two-, and three-backtick spans; shorter internal runs; multiline spans; hidden exact generated and canonical lines; escaped and unescaped backtick cases; backslash before a closer; wrong-length and unmatched runs; every named block boundary; comment-first input; quote/fence isolation; a real comment after a balanced span; ordinary and flagged recovery preserving the literal example. Each is an integration subject. All 26/8/95 prior subjects remain individually green.

The installed rule adds inline code to the same current-evidence sentence in `AGENTS.md`, `CONTRACT.md`, and `README.md`; behavior and controls remain in `bin/speck-next.js` and the checker; `capabilities.md` changes only for measured results. No other home changes. Product re-hands implementation to `/root/piece8_engineering` after this record is committed. No candidate becomes Built until all four active roles return on the paired run and Business rules again.

### Public-detail label repair and third Engineering handoff

The repaired parser and full 26/8/126 population ran, and Experience found no remaining journey problem. Business then ran the required positive-control-backed public-detail boundary and found one generic test term colliding with a real product name: two matches in this record and fourteen across the Piece 8 record plus checker identifiers and labels. Because the release gate is literal, the otherwise working candidate `1065831` remains Business `broken` until those matches are zero.

**Role calls:** Product and Engineering remain called and separately carried. Business is called because this is the binding public-release and durable-value gate. Experience is called because the replacement appears in a builder-readable record and checker output. No specialist is needed.

**Business — carrier `/root/piece8_business` — active.** The independent 26/8/126 run, twelve-call adoption harness, control-red run, syntax checks, footprint, and always-read measurement all held. The planted-name scan matched ten of ten controls, but the candidate returned the accidental generic collisions above. A read-only substitution projected zero matches. Conclusion: no runtime change is needed, but the literal public-detail promise is not yet met. Binding ruling: **`broken` for `1065831`; the terminology repair alone is permitted.**

**Experience — carrier `/root/piece8_experience` — active.** Experience rejected “uneven parity” as imprecise and chose the clearer behavior-level language: “an unpaired backslash escapes a prospective opener,” “escaped and unescaped backtick cases,” fixture `backslash-escaped-opener`, and result “a backslash-escaped backtick cannot shield a real comment.” Its earliest informative return is the repaired record and checker wording, the paired escaped/unescaped subjects, and the public-detail scan. No installed-method or journey change is needed.

**Engineering — carrier `/root/piece8_engineering` — active.** Engineering found the same two homes and confirmed names are not consumed by parser behavior: `escapedBacktickOpener()` still counts preceding backslashes, one-backslash refusal and two-backslash success remain unchanged. Conclusion: rename the checker fixture title, slug, result label, and local identifiers only. No parser, installed home, version, footprint, or Piece 9 change is needed.

**Product synthesis and handoff:** Use the Experience wording and remove every accidental collision from the complete Piece 8 record/checker population, including local identifiers, so both the exact release scan and a plain substring attack return zero. Product has changed its two record phrases in this commit. After this commit, Engineering changes only the checker names and labels. The first run is the planted-name scan: positive ten of ten and candidate zero. Then the paired escaped/unescaped subjects and the entire 26/8/126 population must retain their previous behavior; control-red, syntax, diff, budgets, census, and cleanup remain floors. Product makes no implementation edit and hands this narrow repair to `/root/piece8_engineering`.

### Final informative returns after the public-detail repair

**Engineering — carrier `/root/piece8_engineering`:** Commit `0e170ce` changes only checker-local fixture names, identifiers, and output. Both exact and plain-substring planted controls matched ten of ten; candidate additions returned zero under both. The paired escaped and unescaped cases stayed green, the full population returned 26 routing, eight assessment, and 126 migration subjects with `PASS`, and control-red, syntax, compilation, diff, budgets, and census held. Resulting product change: terminology only. No Engineering concern remains before Built.

**Experience — carrier `/root/piece8_experience`:** The record now says an unpaired backslash escapes a prospective opener and names escaped and unescaped backtick cases. The checker uses `backslash-escaped-opener` and reports the behavior in plain language. The paired cases, complete population, and both positive-control-backed scans held; the precise remaining even-parity label is not a product-name collision. Resulting product change: none beyond the committed terminology. No Experience concern remains before Built.

**Business — carrier `/root/piece8_business`:** In a clean clone, the 26/8/126 population passed, the same-context control stayed red, source and fresh-install footprints stayed 17 files / 78,993 bytes and 19 files / 79,463 bytes, the five-skill limit held, and always-read remained 49,349/50,000 bytes. Both exact and plain scans kept their ten-of-ten positive controls while the public and complete Piece 8 candidate populations returned zero. The earlier governed and setup-cost failures remain failed and make no host-affordability claim. **Binding ruling: `kept` for Piece 8 candidate `0e170ce`, permitting a subtractive Built commit and fresh review only.** Piece 9, the milestone, tag, and release remain `not judged`.

**Product — carrier `/root`:** Product inspected the one-file implementation diff and reran the candidate from disk. The deterministic command returned 26 routing subjects, eight assessment subjects, 126 migration subjects, and `Piece 8 controls: PASS`. The same-context control returned one of one tasks red as required. Node syntax, cache-free Python compilation, `git diff --check ddb62f1..HEAD`, and the rc.2 version assertion returned zero. Root's exact and plain scans each returned ten planted matches and zero candidate matches. A disposable fresh Git repository installed 19 files / 79,463 bytes with source checkout `0e170ce`, method-surface digest `f440af1a1c9f87239c7b9191e1bfa54684f45509fea021e45c85dbff28a9f00e`, explicit assessment `null`, and no invented `product.md`; the temporary repository was then trashed. Source method remained 17 files / 78,993 bytes, five skills, and the four always-read files totaled 49,349 bytes.

**What changed or held:** The public-detail gate changed from red to green without changing parser behavior, installed bytes, version, dependencies, or the builder journey. All four active roles now permit the new Built boundary. Product must replace stale rejected-state prose rather than append to it, then remeasure the always-read set at or below 50,000 bytes. No governed host or Piece 9 run has occurred.

## Review receipt — round 4

**Built line and commit:** `state.md` at `e0b2093` says:

> **Built — Piece 8 “The right roles show up,” candidate `0e170ce`:** product commits `10acad3`, `8bf17be`, `78857f9`, `737959a`, `99a0f38`, `9273500`, `a20a537`, `cd1b45f`, `1065831`, and `0e170ce` cover exactly `AGENTS.md`, `.claude/skills/shape-product/SKILL.md`, `.claude/skills/shape-product/references/questions.md`, `.claude/skills/map-build/SKILL.md`, `.claude/skills/map-build/references/questions.md`, `templates/product.md`, `templates/map.md`, `templates/piece.md`, `templates/state.md`, `CONTRACT.md`, `README.md`, `capabilities.md`, `package.json`, `bin/speck-next.js`, and `devsuite/tasks/separated-product-team/check.py`. `PYTHONDONTWRITEBYTECODE=1 python3 devsuite/tasks/separated-product-team/check.py --piece-8-controls .` returned 26 routing, eight assessment, and 126 migration subjects with `PASS`; `./devsuite/run.sh --control separated-product-team` returned one of one tasks red as required; `node --check bin/speck-next.js`, cache-free Python compilation, `git diff --check ddb62f1..HEAD`, and the rc.2 version assertion returned zero. Positive-control-backed exact and substring scans returned ten planted and zero candidate matches. A disposable Git repository plus `node bin/speck-next.js install` returned 19 files / 79,463 bytes, five skills, explicit assessment `null`, and no `product.md`; the source-surface `find ... | wc -c` returned 17 files / 78,993 bytes. `wc -c AGENTS.md product.md map.md state.md` returned **45,623 bytes**, within 50,000. No product implementation commit follows `0e170ce`.

**Opened:** 2026-09-03, after `e0b2093` and before either tester was dispatched.

**Candidate:** exact product implementation ending at `0e170ce`; later commits `2d4ea86` and `e0b2093` change only role evidence and state. `git rev-list 0e170ce..e0b2093 -- <the 15 product files>` returned no commit.

**Fresh tester 1 — first-time builder, planned carrier `/root/piece8_round4_builder`:** receives the product promise, Built line, installed candidate, and every earlier judgment requirement, never product-role conclusions. They install fresh; route Product-plus-Engineering, Business, Experience, and all-four changes; check honest inactive roles, synthesis-before-code, named informative runs and returns, false-inactive repair, handled-miss continuity, exclusions, provenance, and plain next actions. They rerun the 26/8/126 population, both public-detail scans with positive controls, both HTML-comment failures, the balanced-inline and unmatched-opener pair, escaped/unescaped cases, and the mixed-line attack. They add one running skeptical attack.

**Fresh tester 2 — mid-product upgrader, planned carrier `/root/piece8_round4_upgrader`:** receives supported v5, generated rc.1, rejected and current rc.2, custom, missing-product, dirty, retry, corrupt, pending, and every completion route plus every earlier judgment requirement, never product-role conclusions. They independently exercise normal and flagged ambiguity recovery; full target, marker, and porcelain atomicity; canonical deletion; HTML comments; balanced, unmatched, escaped, multiline, and boundary inline code; same-surface/different-checkout provenance; bad arguments before target access; state-specific next actions; and no historical backfill. They add one running skeptical attack.

**Judge:** after both records are committed, planned fresh carrier `/root/piece8_judge_round4` challenges every tester claim and reruns the complete requirements from all three send-backs, both new skeptical attacks, the full deterministic population, the control arm, and the public-detail boundary. They rule works, delivers the promise, good to use, quality hangs together, and sound structure; every open item gets a destination.

**Excluded:** `/root`, `/root/piece8_business`, `/root/piece8_experience`, `/root/piece8_engineering`, `/root/piece8_migration_audit`, every Shape/Map/product-role or specialist carrier, every builder and auditor, and every tester or judge named in the first three receipts, including `/root/piece8_fresh_builder`, `/root/piece8_midproduct_upgrader`, `/root/piece8_judge_round1`, `/root/piece8_retest_builder`, `/root/piece8_retest_upgrader`, `/root/piece8_judge_round2`, `/root/piece8_round3_builder`, `/root/piece8_round3_upgrader`, and `/root/piece8_round3_judge`. The two round-4 testers and judge must be distinct from one another and this whole set.

**Run owner:** `/root`. No governed host/model run belongs to this review.

## Fresh testing — round 4

### First-time builder — carrier `/root/piece8_round4_builder`

**Candidate used:** exact product implementation `0e170ce` under receipt `1522b5f`, installed into disposable fresh Git repositories. The tester received the product promise, Built line, installed method, and round-4 receipt, not product-role conclusions.

Fresh install reported 19 files, explicit assessment `null`, source checkout `0e170ce`, and method digest `f440af1a1c9f87239c7b9191e1bfa54684f45509fea021e45c85dbff28a9f00e`. Four substantial-change records called Product plus Engineering only, added Business, added Experience, and called all four exactly when the product conditions fired. Product and Engineering stayed distinct; inactive roles contained only their condition and direct current evidence; synthesis preceded code; named runs and returns blocked landing; false inactivity repaired the current evidence; handled misses widened later involvement; and every contributor stayed excluded from fresh review.

The full command returned 26 routing, eight assessment, and 126 migration subjects with `PASS`; the same-context control returned one of one tasks red. Exact and substring public-detail scans each caught ten planted controls and zero candidate additions. Independent HTML-comment, balanced and unmatched inline opener, escaped and unescaped opener, and mixed-line fixtures all held with before-and-after hashes. The tester's free attack changed hidden history to CRLF line endings: ordinary upgrade refused with an identical product hash, and flagged recovery preserved all 534 original bytes as the exact prefix before appending one pending block.

**Verdict: SUFFICIENT.** No product finding needs a destination. An archive-only test attempt lacked Git provenance and was rerun from a disposable Git checkout; that was tester setup, not the installed product.

### Mid-product upgrader — carrier `/root/piece8_round4_upgrader`

**Candidate used:** exact product implementation `0e170ce` under receipt `1522b5f`, across independently constructed v5, generated rc.1, rejected/current rc.2, custom, missing, dirty, retry, corrupt, pending, and completed fixtures. The tester received the same cold review surface and no role conclusions.

The 26/8/126 population passed and the same-context control stayed red. Refusals preserved every non-Git byte, marker byte, unrelated dirty byte, and porcelain column. Ordinary fieldless ambiguity refused with the exact recovery; the flag opened only that undecided state; Shape, Map, and named-piece completion routes preserved historical work and printed their specific next actions. Different source checkouts with identical installed bytes shared method digest `f440af1a…`; bad arguments refused before target access. HTML-comment, inline-code, provenance, and CRLF recovery attacks otherwise held.

**Free skeptical attack and finding:** The exact generated rc.1 path removed its obsolete generated prose but left an empty `## Product team`. The tester completed a four-carrier assessment and selected the named-piece resume route without adding Product-specific Business or Experience call conditions, trusted evidence, expiry, or material-change examples. Upgrade exited zero, said `product.md` was unchanged, and instructed the builder to resume. The authoritative product still contained no conditions from which the next substantial piece could decide whether Business or Experience joins.

**Verdict: INSUFFICIENT.** Worst trust failure: a plain resume instruction overclaims readiness beside a visibly empty authoritative section. Proposed destination: Build. Require an upgrade completion to establish the Product-team responsibilities and observable call conditions in `product.md`, and make the running completion path reject or route incomplete product setup instead of resuming.

## Judgment — round 4

**Judge — carrier `/root/piece8_judge_round4`:** The judge received Built commit `e0b2093`, receipt `1522b5f`, tester records `50f9b59`, and exact product implementation `0e170ce`. `git log e0b2093..50f9b59 -- <the 15 product files>` returned empty; the Built and receipt commits changed only their named records; neither tester belonged to the excluded set. Both testers ran disposable product repositories.

The judge independently reproduced the upgrader's finding. An exact generated rc.1 product with an empty `## Product team` accepted a complete four-carrier assessment, said `product.md` was unchanged, and instructed `resume Piece alpha from state.md`. A complete selective control printed the same success. The judge's free attack widened the defect: a current rc.2 product containing only Product and Engineering bullets also resumed, while `rg -n '^- \*\*(Business|Experience)\*\*' product.md` returned no matches. The completion parser proves the assessment block and record exist, but never proves that the authoritative product gained the Business and Experience definitions later pieces must use.

The first-time builder's routing, parser-boundary, control, and public-scan evidence held, but its sufficient verdict did not cover post-assessment Product-team completeness. The upgrader's insufficient verdict and Build destination stand. The judge reran the 26/8/126 population and same-context control successfully; focused CRLF hidden-history refusals and recovery also held. This is a running false-ready path, not a documentation-only finding.

**Rulings:** Business — `broken` · works — insufficient · delivers the promise — insufficient · good to use — insufficient · quality hangs together — insufficient · structure — fighting.

**Route: Build.** A completed upgrade assessment may resume work only after `product.md` contains the Product-team responsibilities and Product-specific Business and Experience protections, call conditions, trusted evidence, expiry, and material-change examples required by the selective method. Otherwise the installed instructions and upgrader must refuse or route to Shape without claiming readiness. Preserve the empty and partial Product-team subjects as controls, rerun every earlier requirement plus a new skeptical attack, obtain all active-role returns, then write a new Built line and receipt before another review.

## Repair setup after judgment round 4

The in-app role pool refused another turn at its thread limit. Product therefore dispatched three read-only Codex CLI contexts with stripped configuration and recorded their host-issued session identities below. They replace the earlier role carriers for this contribution; they inherit the earlier evidence but never claim the earlier identities. Every original and replacement carrier remains excluded from testing and judgment.

### Four active contributions before code

**Product — carrier `/root` — active.** Product read the round-4 tester records and judgment, the exact running false-ready path, `product.md`, the product template, installed Finish-an-upgrade rule, contract, upgrader, and checker. Conclusion: the upgrade assessment currently produces a route but not the Product-team information that makes selective routing possible. This is a Build defect within Piece 8, not a wrong promise or piece cut. Assumptions: `product.md` remains authoritative; the upgrader validates but never invents product judgment; current custom prose stays intact. Proposed change: make Product-team completion an explicit output of the one-time assessment and a preflight condition for any Map or resume route.

**Business — replacement carrier `codex-cli:01a0647e-1b1e-7ab0-85f6-285e7219da72`, inheriting `/root/piece8_business` — active.** Business traced the empty and Product/Engineering-only successes through `parseAssessment()`, `planProductTeamAssessment()`, and the checker's completion helper. Without Product-specific Business and Experience conditions, later pieces either miss material concerns or pay universal-carrier cost. It compared strict canonical validation, semantic field validation, and routing every incomplete case to Shape. Recommendation: parse the template's exact field labels but treat substantive values as opaque; pending may remain incomplete; incomplete Map or resume refuses atomically; Shape remains allowed because it produces the missing product definition. Binding ruling: **`broken` for `710d152`; this narrow repair only is authorized.**

**Experience — replacement carrier `codex-cli:01a0647e-1b1e-77b2-8d99-32bf09b1bede`, inheriting `/root/piece8_experience` — active.** Experience found that a resume instruction is usable only when `product.md` itself can answer the next piece's role-call decisions. It requires one current Product-team section with non-placeholder Product and Engineering responsibilities and Business and Experience values for `Protects`, `Call when`, `May stay out when`, `Evidence expires`, and `Material changes`. Quoted, fenced, commented, inline-code-hidden, blank, bracket-placeholder, or trivial filler cannot satisfy it. On failure, name missing fields, say nothing changed, print no resume, and tell the builder to return the status to pending and finish `product.md` with the same four-role assessment. It rejects automatic rewriting and automatic Shape reopening.

**Engineering — replacement carrier `codex-cli:01a0647e-1b1e-75c0-a580-fd49c4c991aa`, inheriting `/root/piece8_engineering` — active.** Engineering confirmed the root cause: completion checks the canonical status and record file but never the authoritative Product-team section. Recommendation: add one read-only parser using the existing current-Markdown classification; require exactly one current Product-team section and exactly one canonical row per role; parse exact labels, keep values opaque, and reject missing, duplicate, blank, placeholder, or trivial filler. Validate after assessment-record existence and before migration planning or any write. Change `AGENTS.md`, `CONTRACT.md`, `README.md`, the installer, and checker; update `capabilities.md` only for measured evidence. Leave the already-canonical product template, every skill, version, Product record, and Piece 9 untouched.

The three role sessions ended with terminal success events and no error events. Their host-reported usage was: Business 601,516 gross / 87,596 fresh; Experience 559,687 gross / 91,335 fresh; Engineering 1,419,742 gross / 130,014 fresh; aggregate 2,580,945 gross / 308,945 fresh. The Node reduction over their three JSON event streams produced those values from `input_tokens + output_tokens` and `input_tokens - cached_input_tokens + output_tokens`. This is further failed cost evidence for Piece 9, not an affordability claim and not a moved limit.

### Product synthesis and Engineering handoff

The one-time assessment has two outputs: its record and a usable Product-team definition in `product.md`. Before Product marks the route complete, one current `## Product team` section must contain exactly one current row for Product, Business, Experience, and Engineering. Product and Engineering each need a non-placeholder responsibility. Business and Experience each need substantive values after the exact labels `Protects`, `Call when`, `May stay out when`, `Evidence expires`, and `Material changes`. Values are opaque product judgment; the validator only rejects absence, duplicates, blank text, untouched bracket placeholders, and a small explicit set of filler values. It reuses the same current-Markdown boundary, so inert text cannot satisfy the fields.

Product resolves the route disagreement in Business's favor. A completed **Map** or **resume** route requires the full Product-team definition before any target write. A completed **Shape reopened** route may proceed without it because it does not claim readiness: Shape must create or repair the definition before `product.md` can be ratified. Pending may remain incomplete. Missing definitions alone never invent a wrong-promise finding or reopen Shape.

For an incomplete Map or resume route, preflight lists the exact missing or unusable roles and fields, states that nothing changed, prints no affirmative Map or resume instruction, and tells Product to restore the status to pending, finish the Product-team section with the existing four-role evidence, commit the product/assessment/state records together, and rerun. The upgrader preserves all owner-authored bytes and never inserts role values.

Before implementation, Engineering preserves the exact empty-section resume and Product/Engineering-only resume as red controls. The repaired first trio must show both refuse with the full target, marker, unrelated dirt, and porcelain unchanged and no resume text, while a complete canonical product resumes the named piece. The widened matrix covers every required item missing, blank, bracket-placeholder, or filler; duplicate section, role, and field; impostors inside each inert Markdown container; current declarations after closed containers; incomplete Map refusal; incomplete Shape success; all three complete routes; custom prose preserved; and `--open-assessment` unable to override a completed-state refusal. Every prior 26 routing, eight assessment, and 126 migration subject remains individually present, followed by the same-context control, public-detail scans, syntax, diff, fresh install, retry, provenance, marker-last, budget, census, and cleanup gates. No governed model, Piece 9, new file, skill, template, dependency, owner beat, or version change belongs to this repair.

Product hands implementation to a new Engineering carrier after this synthesis is committed. That carrier inherits the replacement Engineering contribution above; all three Engineering carriers remain excluded from testing and judgment.

## Active-role returns after judgment round 4

**Engineering — implementation carrier `codex-cli:01a06485-5574-7413-8ac9-7ff6c50e767c`, inheriting `/root/piece8_engineering` and `codex-cli:01a0647e-1b1e-75c0-a580-fd49c4c991aa`:** Before editing, the empty Product-team and Product/Engineering-only completed resumes both exited zero and printed the resume instruction. Candidate `edab731` adds a read-only completeness check before any target write. It requires one current section, one row per role, usable Product and Engineering responsibilities, and every exact Business and Experience field; it leaves values opaque and exempts pending and Shape-reopened routes. The committed run passed 26 routing, eight assessment, and 210 migration/refusal subjects, including all prior 126, while the same-context control stayed red. Syntax, cache-free compilation, diff, rc.2, marker-last recovery, public-detail, and footprint gates passed. Engineering found no unresolved Piece 8 concern.

The implementation carrier ended with one successful host turn and no error event. Host-reported usage was 12,971,763 gross tokens, 12,718,208 cached, and 253,555 fresh. That is further failed cost evidence for Piece 9's host design, not an affordability claim or a moved Piece 8 acceptance boundary.

**Business — carrier `/root/piece8_business`:** A focused supported-v5 pair opened pending, rejected an empty completed resume with all 12 missing values and every target/porcelain byte unchanged, then accepted the usable four-role twin while preserving the owner's definitions and unrelated dirt and resuming `Piece alpha`. Following the refusal literally reached refusal → pending → resume with two Product commits/retries, zero owner questions, zero new carriers, and no installed file. Business independently reran the 26/8/210 population and same-context control. Source measured 17 files / 80,540 bytes; fresh install 19 files / 81,010 bytes; five skills; always-read before the Built rewrite 46,270 bytes; public-detail controls 10/10 with zero candidate matches. **Binding ruling: `kept` for Piece 8 candidate `edab731`, permitting a new Built line and review only.** Piece 9, the milestone, tag, and release remain not judged.

**Experience — carrier `/root/piece8_experience`:** On a real v5 journey, incomplete resume named all 12 missing items, said nothing changed, emitted no resume, and preserved full-target, product, marker, and porcelain bytes. Supplying the four authored rows from the existing assessment then produced the exact named-piece resume; Map produced its exact continuation; empty Product-team remained permissible only for Shape. No role prose was generated and owner values remained opaque. Experience accepts `edab731` for Built. A minor wording wrinkle remains: following “restore pending, commit, retry” literally creates one safe pending run before the route is marked complete again. It never resumes falsely or strands the builder; clearer one-run wording is future polish, not another repair cycle.

**Independent implementation audit — carrier `/root/piece8_migration_audit`:** The auditor reran the installed upgrader at `edab731`, not a detached model. The 26/8/210 population passed; completed Map/resume refused incomplete definitions before writes; Shape stayed exempt; the flag could not override completed-state refusal; inert Markdown impostors failed; live rows after closed containers passed; and custom product prose stayed byte-identical. Node syntax, diff, and added-line public-detail checks passed. Verdict: sufficient, with only the non-blocking runtime of the expanded deterministic matrix noted.

**Product — carrier `/root`:** Product inspected the exact six-file implementation commit and the auditor's independent running evidence. Root reran Node syntax, cache-free Python compilation, `git diff --check 7cb3f86..edab731`, the rc.2 assertion, and the same-context control, which returned one of one tasks red. A disposable committed install returned 19 files / 81,010 bytes, explicit assessment `null`, no invented `product.md`, source checkout `edab731`, and method-surface SHA-256 `1eb7042fe2b50b969b3cb6cbe1b865d096fd3d35b4356adb85f2f6358c899979`. Root's exact and substring scans each caught all ten planted names and zero candidate additions. The worktree was clean.

**What changed or held:** The round-4 false-ready path is closed at the authoritative product boundary. An assessment route alone can no longer masquerade as selective readiness: Map and resume require the definitions later pieces will actually use, while Shape remains able to produce them. Every earlier migration, parser, recovery, routing, exclusion, and preservation condition held. All four active roles now permit a new Built boundary. Business and Experience remain continuously involved through Piece 9 and the v6 milestone; live-host affordability remains explicitly unproven.

## Review receipt — round 5

**Built line and commit:** `state.md` at `b0abf71` says:

> **Built — Piece 8 “The right roles show up,” candidate `edab731`:** product commits `10acad3`, `8bf17be`, `78857f9`, `737959a`, `99a0f38`, `9273500`, `a20a537`, `cd1b45f`, `1065831`, `0e170ce`, and `edab731` cover exactly `AGENTS.md`, `.claude/skills/shape-product/SKILL.md`, `.claude/skills/shape-product/references/questions.md`, `.claude/skills/map-build/SKILL.md`, `.claude/skills/map-build/references/questions.md`, `templates/product.md`, `templates/map.md`, `templates/piece.md`, `templates/state.md`, `CONTRACT.md`, `README.md`, `capabilities.md`, `package.json`, `bin/speck-next.js`, and `devsuite/tasks/separated-product-team/check.py`. `PYTHONDONTWRITEBYTECODE=1 python3 devsuite/tasks/separated-product-team/check.py --piece-8-controls .` returned 26 routing, eight assessment, and 210 migration/refusal subjects with `PASS` on the implementation carrier, Business return, and independent audit. `./devsuite/run.sh --control separated-product-team` returned one of one tasks red as required. `node --check bin/speck-next.js`, cache-free Python compilation, `git diff --check 7cb3f86..edab731`, and the rc.2 version assertion returned zero. Exact and substring public-detail scans each found all ten planted controls and zero candidate additions. A disposable Git install returned 19 files / 81,010 bytes, explicit assessment `null`, no `product.md`, source checkout `edab731`, and method-surface SHA-256 `1eb7042fe2b50b969b3cb6cbe1b865d096fd3d35b4356adb85f2f6358c899979`; the source surface is 17 files / 80,540 bytes with five skills. `wc -c AGENTS.md product.md map.md state.md` returned **47,415 bytes**, within 50,000. No product implementation commit follows `edab731`; `7b4ea4b` changes only the role-return record.

**Opened:** 2026-09-03, after `b0abf71` and before either round-5 tester was dispatched.

**Candidate:** exact product implementation ending at `edab731`. Later commits `7b4ea4b`, `b0abf71`, and this receipt change only role evidence, state, and review records. No product implementation commit follows the quoted Built line.

**Fresh tester 1 — first-time builder:** a new carrier receives the product promise, installed candidate, Built line, this receipt, and the full running requirements from all four prior judgments, never the product-role conclusions. They install fresh; exercise Product-plus-Engineering, Business, Experience, and all-role routing; prove factual inactivity, synthesis-before-code, false-inactive current-piece repair, named informative runs and returns, continued staffing, exclusions, and public abstraction; run the 26/8/210 population and the same-context control; then independently attack whether a migrated Product-team definition that merely looks complete can still resume without giving the next piece usable selection conditions.

**Fresh tester 2 — mid-product upgrader:** another new carrier receives the supported v5, generated rc.1, rejected/current/fieldless rc.2, custom, missing, dirty, retry, corrupt, pending, and all completion histories plus every earlier judge requirement, never the product-role conclusions. They compare full target, product, marker, unrelated dirt, and porcelain bytes; exercise normal and flagged recovery, bad arguments, Markdown containers and inline-code boundaries, provenance, exact next actions, and all three routes; rerun the 26/8/210 population and control; then add one running skeptical attack against Product-team completeness or its recovery wording.

**Judge:** after both records are committed, a third new carrier receives candidate `edab731`, Built commit `b0abf71`, this receipt, and both tester records. They verify the receipt chain and reviewer independence; challenge every tester claim; rerun the round-4 empty and partial Product-team failures, every earlier judgment's complete requirements over its population, both new attacks, the 26/8/210 command, control, public-detail boundary, and footprint. They separately rule Business, works, delivers the promise, good to use, quality hangs together, and structure; every open item gets a destination.

**Excluded:** `/root`; every Product, Business, Experience, Engineering, specialist, builder, and auditor carrier, including `/root/piece8_business`, `/root/piece8_experience`, `/root/piece8_engineering`, `/root/piece8_migration_audit`, `codex-cli:01a0647e-1b1e-7ab0-85f6-285e7219da72`, `codex-cli:01a0647e-1b1e-77b2-8d99-32bf09b1bede`, `codex-cli:01a0647e-1b1e-75c0-a580-fd49c4c991aa`, and `codex-cli:01a06485-5574-7413-8ac9-7ff6c50e767c`; and every tester or judge in rounds 1–4. The two round-5 testers and judge are distinct from one another and the complete excluded set.

**Run owner:** `/root`. Testers and judge are read-only against the shared tree and use disposable Git repositories for every running subject. No governed host or Piece 9 run belongs to this review.

## Fresh testing — round 5

### First-time builder — carrier `codex-cli:01a064ad-6a6d-7260-a689-55ef5025c20c`

**Running product:** candidate `edab731` through fresh installed repositories. The tester received the product promise, Built line, committed receipt, and prior judgments' running requirements, never the product-role conclusions.

The committed support run passed 26 routing, eight assessment, and 210 migration/refusal subjects. The same-context control stayed red. Four fresh repositories exercised Product plus Engineering only, Business added, Experience added, and all four called. Factual inactive entries carried dated direct evidence, hashes, and expiry; every setup correctly stopped before synthesis or code when the tester had only one carrier. False-inactive repair, informative-run and return blocks, continued staffing, replacement lineage, contributor exclusion, provenance, all three upgrade routes, and public abstraction held. Fresh installation measured 19 files / 81,010 bytes; the source surface was 17 files / 80,540 bytes; always-read files measured 47,415 / 50,000 bytes. Exact and substring public-detail controls each caught ten planted names and zero candidate additions.

Their free attack used structurally complete but vague Product-team values. The upgrader accepted the values, as the recorded mechanical boundary requires, but the next piece treated the conditions as uncertain and the evidence as unsupported, called Business and Experience, and stopped before synthesis or code. **Verdict: SUFFICIENT.** No finding needs a destination.

### Mid-product upgrader — carrier `codex-cli:01a064ad-6a6d-77e3-b55d-45336bbfda68`

**Running product:** candidate `edab731` against 33 disposable repositories and five source checkouts covering v5, generated rc.1, rejected/current/fieldless rc.2, custom, missing, dirty, retry, corrupt, pending, Shape, Map, resume, recovery, provenance, Markdown containers, inline code, and argument failures. The tester received the same cold review surface and no role conclusions.

The committed support run passed 26/8/210 and the same-context control stayed red. Every named existing path otherwise behaved correctly: refusals preserved the complete target, product, marker, unrelated dirt, and porcelain bytes; distinct checkouts with byte-identical installed surfaces shared method hash `1eb7042f…c899979` while retaining their own source labels.

Two running findings remain. First, a CRLF-only `product.md` containing the exact canonical pending block was treated as if no assessment evidence survived. Ordinary upgrade refused without changing the target, but following its printed `--open-assessment` action appended a second pending block and mixed 14 CRLF lines with five bare-LF lines. Second, a completed resume whose Product responsibility contained only U+200B was accepted and printed the named resume action, while a spaces-only twin correctly failed as unusable. The first finding can duplicate lifecycle state; the second lets an invisible blank satisfy a required role definition. **Verdict: INSUFFICIENT. Route: Build.** Preserve both failures as controls, repair the upgrader and Piece 8 suite, then require a new Built line and receipt.

The two testers were distinct from every excluded contributor and from one another. Both left the shared repository unchanged and removed their disposable repositories. Their host-reported use was 4,479,613 gross / 4,349,056 cached / 130,557 fresh tokens for the first-time builder and 2,955,140 gross / 2,806,016 cached / 149,124 fresh for the upgrader. These are review-cost evidence for Piece 9, not Piece 8 acceptance evidence.

## Judgment — round 5

**Judge — carrier `/root/piece8_judge_round5b`:** The judge received candidate `edab731`, Built commit `b0abf71`, receipt `fefa003`, and both tester records committed at `26641d4`. The chain was valid: neither the receipt nor tester-record commit changed a product file, both tester carriers were distinct and eligible, and no product implementation followed `edab731`. The judge reran the 26 routing, eight assessment, and 210 migration/refusal subjects with `PASS`; the same-context control again went red.

The judge did not produce counter-evidence against the mid-product upgrader's two running failures. The passing population does not contain either CRLF-only canonical assessment state or an invisible-only required Product-team value, so its green cannot clear those findings. The first-time builder's sufficient verdict stands for the routing path it exercised; it does not establish the whole upgrade boundary. The upgrader's insufficient verdict therefore governs the piece.

**Rulings:** Business — `not judged` · works — insufficient · delivers the promise — insufficient · good to use — insufficient · quality hangs together — insufficient · structure — clean review chain, incomplete product boundary.

**Route: Build.** Preserve the CRLF-only pending assessment and U+200B-only Product responsibility as pre-fix controls. Repair logical-line recognition without rewriting owner bytes, keep appended assessment text in the file's existing line-ending style, and treat values made only of whitespace or invisible formatting code points as blank without judging substantive owner text. Add one neighboring line-ending and one neighboring default-ignorable attack; retain every existing 26/8/210 subject and all prior judgment requirements. Then all active roles return, Product writes a new Built line, a new receipt names fresh testers, and a new judge rules the whole piece.

## Repair setup after judgment round 5

### Four active contributions before code

**Product — carrier `/root` — active.** Product read the ratified selective promise, current product/map/state, candidate code and checker, both round-five tester records, and the judge's send-back. The defect is inside the existing Piece 8 upgrade boundary: different physical newline bytes can hide the same canonical state, and an invisible-only value can impersonate an authored responsibility. Neither changes the promise or piece order. Assumptions: a product file is valid UTF-8; LF, CRLF, and lone CR are equivalent logical line boundaries; owner-authored bytes stay untouched; structural visibility can be checked without judging meaning. Proposed change: one lossless logical-line representation for all current-Markdown classification and a temporary visibility-normalized copy for the existing blank/placeholder/filler test. Earliest informative Product run: the exact failed subjects plus clean visible twins produce the right route without duplicate state or owner-byte change.

**Business — replacement carrier `/root/piece8_round5_business`, inheriting the prior Piece 8 Business evidence — active.** The CRLF path turns a supported repository into false ambiguity, adds repair work, duplicates lifecycle state, and weakens adoption trust. The invisible-only path resumes without a real Product responsibility. Business assumes line-ending differences do not change product meaning and formatting-only content is blank, while any visible-content value remains opaque owner judgment. It proposes lossless line parsing, delimiter-preserving append, default-ignorable-aware presence checks, atomic snapshots, and no owner question or manual repair. Earliest Business run: canonical CRLF pending, eligible CRLF recovery, U+200B-only and U+2060-only refusals, and a visible-content twin all behave correctly before the full retained population. **Binding ruling: `broken` for `edab731`; this repair and its controls only may proceed.** A later `kept` return is required before Built.

**Experience — replacement carrier `codex-cli:01a064d3-d98e-7592-bff2-b54184823f15`, inheriting the prior Piece 8 Experience evidence — active.** Builders currently cannot trust what the upgrade sees: following its own recovery can duplicate an already-visible assessment, while a visibly empty role definition can appear complete. Experience assumes whitespace and invisible formatting characters alone are blank, but any value with visible content remains accepted exactly as authored. It proposes recognizing logical structure across newline styles without normalizing the file; using the existing delimiter for a legitimate append; and checking visible presence on a temporary copy only. Earliest Experience run: CRLF pending stays single, CRLF recovery preserves its whole prefix and style, U+200B-only and U+2060-only values refuse, and visible text containing U+200B remains byte-identical and accepted. A no-final-newline recovery is its neighboring journey. Plain installed wording must say both what counts as the same line and what counts as structurally blank.

**Engineering — replacement carrier `codex-cli:01a064d3-d98e-7481-b351-a451fe941567`, inheriting the prior Piece 8 Engineering evidence — active.** `activeMarkdownLines()` splits only on `\n`, so CR remains attached to CRLF lines; `removeGeneratedLine()` reconstructs with LF; `appendAssessment()` always adds LF; and `unusableProductTeamValue()` uses `trim()`, which leaves U+200B. Engineering proposes records of `{text, ending}` for `\r\n`, `\n`, and `\r`; the existing classifier consumes only `text`; generated-line removal blanks only the matched text while retaining its delimiter and every unrelated byte; append uses the last existing delimiter, or LF when none exists; and validation removes Unicode `Default_Ignorable_Code_Point` characters only from a temporary classification copy before the existing blank, placeholder, and filler checks. Earliest Engineering run is the repaired byte-exact subjects followed by the full 26/8/210 floor. Engineering originally proposed changing only installer and checker because the present promise already says blank and byte-preserved; the other roles ask for explicit installed wording.

The two external role contexts ended with terminal success events and no error events. Experience used 285,206 gross / 65,942 fresh tokens; Engineering used 929,598 gross / 127,678 fresh. This is further Piece 9 cost evidence, not a Piece 8 affordability claim.

### Product synthesis and Engineering handoff

Product keeps the product boundary and accepts Engineering's lossless representation. A logical line may end in CRLF, LF, lone CR, or end-of-file; all feed the same existing Markdown classification without their delimiter. The original `{text, ending}` records remain the write source. If an exact generated line is removed, only its text becomes empty and its delimiter remains. If a pending block is legitimately appended, every prior byte remains an exact prefix and the new block uses the last delimiter found in the file; a file with no delimiter uses LF. Existing canonical state is never appended again.

For required Product-team values, the upgrader makes a classification copy with Unicode default-ignorable code points removed, then applies the existing whitespace, bracket-placeholder, and explicit-filler tests. It never writes that copy. A value made only of whitespace or invisible formatting is blank; `T` + U+200B + `BD` remains the existing explicit filler after classification; any value with visible non-filler content is accepted and preserved exactly, including visible text containing invisible formatting. This is structural completeness, not semantic scoring.

Product resolves the wording disagreement in Business and Experience's favor. `AGENTS.md`, `CONTRACT.md`, and `README.md` already state the parser and blank-value contract, so they receive one compact clarification: LF, CRLF, and lone CR count as the same logical lines while owner bytes stay unchanged; appends use the file's nearest existing line ending; whitespace/default-ignorable-only values are blank, while visible owner text stays opaque. `capabilities.md` changes only after measured proof. No template, skill, package version, installed file, dependency, Product record, or Piece 9 file changes.

Before implementation, Engineering adds and runs the two exact round-five failures against `edab731`; both must be red. The repaired matrix then proves: CRLF canonical pending is recognized without a write or duplicate; fieldless CRLF with and without a final newline recovers to exactly one all-CRLF block while preserving the full original prefix; a lone-CR pending and recovery follow the same rule; generated-line removal preserves every original delimiter; U+200B-only Product, U+2060-only Engineering, and U+FE0F-only Business field values refuse atomically; invisible-split `TBD` remains filler; and visible text containing U+200B is accepted byte-identically. Each refusal compares complete target, product, marker, unrelated dirt, and porcelain bytes and emits no resume. Every prior 26 routing, eight assessment, and 210 migration/refusal subject remains individually present, followed by the same-context control, syntax, cache-free compilation, full diff check, fresh install, idempotence, provenance, footprint, changed-home census, and positive-control-backed public-detail scans.

The pre-edit census used the case-insensitive families `line ending|crlf|current lines`, `blank value|invisible|default-ignorable`, and `owner bytes|normalize|Product-team usable` across the method page, all skills and references, all templates, contract, README, capabilities, installer, checker, and Product records. Binding implementation homes are `AGENTS.md`, `CONTRACT.md`, `README.md`, `bin/speck-next.js`, and `devsuite/tasks/separated-product-team/check.py`; `capabilities.md` is the measured-result home. Returned but unchanged are `CLAUDE.md`, every skill/reference, every template, `package.json`, `product.md`, `map.md`, `state.md`, and earlier work evidence. Engineering owns those six implementation/result homes after this synthesis commit. Product records and every Piece 9 runner, broker, proof, prompt, setup, and evidence file remain untouched. The new Engineering implementation carrier inherits the contribution above and is distinct from Product; all original and replacement role carriers remain excluded from later testing and judgment.

## Round-five repair returns

**Engineering — implementation carrier `codex-cli:01a064d9-761a-70e2-b13d-27c2b0987b0b`, inheriting the read-only Engineering contribution above:** Before editing, the exact CRLF pending and U+200B-only Product subjects were the only new red cases while every prior 26 routing, eight assessment, and 210 migration subject stayed green. Candidate `f3cf2d4` represents logical lines as preserved text and delimiter records; generated-line removal blanks only the matched text; a legitimate append uses the last existing delimiter; and default-ignorable characters are removed only from a temporary structural-classification copy. The committed population passed 26 routing, eight assessment, and 221 migration/refusal subjects. The same-context control stayed red; syntax, compilation, both full diff checks, version, footprint, census, and public-detail gates passed. Engineering found no unresolved Piece 8 concern before Built.

**Independent implementation audit — carrier `/root/piece8_r5_impl_audit`:** The auditor inspected the committed six-file behavior and reran the 26/8/221 population. Three additional disposable products held: a lone-CR generated-status repair preserved its prefix and emitted no LF; visible text containing U+200B resumed unchanged; and U+200B wrapped around a bracket placeholder refused atomically. Verdict: no material implementation finding. This auditor remains excluded from fresh review.

**Experience — incomplete replacement carrier `codex-cli:01a064ef-261a-7fa3-ac40-8e6825f11a2d`:** The first replacement inspected the exact candidate and passed syntax and static checks, but its read-only filesystem could not create a disposable product. It correctly left the Experience return unresolved and requested a writable replacement rather than turning paper evidence into a product verdict. It remains excluded.

**Experience — running replacement carrier `codex-cli:01a064f2-195d-7940-8422-a10e594843ab`, inheriting both earlier Experience records:** Eleven focused disposable products exercised the real upgrader. Canonical CRLF and lone-CR pending blocks stayed single and byte-identical. Fieldless CRLF with and without a final newline and fieldless lone-CR each refused unchanged, then followed the printed flag into exactly one same-delimiter pending block with the whole original product as its prefix and no resume. U+200B-only Product, U+2060-only Engineering, U+FE0F-only Business, and invisible-split `TBD` refused with the exact field and no resume; visible owner text containing U+200B resumed byte-identically. The free skeptical product contained one line and no newline byte: recovery preserved that exact prefix and added one LF-only pending block. Every subject retained tracked and binary untracked dirt. **Experience conclusion: sufficient; no concern blocks Built.**

**Business — carrier `/root/piece8_round5_business`:** Business reran the focused line-ending and value matrix plus the complete 26/8/221 population and same-context control. Every refusal preserved the target, marker, dirt, and porcelain; recovery needed no owner question or manual byte repair. Source measured 17 files / 81,132 bytes; fresh install 19 files / 81,602 bytes; five skills; always-read 46,607/50,000 bytes. The install reported source `f3cf2d4`, method digest `eaec805baa7893b9dee885009df6c7780949d2c74a4f2c4257a7200b7906fe37`, explicit assessment `null`, and no invented `product.md`. Exact and substring public-detail controls each caught ten of ten planted names and zero candidate additions. **Binding ruling: `kept` for Piece 8 candidate `f3cf2d4`, permitting a new Built line and fresh review only.** Piece 9, the milestone, tag, release, host affordability, and field value remain `not judged`.

**Product — carrier `/root`:** Product inspected the exact six-file commit and the separate audit, then reran the committed deterministic population: 26 routing, eight assessment, 221 migration/refusal, `PASS`. The fake same-context team returned one of one tasks red. Node syntax, cache-free Python compilation, `git diff --check aa12f8f..f3cf2d4`, `git diff --check ddb62f1..f3cf2d4`, and the rc.2 assertion returned zero. A fresh disposable install and independent measurements matched Business's 17/81,132 source, 19/81,602 fresh, five-skill, 46,607 always-read, explicit-null, and method-digest evidence. Product's exact and substring added-line scans each caught all ten planted controls and zero candidate matches. Product concludes the two round-five failures are closed without changing the selective promise, version, dependencies, Product records, templates, skills, or Piece 9 machinery.

The three external implementation/Experience turns ended successfully with no error event. Their host-reported use was: Engineering 11,970,967 gross / 253,847 fresh; incomplete Experience 1,020,457 gross / 101,801 fresh; running Experience 1,121,568 gross / 98,720 fresh. These are additional Piece 9 cost evidence, not a moved Piece 8 limit or an affordability claim.

**What changed or held:** Physical line-ending bytes no longer change assessment meaning, invisible formatting cannot impersonate an authored role definition, and visible owner judgment remains untouched. All earlier routing, migration, preservation, recovery, provenance, exclusion, and public-abstraction evidence held. Product, Business, Experience, and Engineering now permit a new Built line for `f3cf2d4`; every original, incomplete, replacement, implementation, and audit carrier remains excluded from fresh testing and judgment.

## Review receipt — round 6

**Built line and commit:** `state.md` at `58bfa2f` says:

> **Built — Piece 8 “The right roles show up,” candidate `f3cf2d4`:** product commits `10acad3`, `8bf17be`, `78857f9`, `737959a`, `99a0f38`, `9273500`, `a20a537`, `cd1b45f`, `1065831`, `0e170ce`, `edab731`, and `f3cf2d4` cover exactly `AGENTS.md`, `.claude/skills/shape-product/SKILL.md`, `.claude/skills/shape-product/references/questions.md`, `.claude/skills/map-build/SKILL.md`, `.claude/skills/map-build/references/questions.md`, `templates/product.md`, `templates/map.md`, `templates/piece.md`, `templates/state.md`, `CONTRACT.md`, `README.md`, `capabilities.md`, `package.json`, `bin/speck-next.js`, and `devsuite/tasks/separated-product-team/check.py`. The committed role returns in `work/right-roles-show-up.md` report 26 routing, eight assessment, and 221 migration/refusal subjects with `PASS`; the same-context control returned one of one tasks red; syntax, compilation, both full diff checks, rc.2, footprint, provenance, census, and positive-control-backed public-detail checks passed. Eleven separate Experience journeys and three independent implementation attacks also passed. Fresh install measured 19 files / 81,602 bytes; source method 17 files / 81,132 bytes; five skills; method SHA-256 `eaec805baa7893b9dee885009df6c7780949d2c74a4f2c4257a7200b7906fe37`. `wc -c AGENTS.md product.md map.md state.md` returned **47,399 bytes**, within 50,000. No product implementation commit follows `f3cf2d4`; `bedacea` changes only the role-return record.

**Opened:** 2026-09-03, after Built commit `58bfa2f` and before either tester received the repository, product promise, candidate, or review task. Three host contexts were reserved by identity only; their first turns said only `READY` and their identifiers.

**Candidate:** exact product implementation ending at `f3cf2d4`; later commits `bedacea` and `58bfa2f` change only the role-return record and state. `git rev-list f3cf2d4..58bfa2f -- <the 15 product files>` returns empty.

**Fresh tester 1 — first-time builder, reserved carrier `codex-cli:01a064fd-494a-75f2-a6b5-5bbd1c298e6f`:** receives the ratified selective promise, Built line, installed candidate, and every earlier judgment's complete running requirements, never role conclusions. They install fresh and use four substantial-piece subjects: Product plus Engineering only, Business added, Experience added, and all four. They challenge factual inactivity, role-authored conditions, uncertainty and expiry, Product/Engineering separation, synthesis before code, informative runs and returns, false-inactive current-piece repair, handled-miss continuity, replacement lineage, contributor exclusions, one owner recommendation, footprint, provenance, and public abstraction. They rerun the complete 26/8/221 population and same-context control, then add one running skeptical attack.

**Fresh tester 2 — worst-day mid-product upgrader, reserved carrier `codex-cli:01a064fd-494a-7952-9ebc-c01a63ce476c`:** receives supported v5, generated rc.1, rejected/current/fieldless rc.2, custom, missing-product, dirty, retry, corrupt, pending, Shape, Map, resume, recovery, provenance, inert-Markdown, argument, and Product-team completeness requirements from every prior judgment, never role conclusions. They independently replay both round-five failures; CRLF with and without a final newline; lone CR; mixed generated delimiters; invisible-only and invisible-split values; visible text containing an invisible formatter; all three completion routes; complete-target/marker/dirt/porcelain atomicity; idempotence; exact reporting; and fresh install. They rerun 26/8/221 and the control, follow every printed repair as a builder, and add one unrelated running skeptical attack.

**Judge — reserved carrier `codex-cli:01a064fd-494b-7d01-bc04-ea79305ac30e`:** receives candidate `f3cf2d4`, Built commit `58bfa2f`, this committed receipt, and both tester records only after those records are committed. The judge verifies the chain and reviewer independence, challenges each tester against the running product, reruns every earlier judgment's complete requirements over its whole population plus both new attacks, and checks the 26/8/221 command, same-context control, public-detail boundary, footprint, and exact product-file set. They rule Business, works, delivers the promise, good to use, quality hangs together, and structure; every open item gets a destination.

**Excluded:** the complete union of every Product, Business, Experience, Engineering, specialist, builder, auditor, tester, and judge carrier named in rounds 1–5 or the role sections above, including `/root`, `/root/piece8_business`, `/root/piece8_round5_business`, `/root/piece8_experience`, `/root/piece8_engineering`, `/root/piece8_migration_audit`, `/root/piece8_r5_impl_audit`, every prior in-app tester and judge, `codex-cli:01a064d3-d98e-7592-bff2-b54184823f15`, `codex-cli:01a064d3-d98e-7481-b351-a451fe941567`, `codex-cli:01a064d9-761a-70e2-b13d-27c2b0987b0b`, `codex-cli:01a064ef-261a-7fa3-ac40-8e6825f11a2d`, `codex-cli:01a064f2-195d-7940-8422-a10e594843ab`, both round-five tester carriers, and `/root/piece8_judge_round5b`. The two reserved testers and judge are distinct from one another and the whole excluded set.

**Run owner:** `/root`. Reviewers may write only disposable repositories outside the shared tree. No governed host or Piece 9 run belongs to this review.

## Fresh testing — round 6

### First-time builder — carrier `codex-cli:01a064fd-494a-75f2-a6b5-5bbd1c298e6f`

**Running product:** exact candidate `f3cf2d4` under Built commit `58bfa2f` and receipt `74d4aaf`. The tester received the selective product promise, the receipt-bound product, and the earlier judgments' running requirements, never the product-role conclusions. The receipt parent was the Built commit, neither record commit changed a product file, and no product implementation followed the candidate.

The first host attempt could not create a disposable `/tmp` directory and therefore produced no product verdict. The same reserved carrier was resumed only after it proved create-and-delete access in `/tmp`; that setup failure remains review evidence rather than being represented as a product failure.

The writable run fresh-installed rc.2 and constructed four substantial-piece records: Product plus Engineering only, Business added, Experience added, and all four. Inactive entries cited the installed product's own conditions and current direct evidence; active roles had distinct carriers, one Product synthesis, and named informative returns. `PYTHONDONTWRITEBYTECODE=1 python3 devsuite/tasks/separated-product-team/check.py --piece-8-controls .` returned 26 routing, eight assessment, and 221 migration/refusal subjects with `PASS`. The same-context control returned one of one tasks red. Fresh install reported 19 files, five skills, no invented `product.md`, source `f3cf2d4`, and method digest `eaec805baa7893b9dee885009df6c7780949d2c74a4f2c4257a7200b7906fe37`. Source remained 17 method files; the candidate always-read set was 46,607 bytes and the Built tree was 47,399 bytes. Syntax, parse, diff, public-detail controls, and the no-later-product-commit check passed.

**Free skeptical attack:** installing into a nonempty Git repository with an existing `AGENTS.md` refused and preserved an owner sentinel. **Verdict: SUFFICIENT.** No product finding needs a destination. The resumed turn used 6,349,171 gross / 6,163,712 cached / 185,459 fresh host-reported tokens. Every disposable path was deleted after the system trash rejected the exact `/tmp` directory.

### Worst-day mid-product upgrader — carrier `codex-cli:01a064fd-494a-7952-9ebc-c01a63ce476c`

**Running product:** exact candidate `f3cf2d4` under the same Built line and receipt, across independently constructed fresh, v5, generated rc.1, rejected/current/fieldless rc.2, custom, missing-product, dirty, retry, corrupt, pending, completion, recovery, provenance, inert-Markdown, argument, and Product-team-completeness subjects. This tester was eligible and distinct from the first tester and reserved judge. An initial read-only host attempt was stopped without a verdict; the resumed carrier first proved a writable disposable directory.

The committed command returned 26 routing, eight assessment, and 221 migration/refusal subjects with `PASS`; the same-context fixture stayed red. An independent 71-check replay passed exact prefix and delimiter preservation, complete-tree/marker/product/dirt/porcelain refusal atomicity, executable repairs, all three routes, invisible-only and split-filler rejection, visible U+200B acceptance, provenance separation, budgets, and positive-control-backed public-detail scans. Source measured 17 files / 81,132 bytes; fresh install 19 / 81,602; five skills; always-read 46,607 / 50,000 bytes; method digest `eaec805baa7893b9dee885009df6c7780949d2c74a4f2c4257a7200b7906fe37`.

**Free skeptical attack and finding:** the tester replaced the target repository's `.claude/skills` directory with a symlink to a sibling directory outside the repository, then ran the real upgrader. It exited zero, copied nine method files through the symlink into that outside directory, grew it from one sentinel to ten files / 44,108 bytes, left the symlink in place, and printed a successful upgrade and resume instruction. Standard error simultaneously said `Could not access '.claude/skills/null'`. The target report showed its tracked skills deleted and the symlink untracked, while its claimed complete diff could not show the nine out-of-target writes. The outside sentinel itself remained byte-identical; outside-tree SHA-256 after the run was `fe66d3537aa4962370c4a63a598e5f47b45812a560c3f5c8577fe37b1df0e99d`.

**Verdict: INSUFFICIENT. Route: Build.** Upgrade must not follow an installed-surface symlink beyond the selected product or call the operation successful after its complete-diff command fails. Preserve the escape as a pre-fix control; require atomic refusal or a proven in-target replacement, a clean retry, the complete accumulated population, and one new skeptical attack before another Built line and receipt. The resumed turn used 5,805,218 gross / 5,612,544 cached / 192,674 fresh host-reported tokens. Both the logical and physical disposable roots were deleted; the shared repository remained at receipt `74d4aaf` with empty porcelain.

The split verdict now goes to the distinct reserved judge. No governed host or Piece 9 run occurred.

## Judgment — round 6

**Judge — carrier `codex-cli:01a064fd-494b-7d01-bc04-ea79305ac30e`:** The reserved judge received candidate `f3cf2d4`, Built commit `58bfa2f`, receipt `74d4aaf`, and the tester record committed at `195877a`. The chain was valid: the twelve named product commits changed exactly the fifteen receipt-listed files, every one preceded the candidate, and no later commit touched that set. The judge was distinct from both testers and absent from the complete excluded union.

The judge reran the full 26 routing, eight assessment, and 221 migration/refusal subjects with `PASS`; the same-context control returned exactly one of one tasks red. The first-time builder's nonempty-`AGENTS.md` attack also held: install exited one, target hashes and porcelain stayed identical, and the output said nothing was touched. That tester's direct evidence stands, but its sufficient verdict does not cover installed-directory path types.

The judge reproduced the worst-day finding. With `.claude/skills` linked to a sibling directory inside the disposable root, ordinary upgrade exited zero. The selected target remained nine tracked deletions plus the link, while the sibling grew from one 280-byte sentinel to ten files / 44,360 bytes. All nine skill files were written there. The command printed successful upgrade and resumption while standard error said `Could not access '.claude/skills/null'`.

The independent skeptical attack linked `templates` instead. It reproduced the same class: upgrade exited zero; target, marker, missing `product.md`, and six-deletions-plus-link status stayed unchanged; the sibling grew from one file / 280 bytes to seven files / 12,273 bytes; and stdout again claimed success and a complete diff while stderr said `Could not access 'templates/null'`. Reverting the selected repository cannot remove either sibling write.

Closing checks otherwise held: Node syntax, cache-free Python compilation, both diff checks, rc.2, 17 source files / 81,132 bytes, 19 installed files / 81,602 bytes, five skills, 46,607 candidate and 47,399 Built always-read bytes, matching method digest `eaec805b…fe37`, explicit assessment `null`, no invented `product.md`, and public-name positive controls ten of ten with zero exact or substring candidate matches. The completed judging turn used 3,492,707 gross / 3,345,664 cached / 147,043 fresh host-reported tokens.

**Rulings:** Business — `broken` for Piece 8; Piece 9 and the milestone remain `not judged`. Works — insufficient. Delivers the promise — insufficient. Good to use — insufficient. Quality hangs together — insufficient. Structure — insufficient: destination copying and digest verification follow unchecked target links, reporting suppresses Git command failure, and the process still prints success.

**Route: Build.** Piece 8 stays live. Refuse before any write when an installed-surface destination or ancestor can redirect a write outside the selected target, or prove an atomic in-target replacement. On refusal, target, sibling, marker, product, unrelated dirt, and porcelain remain byte-identical. Copy, verification, and reporting failures propagate; none may print success, complete-diff, commit, or resume guidance. Preserve both link failures as controls, prove a clean retry, retain the complete 26/8/221 floor and every earlier requirement, then add one skeptical path-boundary attack. Product, Business, Experience, and Engineering return to the repaired run before a new Built line, receipt, fresh tests, and judgment.

The judge deleted the exact disposable root after system Trash refused it. Shared HEAD remained `195877a` with empty porcelain. No governed host or Piece 9 run occurred.

## Repair setup after judgment round 6

### Four active contributions before code

**Product — carrier `/root` — active.** Product read the owner's ratified selective-separation decision, `product.md`, `map.md`, `state.md`, Promise 5, the round-six tester and judge runs, the candidate installer and checker, and every role's new direct evidence. The defect stays inside Piece 8: the command follows a link on a method destination beyond the selected product, suppresses the resulting Git-report failure, and then tells the builder to resume. It does not change the selective promise or piece order. Product assumes the physical `realpath` of the command target is the product boundary; the kernel owns its exact installed destinations but owns neither an outside referent nor unrelated bytes reachable through it; and a link can be localized only through a transaction that preserves both sides until every copy, digest, marker, status, and diff operation has succeeded. Proposed change: turn linked method destinations into real local entries without writing through them, preserve all outside bytes, retain rollback material until reporting succeeds, and emit one truthful disclosure only after completion. Earliest Product run: the two exact linked-directory failures change from out-of-product success to in-target success, every outside and unrelated hash stays fixed, a planted late report failure restores the whole target, and the retry is byte-stable.

**Business — replacement carrier `/root/piece8_r6_business`, inheriting the complete earlier Business record — active.** Business reproduced `.claude/skills` linked to a sibling: upgrade exited zero, expanded the sibling from one sentinel to ten files / 44,080 bytes, left nine deletions plus the link in the selected product, printed success and resume, and wrote a Git-access error. That destroys adoption trust and can cost more owner repair than selective separation saves. Business assumes kernel-owned installed paths may be normalized while owner and referent bytes are inviolable. It proposes a staged, transactional in-target replacement across the whole planned write path, restoration on every later failure, checked reporting, explicit topology disclosure, and generated controls over path positions and link forms. A refusal is an acceptable safety fallback during implementation but not the finished product: the ratified promise does not make builders repair supported repositories by hand. Earliest Business run is the complete generated link-boundary population plus injected copy, digest, marker, status, tracked-diff, and untracked-diff failures, followed by fresh, v5, rc.1, and current retries. **Binding ruling: `broken` for `dd06bfb`; only this repair and its controls may proceed.**

**Experience — replacement carrier `/root/piece8_round4_upgrader`, inheriting prior Piece 8 Experience and round-four test evidence — active.** Experience independently lived fresh install, linked `.claude/skills`, linked `templates`, and manually restored clean retries. Both linked upgrades changed sibling trees, emitted `/null` errors, claimed success, and gave continuation instructions; both retries became calm and correct only after the links were removed. A top-level `AGENTS.md` leaf link already gave the intended journey: its outside sentinel stayed unchanged while the selected product received the canonical local file. Experience proposes applying that same model to linked subtrees, naming the replaced path and saying the linked destination was not changed, withholding every success/diff/commit/resume line on failure, and falling back to an untouched refusal only if safe replacement cannot be proven. Earliest Experience run is both escaping subtrees, one late reporting failure, and the clean retry, observed through target, referent, marker, product, dirt, porcelain, exit status, and stdout/stderr. Experience stays active because the builder's trust and next action change.

**Engineering — replacement carrier `/root/bounded_transport_review`, inheriting the earlier Piece 8 Engineering record — active.** Engineering reproduced the skills escape, a `templates` regular-file collision, a dangling skills link, and a command-target link that correctly resolved to the real repository. Source inspection found direct live `cpSync`, destination-following `statSync`, marker-last without surface rollback, and `gitRead()` swallowing every Git failure. Engineering concludes path containment and truthful failure are both broken. Its preferred minimum is read-only `lstat`/`realpath` preflight plus untouched refusal for every redirect or wrong kind, then same-filesystem staging, backup-and-rename application, immediate path revalidation, marker last, rollback, and checked Git reporting. It dissents from automatic link replacement because a link may encode owner structure. Earliest Engineering run is the skills-sibling failure refusing atomically, then a clean retry after restoration, widened through the templates twin, dangling and wrong-kind cases, target-root link, an interior-ancestor attack, every injected late failure, and the retained 26 routing / eight assessment / 221 migration floor.

The three replacement roles are distinct from Product and one another. `/root/piece8_round4_upgrader` previously tested an older candidate and is now a role contributor; it and every original or replacement carrier remain excluded from future testing and judgment. No specialist is active.

### Product synthesis and Engineering handoff

Product keeps Promise 5 and chooses automatic, transactional localization for the round-six link class. A builder should not have to repair a method-owned destination by hand, and an untouched refusal alone would turn a safety fix into an adoption regression. Engineering's dissent is preserved and sets the safety bar: the command may localize only a mapping it can stage and reverse completely. A validated link may be traversed once for an explicit read-only copy of its logical non-method contents into private staging; no write, installed-method digest, final verification, or Git report may traverse the link or touch its referent.

The selected product is the resolved command-target directory. Before the first target write, the installer inventories the complete planned write set — the copied surface, stale method paths, marker, generated `map.md`, and any planned `product.md` change — with `lstat`, records every link or incompatible kind on that path, resolves every existing component, and proves what lies inside or outside the selected product. A link at a method destination is data about topology, not permission to write to its referent. The staged result replaces that entry with the required local file or directory. For a linked directory, the planner preserves non-method content in the logical directory view without dereferencing descendant links, overlays the canonical method entries, and leaves every referent byte unchanged. A dangling link contributes no referent content but can still be replaced locally. If a linked ancestor below the product root affects several destinations, one staged local ancestor preserves its non-method entries and receives all owned overlays. The command-target link itself remains supported by resolving it once to the physical product root.

The transaction is all-or-nothing at the product boundary. Stage on the same filesystem, verify the staged method digest and planned assessment bytes, retain exact backups of every target entry until application and all checked report commands finish, revalidate each destination immediately before replacement, and write the marker last. `git status`, tracked diff, and untracked diff are product operations: an unexpected exit or unreadable output fails the transaction. Any preflight, stage, copy, digest, apply, marker, status, or diff failure restores the complete target topology and bytes, leaves every outside referent and unrelated byte unchanged, exits nonzero without a stack, and prints no migration success, working-tree/diff banner, commit instruction, or resume action. Only after those conditions hold may cleanup occur and buffered success output become visible. A successful localization names each replaced link and its former target in plain language, says the linked destination was not changed, then prints the complete ordinary report and state-specific next action.

This is process-failure atomicity under the existing exclusive-command assumption, not a claim of power-loss or hostile-race atomicity: plain Node path APIs cannot close a concurrent rename race. The command proves the transaction root and resolved product share a filesystem before staging. It normally stages beside the product, outside its Git worktree; when the product root is itself a mount point, it may use one hidden in-product transaction root outside every reported path, remove it before emitting buffered success, and prove cleanup or rollback. The command also compares the resolved target with the resolved kernel source so a target-root link cannot bypass the kernel-self-install refusal.

Product resolves the remaining non-link kind collision without deletion or hand repair. If an owner object occupies a method path with the wrong kind, the same transaction moves that exact file, directory tree, or other link-free object intact to the first unused real directory in the family `.speck-next-preserved`, `.speck-next-preserved-2`, and so on, under a readable path-derived name plus a short lstat-tree fingerprint; it then installs the required local method entry. Existing preservation roots are never followed when linked or overwritten when incompatible. The dynamic preservation path joins the checked status and complete-diff report, and the localization disclosure names both old and new paths. Rollback restores the object to its original pathname and removes the new preservation entry; a committed upgrade therefore reverts through Git like the rest of the migration. A clean retry creates nothing else. This conditional path holds displaced owner data, not installed method content: it is absent from fresh and ordinary upgrades and does not add a kernel file, skill, dependency, state, or owner term. If the command cannot prove a unique reversible placement, it refuses untouched and Business remains `broken`; it may not discard or hide the collision to reach green.

Before implementation, Engineering preserves the exact skills and templates escapes against `dd06bfb` and runs them red. The repaired population generates every writable destination and every descendant/ancestor position from the actual manifest, including top-level files, `.claude`, `.claude/skills`, `templates`, nested files, marker, generated map, and migrated product; relative, absolute, dangling, sibling, and in-product-outside-surface link targets; and a command-target link clean twin. It snapshots the complete selected tree, every referent, marker, product, unrelated text and binary dirt, and porcelain. It also plants failures at stage copy, staged digest, final revalidation/apply, marker write, Git status, tracked diff, and untracked diff. Each negative is watched fail; each successful localization runs twice byte-stably. The retained 26 routing, eight assessment, and 221 migration subjects remain individual floors, the same-context team remains red, and one decorrelated path attack is added after the designed population.

The pre-edit home census used the case-insensitive families `copy|copied|installed surface|method surface`, `target|outside|inside|path`, `symlink|symbolic link|linked|regular file`, `report|complete diff|working-tree`, and `failure|fails|refus|nothing.*touch|marker.*last|revert` across `AGENTS.md`, every skill and reference, every template, `CONTRACT.md`, `README.md`, `capabilities.md`, the installer, package, checker, and Product records. `AGENTS.md`, `CONTRACT.md`, and `README.md` already carry the seamless, marker-last, complete-report, and nothing-touched promises; the Experience and Judge skills carry review behavior rather than installer path handling; templates carry record fields; `CLAUDE.md` is only the method pointer. The broken implementation homes are `bin/speck-next.js` and `devsuite/tasks/separated-product-team/check.py`; `capabilities.md` changes only if measured counts or claims change after proof. No installed rule, skill, template, package version, Product authority, dependency, installed file, owner beat, or Piece 9 file changes in this repair.

After this setup commit, Engineering owns only `bin/speck-next.js`, `devsuite/tasks/separated-product-team/check.py`, and measured-result lines in `capabilities.md` if required. Engineering is not alone in the repository, must preserve every Product and other contributor edit, and must not touch `product.md`, `map.md`, `state.md`, `decisions.md`, this work file, any skill/template, `package.json`, or Piece 9 runner/broker/host files. Product hands implementation to Engineering only after this synthesis is committed.

**Engineering carrier replacement before completion:** `/root/bounded_transport_review` built the first uncommitted transaction draft and preserved the exact pre-fix controls, then was interrupted after repeated audit boundaries without reaching the committed preservation policy or checker. `/root/piece8_judge_round4` now inherits that contribution, direct evidence, draft, and every Product constraint as the implementation carrier. Its earlier judgment concerned an older candidate; by accepting this build role it joins the contributor exclusion set and cannot test or judge this repair or the milestone. Product made no implementation edit.

## Product amendment after the carried-map attack

The round-six repair produced candidate `fba4dd4`, but its own complete command was red at the capability home and a bounded audit found incomplete installed-path reporting, unsafe cleanup recovery, hand-picked rather than manifest-derived controls, fresh-only failure fixtures, and a link whose referent overlapped another method root. Engineering repaired those findings in `9b8a736`: 65 focused path subjects passed, including 32 positions derived from the actual installed manifest, full-state failure and retry twins, checked Git behavior, cleanup verification, and cross-root refusal. That candidate still did not reach Built. The auditor's required free attack used a carried `map.md` linked to `AGENTS.md`; upgrade overwrote the method file and silently changed the logical owner map. Candidate `303aea0` preserved that attack as the only pre-fix red and made 67 of 67 focused subjects pass by refusing untouched.

### Active role decisions before the alias repair

**Product — carrier `/root` — active.** Product read Promise 5, the round-six synthesis, the exact `map.md -> AGENTS.md` reproduction, the `303aea0` refusal, and the new Business and Experience evidence. The alias is a Build defect, not an unknowable repository state: its logical bytes and mode are readable before any write. Promise 5 permits refusal only when the version or required assessment disposition cannot genuinely be determined. Proposed change: localize only a carried map link whose logical file overlaps a planned method root; leave non-overlapping carried links alone.

**Business — carrier `/root/piece8_business_return` — active.** Business independently ran `fba4dd4` and found 27 path subjects green, zero-repair linked-path adoption, exact owner-data preservation, and stable retries, but gave the candidate a binding **`broken`** ruling because its claimed complete gate actually returned `FAIL`. On `303aea0`, Business found the new refusal safe but commercially and operationally incomplete: it makes a supported builder repair a knowable repository by hand even though the map bytes can be staged. Proposed change: preserve the logical map locally inside the existing transaction, disclose it, and retain the one-command journey. Earliest Business run: a supported v5 alias upgrades without owner repair, a late failure restores it exactly, and clean plus stable retries pass.

**Experience — carrier `cursor-agent:91c011d0-969b-4728-bfde-4d5b3e4b6dbf` — active.** Experience lived `map.md` linked to `AGENTS.md` and `CLAUDE.md` on exact `303aea0`. Both refusals were calm and untouched but gave no next action; an outside-map-link twin stayed linked and upgraded correctly. Experience supports localization. It requires wording that does not falsely say the old linked destination stayed unchanged when that destination is the method file being replaced. Instead, say that the map's logical bytes and mode became a local regular `map.md`, name its former target, and say the change prevents the method-file replacement from changing the map. Earliest Experience run: both method-file aliases localize with that explanation, the outside twin stays carried, a late report failure restores the original link, and two retries are calm and stable.

**Engineering — carrier `/root/piece8_eng_finish`, inheriting the prior Engineering carriers — active.** Engineering turned the auditor's attack into one pre-fix red and the outside-link case into its clean twin, then made `303aea0` pass 67 focused subjects with an untouched refusal. Engineering's safety preference remains refusal; it accepts that the existing transaction can instead snapshot the readable logical file before any write, stage those exact bytes and mode as a local map root, restore the original link on failure, and keep non-overlapping links out of the write plan. Earliest Engineering run is the same alias population through apply, marker, reporting, rollback, and retry, followed by the complete retained gate.

### Product synthesis and renewed handoff

Product resolves the tradeoff for one-command adoption. If a carried `map.md` is a readable file through a symbolic link and either its immediate target or resolved referent overlaps a root this transaction will replace, the planner reads its logical bytes and mode once before the first target write and adds `map.md` to the transaction as owner content. The staged result is a local regular file with those exact bytes and mode. It never writes through the link. Rollback restores the original link topology; success leaves every old referent byte unchanged. A non-overlapping carried map link remains a link and is reported as carried forward.

The success disclosure names `map.md` and its former target and says its logical contents were made local before the overlapping method path changed. It must not claim that the former destination itself stayed unchanged when that same transaction replaces it. The `map.md -> AGENTS.md` refusal at `303aea0` remains the pre-fix control. Post-fix controls add the `CLAUDE.md` twin, a late checked-report failure restoring marker, product, dirt, link, map bytes, referent, and porcelain, then a clean upgrade and byte-stable retry. The outside-link twin must still stay linked and stable. All 67 focused subjects, the 26 routing, eight assessment, 230 migration/refusal subjects, same-context red control, capability-home check, budgets, and public-detail scan rerun from the final committed tree.

Engineering still owns only `bin/speck-next.js`, `devsuite/tasks/separated-product-team/check.py`, and the measured lines in `capabilities.md`. Product has changed no implementation file. `/root/piece8_eng_finish`, `/root/piece8_business_return`, `cursor-agent:91c011d0-969b-4728-bfde-4d5b3e4b6dbf`, and `/root/piece8_bounded_audit` join the contributor exclusion set for every later Piece 8 and milestone test or judgment.
