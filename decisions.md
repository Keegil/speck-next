# Decisions

**2026-08-17 · The bridge from shaping to building — the map, the window, the summons, the live-owner beat.** Pilot's first build day failed the transition ("the shaping was really, really good, but the bridging into building is a complete mess. It feels aimless"), and the owner was interviewed on the forks. What landed, on his choices: (1) **`map.md`, a separate fifth file** — the ordered build pieces, each naming what it serves and which shaped material it consumes, one live; written when shaping closes, re-cut when rulings change the piece-space, before code. Old Speck's breakdown was right in kind and catastrophic in weight; this is its useful core. (2) **Shaping's outputs are the build's inputs** — his sharpest finding: the build day ignored a 37-frame owner-ruled deck entirely ("we had basically built out the shell of the entire product... and the agent just ignored it"). The map carries asset consumption, unconsumed gold stays listed and visible, and a piece that realizes drawn frames starts from those frames. Surface-first vs machinery-first stays agent judgment per his ruling — but "run the product" now means the product's own surface once one exists, a self-built harness never satisfies it, and state says "no user surface exists yet" in as many words while true. (3) **The review summons is structural**: nothing reaches Proven without the receipt, and at most one substantial piece may sit unproven when the next starts — review debt is bounded like WIP, chosen by him over strict serialization and periodic cadence. (4) **State follows the map while the owner is in the room**: state.md re-lands at every map-changing event (piece start/end, ruling, reopen) and every reply names where we are — he should never have to ask. Rulings that invalidate work get a trace: the decision entry names the reopened pieces and the reopened work files point back. (5) The reviewer's playbook gains the four git-observable loop checks (work-file-before-code, state staleness, receipts past Built, map-vs-reality) — the loop's disciplines were all observable that day and nothing observed them. Agent-failure half acknowledged plainly: the method said to do most of this and it wasn't run; the fixes make the violations mechanically visible rather than adding rules. Rejected: map-in-state (his call: separate artifact), hard surface-first ordering (his call: judgment, given an excellent map), every-piece serial review, and per-message state refresh. Prediction, measured at Pilot's next build day via the playbook's git checks: the owner never has to ask where we are, deck frames appear as consumed inputs in the map, and review debt never exceeds one.

**2026-08-17 · Releases: versioned surface, stateless upgrades, and the two upgrade roads.** How repos stay current, decided small: (1) `main` is the release line; any change to the installed surface (AGENTS.md, skills, `bin/`) bumps the version and gets a `vX.Y.Z` tag, so `npx github:Keegil/speck-next` installs latest and `#vX.Y.Z` pins. (2) The install marker records version *and* kernel commit, so `upgrade` reports exactly what span a repo crossed, and git in the target repo shows every changed line — no changelog corpus; the kernel's own plain-language git log is the changelog. (3) Upgrades are structurally safe because the method surface is stateless: it never touches the four product files, so upgrade = overwrite + one revertible commit, no markers, no catch-up ladders. The day a version changes the *file contract* (state.md's sections, product.md's expectations), that version's upgrader must carry the file migration itself — the marker's version is what it keys on; until then every upgrade is surface-only and free. (4) Old Speck v11 → Speck Next remains promise 5's deferred converter: the CLI refuses old-Speck repos loudly today, and the converter gets built as its own piece when the first real old-Speck repo (streb, brightstance, speilet, flyt) reaches a natural crossing point — against those real vintages as fixtures, with odd-old's distilled era knowledge as the quarry. Rejected: GitHub release artifacts and an exporter (the repo is the package — npm's git install ships it whole, and old Speck's exporter once shipped 18k lines of its own tests downstream); auto-upgrade hooks (an upgrade is a deliberate act with a diff the owner can see). First proof: the version fiction ended today — every install since day one said "1.0.0" while the surface moved through the whole campaign-feedback arc; v1.1.0 is the first true version, and Pilot's upgrade to it is the first real span-crossing.

**2026-08-17 · The read-set budget rises to 50 KB — owner decision, with the contract's required trade.** His ruling: "I think the shaping process with Pilot benefited from the additional context added to product.md. I think we should raise the budget." The always-read limit (AGENTS.md + state.md + product.md) goes 25 → 50 KB; the compensating tightening is the installed-footprint limit, 25 files / 250 KB → 15 files / 100 KB (today's real install: 9 files / 23 KB — the tightening binds without pinching). The spirit written into the contract with the number: the budget bounds drag, not thinking — a product.md full of shaped decisions is inside it, one full of narration is not, at any size. Reopens if a product.md approaches 50 KB with content a work file should own, which is the narration failure wearing the new budget.

**2026-08-17 · The first real campaign's six lessons land — each in its smallest form, none as new machinery.** Pilot's 30-round shaping campaign (verified against its primary artifacts, not the session's self-report) had to invent six disciplines the kernel didn't provide, and each now lives where it was missing: (1) the owner's words kept verbatim and append-only in the work file, the reference every review runs against (shape-product rule 10 — its scar: three refuters found non-verbatim quotes in the owner's mouth in product.md's first draft); (2) the round-trip rule extended to shaping (`AGENTS.md` — the owner's round-7 instinct was the method's own principle applied earlier than the method said; the wire reshaped a whole job when dinners turned out to have no evidence source); (3) drawings pressed against model and wire before the owner judges them (shape-product rule 11 — the journeys stress test found the campaign's largest defect); (4) shaping artifacts get the decorrelated refute pass *before the owner reads them*, judged against the owner record and the wire, never sibling documents (independent-review — and the census found the sharpest scar here: **every pass in the campaign was receiptless**, because the receipt law was scoped to reviews-that-run-products and the whole document stage fell in its blind spot; the extension closes the scope hole and states the receipt law applies unchanged); (5) a judging number is a hypothesis until it meets data, with honest denominators (attack playbook — the 0.60-floor and 431-av-609 scars); (6) where artifacts disagree, `product.md` and `decisions.md` win, and superseded artifacts say so at their top (`AGENTS.md` — cross-artifact contradiction was the campaign's biggest recurring defect class, and four superseded HTML decks still carry no marker, which is why the rule says *artifact*, not *document*). Plus one blessing: long shaping is legitimate exactly while real evidence increments run through it (shape-product intro — three days was right *because* the round-trip, the backfill, and the probes ran the whole way). Not adopted, on purpose: no journeys/deck artifact classes, no design-loop skill (that path is how the predecessor grew), no mechanized state-consistency checking (the drift class — "29 rounds" vs "30" in one file — is noted and watched), and the closing-audit's merge-by-the-audited-party conflict is recorded here as a known tension rather than machinery. Escalated to the owner: the read-set budget — Pilot's shaped `product.md` alone is 34 KB against the contract's 25 KB always-read promise, and budgets move only by owner-approved trade. Prediction, measured at Pilot's next campaign round: these disciplines appear without being invented, and the review receipts exist from pass one. Reopens per item if the next campaign has to invent it again anyway.

**2026-08-14 · AI-first means rebuilding the category, not decorating it — owner doctrine, refined.** The shaping skill's first AI-first cut asked "how would each surface work in 2126?" — which produces the same product with smarter screens. The owner's actual doctrine, now encoded as rule 2 and questions 9–13: reason from the job, the domain's first principles, and the user's real mental model; treat the category's inherited concepts as suspects (structures born of dumb software or physical constraints are fossils — deleted and rebuilt, never automated); take the best human who ever did this job as the interaction bar (asks, assumes out loud, takes corrections, remembers, adjusts); center the product on a living model of the user that does the work; and keep it honest with the collapse test — unplug the model, and a product that still works was never AI-first. Considered instead: keeping the per-surface question only (rejected — it decorates fossils), or prescribing "conversational UI" (rejected — the bar is the relationship, not a chat window; an expert's assumptions can surface anywhere). Refined same day by the owner: this is a bias with teeth, not a mandate — not every product reinvents its category, but the consideration is always run thoroughly, the burden of proof sits on convention (unguided builders churn out lookalikes), and "convention serves this job" is a legitimate verdict only when earned by the fossil hunt and written into `product.md`, never reached by silence. Examples in the skill kept generic (filing-cabinet folders, paper-form tax software) rather than drawn from the owner's own products. Reopens if shaped products under this rule still come out as the incumbent category with AI on top — which the owner will feel immediately, and the felt rating exists to catch.

**2026-08-13 · Structural investment is harness-owned at four points; judgment fills them — the anti-spaghetti design.** The owner's concern, and it's the classic agile failure: small loops that are each proven good can still stack jank, because stateless agent sessions don't accumulate the pain that triggers human refactoring, and nothing in a greedy loop rewards stopping. Decision: the harness owns *where pain is recorded, when it must be read, who calls it, and what repeated pain forces* — never *what the structure should be*. Concretely: (1) a strain ledger in `state.md` — working around instead of through anything writes a line, so pain survives between sessions; (2) shaping starts by reading it — twice-recorded strain becomes the next piece or is deferred visibly; (3) the structure verdict belongs to the independent reviewer (sound / straining / fighting), because the builder always has momentum and the party that wants to keep building never decides whether building pauses — two straining in a row, or one fighting, forces structural work; (4) predictable foundations are named at shaping time with the trigger that makes each due, and fire as ordinary pieces. Structural work is first-class: shaped and proven like a feature, proof = behavior unchanged and the strain gone. Considered instead: leaving it entirely to model/user judgment (rejected — the momentum conflict-of-interest is structural, same shape as self-review); mandatory periodic architecture reviews (rejected — cadence-based ceremony is how old Speck grew 183 findings with zero real defects); upfront foundation phases (rejected — old Speck's foundations were mostly documents about foundations; Pilot had an architecture.md and no walking skeleton). Reopens if a real product accumulates spaghetti anyway — measured by refactor pieces repeatedly discovering strain no one recorded.

**2026-08-13 · v1 is accepted on confidence, not on a contest — owner decision.** The head-to-head benchmark against Speck v11 is descoped. v1 is good when the owner's agent is confident that: all valuable, non-obvious old-Speck learnings are adopted; the methodology demonstrably works (dev suite green, real exercises built with it end to end); and it's a true value-add over a no-harness agent — judged on the field record and how frontier models actually work. v1's target use: rebooting the Pilot project greenfield (new repo, product brief engineered from the current project). Consequences: the fixtures file becomes a designed-but-shelved contest (kept — its planted-bug classes still seed the dev suite); the upgrader drops off the v1 critical path (greenfield reboot needs a fresh-start path, not conversion — promise 5 stays for a later version); v1's proof burden shifts to the dev suite plus real product exercises plus a decorrelated confidence audit against the owner's goal text. What must newly exist for v1: product-shaping guidance (jobs-to-be-done, first principles, AI-first "year 2126" bar, magic moments) and UI/experience judgment — the parts of old Speck that made products stand out, not just correct.

**2026-08-13 · Kernel development runs as an evolution loop with a train/test split.** The frozen contest stays the held-out exam — never iterated against, plantings secret. What was missing is the *search side*: a cheap development suite (tiny scored tasks on toy repos, plantings visible, runnable in minutes) that kernel changes are measured on between contests. Method changes follow candidate discipline: one mechanism per change, a falsifiable hypothesis with a predicted effect written in the work file, the measured result written back — which is also how promise 8's "predicted benefit, measured or retired" actually gets operationalized. Analysis reads real session transcripts and run logs, not summaries; and a change prompted by a dev-suite failure must pass the generality test — would it help across many unfamiliar tasks, or just this one? Pattern adapted from Stanford's Meta-Harness (github.com/stanford-iris-lab/meta-harness, arXiv:2603.28052), whose reference loop burns ~$500 per iteration — ours must cost pennies, so dev tasks stay pulse-sized. Considered instead: iterating directly against the contest (rejected — overfits the exam and breaks its integrity rules) and no quantitative loop at all, self-hosting friction only (rejected — friction finds usability problems but can't measure detection or cost regressions). Reopens if the dev suite and contest results ever diverge badly — that means the dev suite drifted from what matters.

**2026-08-13 · One method, many subjects.** The method surface (`AGENTS.md`, the skills, later the upgrader) is byte-identical in every repository it governs; only the four product files differ. This repository is not special — it's an ordinary Speck Next repo whose product happens to be the kernel, which is what makes self-hosting and installability the same thing rather than a conflict. Installing into a product repo means placing the method surface and creating (or converting) that product's four files; the kernel repo's own product files never ship anywhere. Product-specific standing guidance (stack, commands, conventions) belongs in the product's own files, which agents always read anyway — the method page stays identical everywhere; the exact seam gets settled when the upgrader is built. Considered instead: a special dev-mode method page for this repo (rejected — then we'd never be testing the real thing), and per-repo customized method pages (rejected — 5 diverging pages is the old upgrade treadmill reborn). Reopens if a product genuinely needs standing agent instructions that fit none of its four files. Editing `AGENTS.md` in this repo is always substantial work — it changes the governor — so it takes the full loop and independent review.

**2026-08-13 · Old Speck's lessons live in four homes, and thinness is only honest with all four.** The kernel page carries only what every piece of work needs. The rest of the validated knowledge lands where it's actually used: dense on-demand references inside skills and extra-care packs (first harvest: the attack playbook in `independent-review`, distilled from old Speck's audit doctrine and the field defects); all era knowledge in the upgrader; and the sharpest form — the lessons as *tests*: the contest's planted-bug classes are the old scars operationalized, and the kernel must match full-strength v11 per class or lose. Old Speck's repo stays intact as the quarry, and any mechanism can re-enter through the six questions with evidence. What we refuse: bulk-importing 93k lines of enforcement plumbing and calling it respect — most of that mass was machinery about machinery, and the knowledge inside it was always small. Reopens per class: any planted-bug class the kernel misses that v11 catches pulls the relevant old mechanism back through the six questions.

**2026-08-13 · The method ships as the host's own loading surface, and this repo self-hosts.** The method page is `AGENTS.md` — the file agent hosts already load automatically — not a standalone document an agent would have to be told about; on-demand procedures are skills, the hosts' native lazy-load mechanism. Considered instead: a `kernel/doctrine.md` referenced from tooling (dead text — no host loads it; this was briefly built and reverted the same day), and a custom loader (rejected outright: the contract calls needing a loader the re-bloat alarm). Also decided: this repository runs under its own method from now on, so every development session is a live test of the thing being built — the tightest feedback loop available, and free. Reopens if a host materially changes its loading rules, or if self-hosting ever masks a difference between how the kernel treats itself and how it treats a product repo.

**2026-08-13 · (pulse) Storage: one human-readable JSON file with a write lock** — moved to [examples/pulse/decisions.md](examples/pulse/decisions.md), where it belongs.
