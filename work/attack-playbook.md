# The attack playbook

**Outcome:** the `independent-review` skill carries old Speck's hardest-won reviewing knowledge as an on-demand reference — trace promises not tests, real least-privileged users, break the nets on purpose, distrust fixtures, judge the whole — in plain language, loaded only when a review runs.

**How I'll know it works:** the next real review in this repo uses it and catches something the builder's own proving missed, the way the pulse review did before the playbook existed.

**Open:** it's distilled from the audit doctrine and the six field defects; old Speck's UI-specific and LARP-specific depth isn't harvested yet — that pack gets written when the first UI product needs it, not before.
