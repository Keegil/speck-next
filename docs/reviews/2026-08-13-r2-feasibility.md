# Round 1 · Reviewer 2 — feasibility and benchmark integrity (Claude Opus, fresh context, read access to the real consumer repos), as received

Target: CONTRACT.md v0 + fixtures.md v0 + deletion-manifest v0 + README v0. Everything quantitative was measured against the repos, not inferred. Findings F1–F21 (P0×5, P1×12, P2×4), abridged only where repetitive with the sibling reviews.

**Benchmark integrity.** F1 · P0: Lock 1 does not lock the tasks it claims — 8 of 14 prompts are unfilled brackets; difficulty and planting depth remain the author's post-design discretion; the label overclaims, which the contract calls a defect class. F2 · P0: the reused self-eval corpus is an answer key, not a test — 12 fixture dirs totalling ~23 lines of toy strings, each shipping a manifest naming its class and expected outcome; both systems score ~100%, making "≥ v11.2.0" a null test. F3 · P0: the whole-property register enumerates five process virtues and omits the product; quality is judged on 2 of 14 tasks by one judge with no axis separation — the precise structure the register exists to prevent. F4 · P1: no task reaches beyond two increments, so the property Speck's ceremony was buying — coherence across many increments — is unmeasured (odd: 20 story dirs; streb: 176); add a 6–10 increment task judged at the end. F5 · P1: "caught" is undefined — a system emitting 183 findings will mention the planted class; a catch must be reproduced on the real path with a control seen red; mentions bill as cost. F6 · P1: "blind-judged" is unachievable as specified — artifact count identifies the system; define the judge's input set (product diff + running-app evidence only). F7 · P1: n=1 stochastic runs decide a repository-boundary bet — the same "statistically unestablished" error v11's own release memo admits; n ≥ 3, pre-registered, variance reported. F8 · P2: retiring the banned-language linter to an evaluation defect class rigs one falsification class for v11, which wins it with a deterministic grep; keep a ~20-line check or mark the class mechanical.

**Adoption feasibility.** F9 · P0: Promise 5 contradicts the binding migration recommendation and T13 encodes it — measured today, odd is mid-epic: `epic-punch-list.md` 2,243 lines with 10 open items; `speck_graph.py check` returns `GRAPH_CAP = NO-SHIP` with five live `PHANTOM_PROMISE.P1` findings and `UNJUDGED_SURFACE.P2` on all seven magic moments. There is no clean state to convert; seamless must mean the adoption needs no repair, not that the adopted state is clean; findings import with original ids. F10 · P1: "zero manual repair" and "fails loudly" cannot both hold — name the third outcome, *converted with disclosed residue*, and pass T11–T13 on residue-disclosed-and-non-lossy. F11 · P1: the or-clauses let the author pick the easy repo — flyt is platform-level with 960 spec files vs speilet's 486 and no witness.json; brightstance's state file is 3,050 lines vs streb's 1,590; adopt all four or freeze the harder pair. F12 · P1: the footprint budget has no denominator — 176 story dirs becoming 176 packets either breaks the budget at item 26 or leaves on-disk cost unbounded; the index, packs, adapter, and adopter are uncounted; "automatically visible" is an accretion escape hatch; post-adoption odd still carries 1,643 tracked spec files / 231 MB archived. Split the budgets: framework files installed (everything counted, visible or not) vs durable artifacts per work item; the archive stays and is outside the budget. F13 · P1: the 8 KB doctrine budget measures the wrong read set — the per-session load is doctrine + state + product, and today those reach 3,050 lines (brightstance state) and 104 KB (odd product-contract); budget the read set (~25 KB) and make product.md growth a breach. F14 · P2: era detection — `speck_version` is missing in three of five repos and documented advisory; `.speck/VERSION` is the reliable source (v11.0.0 / v9.5.0 / v9.5.0 / v7.16.0 / v7.16.0) and does not exist before v7; ambiguity must refuse loudly, never guess. F15 · P2: four of five snapshots exist only on this laptop (streb +2, brightstance +2, speilet +1, flyt +9 unpushed), and the founding documents themselves lived in a /tmp scratchpad; push or bundle before Lock 2 and commit the documents.

**Falsification sufficiency.** F16 · P1: none of the three conditions is measurable as written; the budget condition's "without a measured quality loss" is an unfalsifiable escape hatch — an overrun must falsify, with quality measured on the four axes. F17 · P1: the falsifier is narrower than the promises — T14 and the synthetic eras are excluded, so "zero legacy knowledge" and "every released era shape" have no falsifier. F18 · P1: the uncaught failure mode is winning the benchmark and never shipping — speilet and flyt sat on v7.16 from May to August; add a use-in-anger condition with a date and a named reader, and a one-line v11 freeze policy so the fallback cannot rot.

**Deletion manifest.** F19 · P0: the witness-graph row deletes promise conservation with no named replacement mechanism and no benchmark task able to notice — the largest BUILD-side catch on record is 59 of 151 promises undelivered behind green gates, and odd exhibits the class live today; add a conservation task and name the replacement's mechanism, finding id, and stop power. F20 · P1: three more rows hand-wave mechanisms into doctrine — the red-capability probe (gate-liveness scar) deleted while the doctrine cites it; rigor selected by the implementer is self-certified risk; the marker ladder is what made partial conversion non-silent, deleted in the same contract that promises never-silent. Keep a kernel probe; owner/evaluator sets risk tier; the adopter writes one marker the kernel refuses to run past. F21 · P2: the engagement-ladder replacement is self-referential — the agent judges the size of its own claims; add a stale-state task.

**Verdict:** The diagnosis and the deletion instinct are sound, and the freeze discipline is real — every SHA resolves and the fixtures honestly separate architectural evidence from framework defects. But the corpus is not yet safe to build on: Lock 1 did not lock what it says; the register reproduces, in the document that cites it, the precise failure shape it was invented to catch; and Promise 5 is not achievable as worded against the measured state of these repos until *converted with disclosed residue* is named. Fix F1, F2, F3, F9 and F19 before any framework code; F19 is the row that would delete a protection the field record proves necessary with no benchmark task able to notice.
