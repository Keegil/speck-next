# State

**What's true now.** Pulse logs (`pulse 4`), charts (`pulse`), and notices (`pulse innsikt`). Two pieces are **Built**:

*Log and view* — **works**: every proof-plan path green including the reviewer's breakers (20/20 parallel writers stored, five corrupt-journal shapes refused, clean errors). **Delivers the promises**: both promise-breaking review findings fixed and re-proven. **Good to use**: the reviewer's judgment, "genuinely pleasant". **Quality hangs together**: the fixed build not independently re-run — not judged yet.

*Innsikt* — went through independent review (fresh cross-vendor context, controlled the model with a fake ollama) which broke it properly: fabricated weekdays and values passed the old validator, banned and diagnosis language passed untouched, the pattern verdict depended on JSON key order, and three crash paths leaked tracebacks. All fixed and re-proven against the reviewer's exact commands. **Works**: proving scenario passes both ways (planted pattern named with real weekdays and averages + gentle question; patternless journal gets an instant honest no-pattern line with no model call — a pattern only counts when it beats 200 shuffles of the user's own values); verdicts independent of file order; every crash path speaks the product's voice. **Delivers the promise**: every weekday and number in the model's words must exist in the computed fact — fabrication, banned words, diagnosis language, emoji, echo, and missing-question are all mechanically rejected (each watched failing red); rejection or model failure shows the computed truth plainly, never anything canned or invented. **Good to use**: honest but plain — an 11B local model restates more than it charms; the wait is named. **Quality hangs together**: not judged yet — the post-fix build hasn't been independently re-run.

**What's blocked.** Nothing.

**What needs the owner.** Nothing — Pulse exists to exercise the method.

**What happens next.** Independent review of the innsikt increment. Open smaller items: viewing a journal dated near year 1 still crashes (absurd edge, accepted); model warmth is capped by the local 11B — a better local model would lift the moment without changing the design.

**Evidence.** Proof runs in this repo's history (2026-08-13): the planted-pattern and flat-journal runs, the validator red-runs (generic advice, instruction echo), the model-down and under-five-days voices, and the permutation-test design decision recorded in [work/innsikt.md](work/innsikt.md).
