# Three producers

**Serves:** the report-of-itself strain (eight bites — every one a defect these three rules would have stopped at the source) and promise 6. · **Consumes:** the definitions piece's converged recommendation (both judgment lines, all four tester runs), the owner's 2026-08-29 selections ("Land + name what's open" for `sufficient` · "Run it now"), and judge 2″'s named root cause: the judge's quoted control becomes the builder's requirement.

**Outcome:** four sentences land, each stopping a defect class at its source instead of catching it a round later. (1) **Same edit, every home:** a rule change lands in every file that states the rule, found by grep before the commit, listed in the record — a rule fixed in one of its homes is not fixed. (2) **A new word gets its sentence:** the first time a rule-carrying word is written, it is defined in the same breath or replaced with a word the pages already define. (3) **A control names its population:** a judge's quoted control states the whole population it must hold over, never only the site the defect surfaced at — the builder executes the requirement; the control is its floor. (4) **The owner's word, transcribed:** a ruling is *sufficient* when the piece delivers what it was shaped to deliver and everything still open is named in `state.md` with where it went — a judge may land a piece with open items, never with silent ones. Homes: 1 and 2 on the method page (they govern every session's edits); 3 in the judge's re-run section; 4 beside "Judged means its review ruled it sufficient" and in the judge's ruling step.

**Before first run:** one session; only the anchor sentences and the eight bites' records read before drafting.

**Proof plan and review cost:** checks that must pass for Built — the four sentences present at their named homes (grep each), no new undefined term minted by the additions (rule 2 applied to its own author), control arm 4/4 red · then two fresh testers — a cold reader who re-runs the census delta (the additions only: every new rule-carrying word must define or be defined; also applies rules 1 and 3 to two planted fixtures: a one-sided rule edit and a site-scoped control, both of which the new sentences must condemn) and a conservation prober (word-diff, budgets pinned with commands, sibling sweep of every home the four sentences touch) · one judge; a second only if the judge orders it. **Predicted effect (one, falsifiable):** the planted one-sided edit and the planted site-scoped control are both condemned by a fresh reader citing the new sentences — each watched failing once — and the census delta stays at zero new undefined terms.

**Planted fixtures** *(committed here so the controls are re-runnable)*:
> Fixture 1 — a work-file line: "Fixed the receipt rule in AGENTS.md." (The receipt rule also lives in the experience skill and the piece template; the edit touched one home of three.)
> Fixture 2 — a judge's route: "Control: `grep -c 'stale figure' state.md` returns 0." (The figure also lives in decisions.md and README; the control names one file.)

**Review receipt** *(committed before the testers run)*: built — `state.md` at fc33a7b says: "The live piece "Three producers" is Built as of this commit" — after the build (77628d1), before this receipt · deltas from `caf557f..HEAD`: the four sentences (AGENTS.md ×3 sites, judge ×2 sites), the state writes · testers: T1 the cold reader (census delta on the additions + the two planted fixtures — each must be condemned citing the new sentences) and T2 the conservation prober (word-diff, budgets with commands, sibling sweep of every home the sentences touch), fresh contexts, model opus · judge: one, fresh, after both return — it may order a second · run owner: this session · empty record: re-dispatch under a named line · records: [T1](three-producers-T1.md) · [T2](three-producers-T2.md) · [judgment](three-producers-judgment.md)

**Judgment** *(fresh context, built and tested none of it)*: Claude Code · Opus 5 (`claude-opus-5[1m]`) · session `d6f7954a` · 2026-08-29 · piece "Three producers" at `def1677` (build `77628d1`). Full ruling: [three-producers-judgment.md](three-producers-judgment.md).

- **Receipt:** valid. Built line at `fc33a7b` literal, build-only commit before it, no build commit after, receipt opened after. `6c22d89` is state-only and invalidates nothing.
- **Works:** check failed. Rule 2 works; rule 4 works where reachable (T1 ruled a live case from it, and so did this judgment). Rules 1 and 3 do not fire at their source — `grep -n "sufficient" judge/SKILL.md` cannot find the definition it landed beside, and `grep -c "population" AGENTS.md` → 0.
- **Delivers the promise:** broken. Promise 6 kept (T1 read all four in one pass and could say what each buys); promise 7 kept (59,596 B of 102,400; 25,738 of 51,200). **Promise 4 broken:** `state.md:5` is byte-identical to `cca7841` and now false, and `state.md:22` cites `decisions.md` for an owner ruling that file does not contain.
- **Good to use:** kept, with two sharpenings — `judge:92` costs a second pass; the `destination` collision fits a landed piece's open items badly.
- **Quality hangs together:** check failed. Seven workmanship defects, six of them the piece's own subject matter.
- **Structure:** straining. The method has no step that makes a new rule reach the surfaces it must govern. The arithmetic does not fire `judge:63` cleanly (line 1 ruled sound, line 2 straining on the previous piece), and I did not manufacture it. The repair is reach, not a fifth sentence — promise 8 forbids the latter.
- **Sent back to build**, nine controls (R1–R9), each quoted with its whole population. Second judge ordered for the re-judgment, not for this one — reasons in §8.

**For the owner — one question, and it decides how the fourth sentence lands.**

*What this changes for you:* the sentence defining when work is finished — *sufficient* — is now law on two pages, and every future judgment runs on it. The record says you ratified it last night. Your words are not anywhere in the repository, so I cannot tell whether the record describes something you said or something the judges recommended.

*What I found:* `state.md` says your two answers are "his calls in `decisions.md`". `decisions.md` has neither, and was last written half an hour before this piece started (`cca7841`, 18:50:49 vs `caf557f`, 19:19:14). The sentence that landed is nearly word-for-word what both of the previous piece's judges had already recommended. Most likely you answered in the session and nobody wrote it down — but "wrote it down" is the thing being claimed, so the files cannot settle it.

*Your options:*
1. **You said it — transcribe it.** Your actual words for both answers go into `decisions.md` with the date, like every other call of yours, and `state.md`'s citation becomes true. The sentence stays; this is a records fix inside the batch already ordered.
2. **You didn't say it, or not in those words.** The fourth sentence has no owner behind it and returns to shaping to be asked properly, on a plain rendering, before it governs anything. The other three are unaffected.
3. **You said something different.** Tell us what, and it is rewritten to match — same route as 2, shorter.

Nothing else in the review waits on you.

**Result:** Routed back to build on 2026-08-29. The piece keeps the live slot; the map stays unticked (it never got ticked — the piece is absent from `map.md`, finding J2). The four sentences themselves need no rewriting: a fresh cold reader condemned both planted fixtures quoting them verbatim and the falsifiable prediction held on both arms. What failed is reach. **For the next builder:** a rule is not landed when it is written, it is landed when it can be found — and rule 1, the sentence written to ask which surfaces carry a rule, asks only which ones already do.

**The judgment** *(one judge, 2026-08-29 — [judgment](three-producers-judgment.md); a second judge is ordered for the re-judgment)*: **routed back to build.** What held: conservation total (pure insertions, +948 bytes accounted to the byte), the census delta at zero undefined words, both planted fixtures condemned by verbatim-quotable sentences, the cold reader ruling a live landing case from the pages alone in four steps, and the piece's falsifiable prediction holding on both arms. What routed it: two of the four rules did not reach the surface where their defect is committed (the sufficient definition invisible to rule 1's own grep by a capital letter; rule 3 living only on the judge's page while the builder's page stayed scope-shaped) · the state file carried a sentence made false by this very piece · and the sharpest catch — the files cited the owner's ratification to decisions.md, and decisions.md did not carry it: the answers had come through the session's question tool and were never transcribed. The judge refused to trust a record that claimed a record.

**The fix batch** *(the judge's controls fired red before, green after — the greps, run now per the very rule under review)*: the owner's three selections transcribed into decisions.md as what they are — selections of drafted options, dated, the option text quoted (`grep -c "Land + name" decisions.md` 0 → 1) · state.md's false census sentence corrected and swept · rule 1 sharpened to places-not-files, case-insensitive distinctive phrases, and a record that carries the grep with every home marked (`grep -c "grep for its distinctive phrase" AGENTS.md` → 1) · rule 2 gains "in the sense you mean" (→ 1) · rule 3's builder half now on the builder's page: the send-back paragraph executes full requirements with quoted controls as floors, plus the free attack that one copy had dropped · the population triad glossed (three kinds of member for three kinds of defect) · the state and piece templates gain the open-items-with-destination obligation · the map names the live piece · the stale-host-pages trap is now a tester law in experience ("read every page from disk at the commit under test") and a recorded strain · coverage claims in this file now carry their commands: the four sentences at their homes — `grep -c "grep for its distinctive phrase" AGENTS.md` → 1 · `grep -c "in the sense you mean" AGENTS.md` → 1 · `grep -c "floor, not its scope" .claude/skills/judge/SKILL.md` → 1 · `grep -ci "delivers what it was shaped to deliver" AGENTS.md .claude/skills/judge/SKILL.md` → both files.


**Re-entry receipt** *(committed before the re-runs)*: built — `state.md` at 8e9140e says: "the fixed pages are Built as of this commit" — a state-only commit after the fix build (684e359), before this receipt · deltas from `def1677..8e9140e`: the judge's nine routes closed (decisions transcription, state sweep, the rules' reach, the templates' obligations, the map entry, the strain, the commanded coverage claims), all in 684e359, plus the state-only Built write · re-runs, per the judgment: sweep siblings first, then the judge's scenarios executed as full requirements with the quoted controls as floors, plus one free attack, one combined tester appending to T2's record · then the re-judgment with a second judge, each with its own receipt line · run owner: this session · empty record: re-dispatch under a named line.

**Correction to the preserved judgment block above (append-only):** its present-tense sentence "decisions.md has neither" was true when the judge wrote it and is false since 684e359 — the transcription landed; the block stands as the judge wrote it, with this note as the marker.

**The judgment continuations split, and the union of their orders is executed:** line 1 closed sufficient with eleven conditions; the ordered second judge, ruling blind from primary evidence, routed back (B1–B8 + E1) — its deciding fact: the batch fixed one of the two builder-page homes the judgment had named and left step 5 byte-identical, a one-home fix inside the piece whose first sentence forbids it, and its sharpest find: rule 1's "not one keyword" forbade the only grep key that would have caught the miss. Everything both lists order, landed: step 5 now carries the floor rule in the same breath as the send-back paragraph, which now sweeps the requirement's whole population · rule 1 rewritten — it fires on landing new rules too, tries several keys widest-first, and reports the set · the stale-page law reaches the judge's own page and its count tells the truth (five reviewers in one night) · the piece template gains the homes-touched slot · the fixture gloss corrected below · the ratification labels downgraded to what the record says (selections) · state.md carries the four rulings in the judgment's own words and the true bite counts.

**Fixture 1's gloss, corrected (the committed control had mis-stated its own corpus):** the receipt/Built rule lives in **four** homes, not three — found by `grep -rnilE "literally say|Built line that covers" AGENTS.md .claude/skills templates` → AGENTS.md · judge/SKILL.md · experience/SKILL.md · templates/piece.md.

**Coverage, restated per rule 1's own record clause** *(each grep over AGENTS.md, all skills, and templates; every returned home listed and marked)*:
- Rule 1 (`grep -rnil "one-home fix is not a fix"`) → AGENTS.md — changed; sole home by design (the build-loop discipline).
- Rule 2 (`grep -rnil "in the sense you mean"`) → AGENTS.md — changed; sole home by design (the files-section discipline).
- Rule 3 (`grep -rnilE "floor, not the scope|as its floor"`) → AGENTS.md — changed (both builder statements, step 5 and the send-back paragraph) · judge/SKILL.md — changed (the population sentence).
- Rule 4 (`grep -rnil "delivers what it was shaped to deliver"`) → AGENTS.md — changed (the ladder) · judge/SKILL.md — changed (the ruling step).


**Third re-entry receipt** *(committed before the final run)*: built — `state.md` at 2eea8d4 says: "the re-fixed pages are **Built** as of this commit" — a state-only commit after the union fix batch (0f30c51 and the state-truth commit before this), before this receipt · deltas from `7bcaf95..2eea8d4`: the union of both judgments' orders (AGENTS.md step 5 and the send-back paragraph, rule 1's rewrite, the judge's from-disk law, the piece template's homes slot, the fixture gloss, the coverage record, state's truth) · the ordered run: E1 — one fresh cold reader, from disk at the commit under test, reads the changed sentences one pass each (AGENTS.md steps 4-5, the send-back paragraph, rule 1, rule 2, the ladder; judge's population and sufficient sentences) and reports what each buys a builder; re-runs both planted fixtures against the fixed text; one free attack · then the blind judgment line rules its final word · run owner: this session · empty record: re-dispatch under a named line.
