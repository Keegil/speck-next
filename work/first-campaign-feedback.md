# What the first real campaign taught the kernel

**Outcome:** the six disciplines the Pilot shaping campaign had to invent land in the kernel — each in its smallest form, none as a new artifact class or skill — so the next campaign gets them for free: the verbatim owner record, the round-trip during shaping, the design loop pressed against model and wire, adversarial review of shaping artifacts against the owner's words, numbers-as-hypotheses, and an explicit artifact-hierarchy winner. Plus the blessing that long shaping is legitimate while evidence increments run through it.

**How I'll know it works:** each adopted change cites verified primary evidence from the Pilot campaign (not the session's self-report — a census pass confirms the claims first); the method-page edit passes its own law (substantial → independent review with receipt); the dev suite stays green; the always-read set stays inside budget. Prediction, measured at Pilot's next shaping or review round: the campaign-invented disciplines appear without being invented — the owner record exists from round one, shaping artifacts get their refute pass before the owner reads them, and no cross-artifact contradiction reaches the owner unflagged.

**Result (2026-08-17):** all six landed after the census pass verified the campaign's claims against primary artifacts (nine of ten confirmed — the SC register fired five times, not the reported four; one PARTLY: four superseded HTML decks carry no supersession marker, which shaped the hierarchy rule's wording). The census also found what the retrospective missed: the campaign's every adversarial pass was receiptless (the receipt law's scope hole — now closed), the product.md pass left its 30+ findings only in a commit message, and the closing audit's six-lens merge was performed by the party under audit (recorded as a known tension, not mechanized). Changes: AGENTS.md (hierarchy winner + round-trip-in-shaping), shape-product (rules 10–11 + the evidence-increment blessing), independent-review (shaping-artifact reviews with the receipt law applying unchanged), attack playbook (numbers-are-hypotheses). Kernel read-set after edits: AGENTS.md 4.9 KB. Awaiting: independent review of these method-page changes (the governor changed — full loop applies), and the dev suite re-run.
