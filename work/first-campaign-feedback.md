# What the first real campaign taught the kernel

**Outcome:** the six disciplines the Odd shaping campaign had to invent land in the kernel — each in its smallest form, none as a new artifact class or skill — so the next campaign gets them for free: the verbatim owner record, the round-trip during shaping, the design loop pressed against model and wire, adversarial review of shaping artifacts against the owner's words, numbers-as-hypotheses, and an explicit artifact-hierarchy winner. Plus the blessing that long shaping is legitimate while evidence increments run through it.

**How I'll know it works:** each adopted change cites verified primary evidence from the Odd campaign (not the session's self-report — a census pass confirms the claims first); the method-page edit passes its own law (substantial → independent review with receipt); the dev suite stays green; the always-read set stays inside budget. Prediction, measured at Odd's next shaping or review round: the campaign-invented disciplines appear without being invented — the owner record exists from round one, shaping artifacts get their refute pass before the owner reads them, and no cross-artifact contradiction reaches the owner unflagged.

**Result (2026-08-17):** all six landed after the census pass verified the campaign's claims against primary artifacts (nine of ten confirmed — the SC register fired five times, not the reported four; one PARTLY: four superseded HTML decks carry no supersession marker, which shaped the hierarchy rule's wording). The census also found what the retrospective missed: the campaign's every adversarial pass was receiptless (the receipt law's scope hole — now closed), the product.md pass left its 30+ findings only in a commit message, and the closing audit's six-lens merge was performed by the party under audit (recorded as a known tension, not mechanized). Changes: AGENTS.md (hierarchy winner + round-trip-in-shaping), shape-product (rules 10–11 + the evidence-increment blessing), independent-review (shaping-artifact reviews with the receipt law applying unchanged), attack playbook (numbers-are-hypotheses).

**Review receipt (the governor changed, so the full loop ran).** Reviewer: a fresh Codex CLI session (`codex exec --sandbox workspace-write`) on a clean clone, 2026-08-17; transcript kept in the session scratchpad (`review-feedback.log`); it executed `git show HEAD`, read the Odd campaign's primary artifacts, and ran `bash devsuite/run.sh --control` — 4 of 4 red, as required. Verdicts: two edits faithful (round-trip-in-shaping, drawings-pressed), three drifted, two overreaching; structure verdict *fighting*. Findings, all fixed in the closure pass: the hierarchy sentence read as documents-beat-evidence (now: documents-vs-documents product.md wins; documents-vs-evidence the evidence wins); the shaping review was unreachable (the skill's own description said "after proving" — now both forms are in the description, shape-product rule 11 dispatches it, and the reviewer must still execute, with probe/grep subjects); rule 10 gave the raw record authority over later work (now scoped: shaping reviews, later rounds supersede, settled outcomes live in product.md/decisions.md); the denominator scar was encoded backwards (now: no rendered denominator the wire hasn't delivered); the long-shaping blessing lacked a stopping rule (now: keep shaping while shaping-scale questions remain open — past them it's deferral in a lab coat); and this repo's own state.md carried three stale claims (fixed, read-set re-measured at 10.0 KB).
