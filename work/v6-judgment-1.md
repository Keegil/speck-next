# v6.0.0 combined Piece 9 and milestone judgment — line one

## Judge receipt

- **Judge:** independent Codex collaboration context `/root/v6_judge_one`, inherited current model, 2026-09-06. I contributed to no product role, implementation, check, tester run, or earlier judgment, and I opened no other judge's record.
- **Work under judgment:** exact product tree `c2912e738c57218d23b574e5684c3aad026d2c07`; Built boundary `fff561e7da52c4cec0e385b10b0ba7bc061a585b`; combined receipt `9e8145621e36bff0a5361c1447ff857caf4ddca5`; four fresh records committed together at `f8da2b2cc46da36a2e33dd05382a52e6ab737a12`; active-role returns at `e6111dedd1f4455b369dc9c762df410ba78be3bb`.
- **Identity check:** `fff561e` changes only `state.md` and contains the quoted literal **Built** line. `9e81456` changes only this piece record and precedes every tester record. The four named record files were added together at `f8da2b2` and are unchanged afterward. No product implementation commit follows `c2912e7`; later commits before this judgment change only state or review records. The four tester identities in their records match the four slots in the committed receipt and none is a named product-building carrier.
- **Evidence read:** `product.md`, `CONTRACT.md`, `map.md`, `state.md`, `decisions.md`, the committed receipt, and exactly the four named fresh records. I challenged those records before reading the combined Business, Experience, and Product returns.

## Decision

**SEND BACK TO BUILD.** Piece 9 is insufficient and the v6 milestone is insufficient. The role-shaped Pulse result is real, useful, and closed to further building. The release candidate nevertheless has two concrete shipped-product defects:

1. The npm release artifact carries internal development freight: 110 entries / 2,193,416 unpacked bytes, including 79 `work/`, review, development-suite, or source-state entries an adopter does not need (`work/v6-first-adoption.md`, lines 235–253).
2. A completed mid-product upgrade accepts an empty assessment record, a duplicate Product section, or four roles with one carrier, then tells the builder to resume substantial work (`work/v6-worst-day.md`, lines 82–114).

The first native Codex admission is also **not judged**: its only authorized turn failed before inference because Codex CLI 0.147.0 could not start the configured `gpt-6-astra`. No Speck file was read, so this is an evidence gap, not proof of a Speck-byte defect (`work/v6-first-adoption.md`, lines 129–152).

Business's binding result agrees with that boundary: Piece 9's product/value direction is `kept`; the v6 milestone is `broken` until the two defects are repaired and a compatible Codex start succeeds. Strength elsewhere cannot compensate for these failures.

## Challenges to the four verdicts

### First-time Codex adoption

The favorable installation claims survive challenge. The exact tarball installed, its marker and recomputed method digest matched, five canonical skills were byte-identical, the relative Codex adapter stayed inside the subject, and the pre-existing owner skill survived byte-for-byte (`work/v6-first-adoption.md`, lines 91–127 and 163–229).

The unfavorable package finding also survives. It is not a confidentiality ruling—the tester inspected distribution paths, not every value—but it is a direct release-scope defect. The shipped tarball contains the kernel's internal work and test corpus rather than only what the CLI needs. The required native Codex conversation did not run, but the failure happened before Speck discovery; it cannot be charged to the installed method and cannot be called green.

### Fresh Codex product builder

The favorable product verdict survives narrowly. On paired mixed, complete, and empty journals, the governed weekly result showed explicit dated gaps, preserved legacy output, rejected invalid input without mutation, and kept the journal bytes unchanged (`work/v6-product-builder.md`, lines 66–216). The 28 clean/mutant dispositions kept v0.9 `reviewable / failed / closed`, rejected incomplete cheap work, and kept every exhausted authorization boundary closed (`work/v6-product-builder.md`, lines 218–256).

That proves a materially better tested result and the correct next decision—fresh review, not another Pulse build. It does not prove that four roles are universally necessary or cheapest. The tester's free attack also showed that the repo-side disposition helper accepts a truthy method complaint as a claimed product finding (`work/v6-product-builder.md`, lines 275–286). Because that helper is not a shipped builder input and the installed rule still requires provenance from fresh product review, this is an instrument limit, not authorization to rebuild Pulse or redesign the role system.

### Claude second host

Claude support is kept within the measured scope. The exact package installed into a separate repository; the marker, digest, five skills, and shared-body adapter matched; one invocation from two directories below root found `/shape-product`, named the correct fresh state, asked for the missing outcome, and made no product change (`work/v6-second-host.md`, lines 14–102 and 121–168). This proves first-action discovery on Claude, not a complete Claude build or every filesystem layout. The dense upgrade paragraph and the phrase “category fossils” are usability strain, not either release repair ordered here.

### Worst-day migration

The favorable migration behavior remains evidence: pending migration preserved the live piece, dirty unrelated bytes, and product history; missing canonical evidence refused atomically; a valid repaired record resumed the existing piece; and the settled route was byte-stable on repeat (`work/v6-worst-day.md`, lines 31–80).

The unfavorable admission finding is decisive. Three real-CLI reproductions isolate one class: the upgrader checks that the record path exists but does not check the record it claims proves separation. Empty evidence, duplicate Product, and a shared carrier all reach the affirmative resume instruction. Stable before/after hashes rule out transaction residue. This directly breaks the active-role and seamless-migration promises.

## Promise rulings

| Promise | Ruling | Evidence |
|---|---|---|
| 1. Iterative to the bone | **Broken** | The result-first/no-duplicate-build disposition works, but the migration path admits four role labels from one carrier and resumes substantial work. |
| 2. Small changes stay small | **Kept on the carried regression evidence** | The candidate's routing population stayed green and the installed split still exempts small work; none of the fresh records found a contrary product run. |
| 3. Judged like a user, challenged like a claim | **Kept** | Four contributor-excluded records exposed both favorable value and release blockers; neither cost nor Product's conclusion erased them. |
| 4. The state file tells the truth | **Broken on the upgrade path** | The CLI can bless fabricated role evidence and issue a resume instruction, so the migrated state can overclaim the assessment that permits work to continue. |
| 5. Upgrading is seamless and honest | **Broken** | Byte preservation and atomic refusal work, but record admission is false-green for the assessment that v6 requires before resume. |
| 6. Fun to drive, in plain language | **Broken for this milestone** | Fresh starts were mostly understandable, but the owner's ten-hour development experience remains failed. His later release direction does not rewrite that grade. |
| 7. Small by law | **Broken for the release artifact** | Installed limits are green at 20 entries / 85,618 bytes, five skills, and 47,374 always-read bytes, yet the tarball ships 79 internal artifacts and 2.19 MB unpacked. |
| 8. It cannot quietly grow back | **Kept narrowly** | Roles produced different product actions, Product integrated one decision, v0.9 remains failed, and no method/cost complaint reopened Pulse. The helper's provenance limitation stays explicit and earns no build. |

## Required rulings

- **Works:** **broken.** Pulse, fresh install, Claude discovery, and valid migration paths work; the real upgrader's assessment gate does not.
- **Delivers the promise:** **broken.** A release that can certify one carrier as four roles does not deliver genuine separation or honest migration.
- **Good to use:** **broken.** The calm Pulse result and clear valid migration cannot offset a worst-day false assurance or a release artifact carrying internal freight.
- **Quality hangs together:** **broken.** Two release-boundary defects remain; strengths on other surfaces do not excuse them.
- **Piece 9 sufficiency:** **insufficient; return to Build.** Packaging v6.0.0 is part of this exact candidate, and the combined fresh review found a concrete product reason for one repair batch.
- **Milestone sufficiency:** **insufficient.** Business is `broken`, the four quality rulings do not stand, and owner grading cannot begin.
- **Business:** **Piece 9 `kept`; milestone `broken`.** The material product result, selective actions, zero recurring service burden, and no-duplicate-build value stand. The ten-hour path, the 266,484-gross stop, the 82,138-gross / 49,370-fresh contribution failure, and v0.9's 850,035-gross / 232,435-fresh result remain failed. None is retroactively rescued.
- **Migration honesty:** **broken.** Preservation is strong; assessment admission is false.
- **Owner-attention cost:** **broken for the lived development milestone; prospectively unresolved in the waiting real product.** The repair receives zero owner interruption and no new method experiment.
- **Codex-first compatibility:** **not judged yet.** Filesystem discovery held, but the only fresh native start failed before Speck loaded. A compatible fresh start is required.
- **Claude support:** **kept within the claimed admission scope.** One separate install and nested native start succeeded against the exact method digest.
- **Distinct role actions:** **kept.** Business removed recurring attention, payment machinery, and unsupported value claims; Experience required neutral explicit gaps; Engineering found the additive seam and date boundary; Product chose and committed the integrated behavior before Engineering changed Pulse. The fresh product-builder observed the resulting difference.
- **Package scope:** **broken.** The runtime package contains internal source-project freight.
- **Structure:** **straining, not fighting.** The product remains usable, but the install is exactly 20 / 20 entries, always-read has only 2,626 bytes of headroom, two fresh readers felt the upgrade text's density, and package scope was uncontrolled. This does not reopen Pulse or the role design.
- **Repair authorization:** **yes, for exactly the two product defects above.** The Codex gap authorizes a run, not code. The disposition-helper limitation, historic cost failures, language blemishes, and method discomfort authorize no Pulse, controller, role-design, or broad kernel work.

## Exact Build repair

One Engineering batch, with Business and Experience returning on its first informative runs:

1. **Narrow the npm artifact.** Add a package allowlist that contains only the runtime needed by the published CLI: `bin/`, `AGENTS.md`, `CLAUDE.md`, `.claude/skills/`, `templates/`, and npm's required package metadata/readme/license. Do not change the installed method body, add a file, or raise a limit.
2. **Fail closed on the assessment record.** Before any target byte changes or affirmative resume output, make `bin/speck-next.js` validate the named `work/product-team-assessment.md`: exactly one Product, Business, Experience, and Engineering contribution; for every role, one non-empty carrier, direct evidence, conclusion, assumptions, proposed change, and active decision; four distinct carriers; exactly one Product synthesis; and exactly one Route. A missing, duplicate, blank, or same-carrier value refuses, names the defect and repair, says nothing changed, and prints no Shape, Map, or resume instruction. Keep owner-authored bytes opaque and preserve every current atomicity and line-ending guarantee.

## Complete focused rerun

1. Reproduce both pre-fix controls against `c2912e7`: the 110-entry / 2,193,416-byte package with its 79 internal entries, and all three real-CLI false greens—empty assessment, duplicate Product, and same carrier across four roles.
2. On the repaired tree, run the complete package population: required runtime paths present and byte-identical; `work/`, `docs/reviews/`, `devsuite/`, source `product.md`, `map.md`, `state.md`, and `decisions.md` absent; packed install succeeds; version, digest, five skills, relative adapter, 20-entry installed limit, and owner-skill preservation hold.
3. Run the assessment-record population, not only the three examples: missing and duplicate roles; every required field missing, blank, or duplicate; repeated carrier identities; missing or duplicate Product synthesis and Route; the valid four-distinct-carrier clean twin; pending, repair/resume, repeat/idempotence, dirty unrelated bytes, line endings, rollback, and atomic refusal. Every invalid case must leave the complete repository snapshot unchanged and must not print a resume instruction.
4. Rerun the existing 26 routing, eight upgrade-assessment, 28 result-disposition, 95 path-transaction, and 230 migration/refusal subjects; all five planted controls; host parser and syntax checks; exact version/install/always-read/skill budgets; `git diff --check`; and the positive-control-backed public-detail scan.
5. Append a compatible first-adopter continuation to `work/v6-first-adoption.md`: pack and install the repaired candidate into a new Git repository with an owner skill, start once from a nested directory on a Codex CLI/model combination that can actually infer, use no path hint or manual method read, and require native discovery of the five workflows, the correct first action, owner-skill coexistence, and no product mutation. One invocation, 180 seconds, no retry or fallback.
6. Append the full invalid-plus-valid migration continuation to `work/v6-worst-day.md`. Each returning tester also performs one free skeptical attack against the repaired class and reports it whether it finds anything or not.
7. Preserve the Pulse product-builder and Claude records if the installed method digest and disposition code remain byte-identical; otherwise rerun the affected scenario. Record Business and Experience returns. Then write a new state-only **Built** line for the repaired product files, open a new committed receipt, and continue both independent judgments in the existing judgment files.

No tag, publication, downstream upgrade, or owner felt grade is permitted until Business rules the milestone `kept` and both judges find the repaired exact tree sufficient.

## Focused continuation — repaired candidate `ca33c64`

The original send-back above remains the correct judgment on `c2912e7`. Its two product failures, the unsuccessful Codex admission, every cost failure, and the owner's ten-hour development experience remain in the record as failures. This continuation judges only whether the exact repair reversed those controls without weakening the product.

### Continuation receipt and identity

- **Product:** `ca33c64f613ae0004c8dd1d9809dbf26ed9c172b`, changing exactly `package.json`, `bin/speck-next.js`, and `devsuite/tasks/separated-product-team/check.py`.
- **Built:** `e3f5b559ff29fe12d54f7ed72951e9c626be5b7c`, a state-only commit with a literal **Built** line naming the repaired files and checks.
- **Receipt:** `5c214fa3dd4654fecea1bf944242710df2c544aa`, committed after Built and before either continuation.
- **Fresh continuations:** the original first-adopter and worst-day testers appended to their existing records at `4e637a5dd2a80e33e21dfdd55ab3bbd5b2008cbb`. No product file changed. Active-role returns followed at `8bf5c7680a62532680178b29197af3348ebd64bb`.
- **Preserved evidence:** the installed method digest is still `1ccba108dcc2eb6f1e15812e53e8e243ee09ee586d69f7b45ac7dad472872d70`, and the 28 disposition behaviors did not change. The earlier Pulse product-builder and Claude records therefore remain evidence about identical subjects.

I inspected the repair diff and challenged both fresh continuations before reading the active-role returns. I also reran the complete deterministic population myself without a model: `PYTHONDONTWRITEBYTECODE=1 python3 devsuite/tasks/separated-product-team/check.py --piece-8-controls .` exited `0`, reporting 26 routing, eight assessment, 28 result-disposition, 95 path-transaction, and 335 migration/refusal subjects with `Piece 8 controls: PASS`. `npm pack --dry-run --json` independently returned exactly 20 entries / 191,381 unpacked bytes and only the permitted runtime paths. `git show --check ca33c64` reported no whitespace error in the repair commit.

### Challenge: package and Codex admission

The original package control remains red above: 110 entries / 2,193,416 unpacked bytes, including 79 internal artifacts. The repaired continuation packed 20 entries / 191,381 bytes and found zero `work`, docs, development-suite, examples, source product/state/map/decision, contract, or capability records. It installed that tarball into a fresh Git repository, retained the exact 20 installed entries and method digest, preserved the owner skill byte-for-byte, and found no unexpected entry in an explicit allowlist attack (`work/v6-first-adoption.md`, lines 318–383 and 425–545). The package defect is closed.

The compatible Codex condition is now observed rather than inferred. From a nested directory, Codex CLI 0.147.0 with explicit `gpt-5.6-sol` completed one invocation in about 85.4 seconds, named all five workflows, selected Shape from the absent product record, used the pre-existing owner skill, asked one outcome-level question, and changed no product file (`work/v6-first-adoption.md`, lines 385–423 and 495–520). Its brief search into the adjacent disposable pack source corrected to the adopter's Git root before deciding; the later containment attack found zero path escapes. The rejected setup wrapper before process creation started no command, product operation, or model invocation, so it does not turn the one successful host invocation into a retry. The old Astra/default failure remains failed; this run proves a compatible Codex pairing, not every model/CLI mismatch.

The 260,556 input plus 3,306 output tokens and 85-second orientation are expensive for a small question. That remains Business cost evidence. It does not erase the correct first-use result and does not authorize another build.

### Challenge: assessment admission and migration honesty

The three original false greens remain the controls. On `ca33c64`, heading-only, duplicate Product, and one-carrier-four-role records all exited `1`, preserved whole-repository snapshots including dirty unrelated bytes, and printed no Shape, Map, or resume instruction. The missing-record and duplicate-record-line siblings remained atomic. The free attack hid a shared carrier behind whitespace, U+200B, and U+2060; normalization still found the collision and refused without a write (`work/v6-worst-day.md`, lines 143–191).

The valid clean twin retained one contribution for each of four distinct carriers, one Product synthesis, and the exact live-piece Route. It resumed Feature Alpha without reopening Shape or Map, inventing backfill, altering history, or touching the dirty note; the second run was byte-identical (`work/v6-worst-day.md`, lines 193–216). My deterministic replay additionally exercised every role and required field as missing, blank, and duplicate; shared-carrier variants; missing, blank, duplicate, and mismatched synthesis/Route; inert Markdown containers; CRLF and lone-CR records; pending state; rollback; and valid repeat. The assessment defect is closed without weakening migration.

### Active returns

Only after those challenges did I read the focused returns. Business rules Piece 9 `kept` and the v6 milestone `kept`; it preserves the ten-hour path and v0.9 as failed, calls the 85-second orientation expensive, and authorizes no further method or product change. Experience likewise preserves the failed development experience while finding the repaired package, Codex start, and migration good enough to release. Product authorizes no further build absent a new concrete product defect or claim. The direct evidence supports all three conclusions.

### Final rulings on the repaired tree

- **Works: kept.** Real package/install/Codex use succeeded, the preserved Pulse product still runs, valid migration resumes the right live piece, and invalid migration evidence now fails before writes.
- **Delivers the promise: kept.** The repaired candidate delivers genuine role separation, result-first/no-duplicate-build behavior, clean distribution, honest migration, and native operation on Codex with preserved Claude support.
- **Good to use: kept.** The first adopter received one understandable next action and retained his own skill; the worst-day owner received bounded pending state, precise refusals, and an idempotent resume without owner repair.
- **Quality hangs together: kept.** Both prior release-boundary weaknesses reverse on their original controls, the valid twins remain intact, and the free attacks found no sibling bypass. Strength elsewhere is not being used to excuse an open defect.
- **Piece 9:** **SUFFICIENT.** Its material product result, separate actions, active returns, cost truth, closed further spend, package, and release paths now stand together. It may become Judged.
- **v6 milestone:** **SUFFICIENT from this judgment line.** Business's binding milestone ruling is `kept`. The milestone still becomes Live only after the second independent judgment agrees and the owner supplies the required felt grade.
- **Migration honesty: kept.** The candidate carries history and dirty work, refuses incomplete or ambiguous completed assessments atomically, and resumes only the route the evidence supports.
- **Owner-attention cost:** the lived ten-hour development path remains **failed** and must stay visible. The focused repair required zero owner interruption; the 85-second Codex orientation remains expensive. These are cost facts, not a reason to repeat product work.
- **Codex-first compatibility: kept on the supported pairing actually run.** Native nested discovery, all five workflows, correct start, owner-skill coexistence, and no mutation passed on Codex 0.147.0 with `gpt-5.6-sol`.
- **Claude support: kept on preserved evidence.** The repaired package changes no installed method byte, and the prior separate-repository nested Claude start exercised this exact digest successfully.
- **Distinct role actions: kept.** Business removed recurring attention, payment machinery, and unsupported value; Experience required neutral explicit gaps; Engineering found and preserved the additive seam; Product integrated the choice before Engineering built. The repairs did not turn those actions into copied labels.
- **Structure: straining, not fighting.** The product works honestly, but it remains at 20 / 20 installed entries and 48,369 / 50,000 always-read bytes. That strain stays routed to the already named density and limit work; it does not justify reopening this result or adding method work before release.
- **Another build:** **not authorized.** No fresh record, skeptical attack, active return, or independent replay identifies a remaining concrete product defect or new product claim. Cost and method strain alone cannot reopen Build.

### Record correction before landing

One state number is stale, but it is not a product-build finding. The repaired Built line records 48,369 / 50,000 always-read bytes while `state.md` still says 2,626 bytes of headroom—the old candidate's figure. `node -e 'console.log(50000 - 48369)'` returned `1631`. Correct that line to **1,631 bytes** in the normal record-only landing update. It needs no new Built line, tester run, or judgment.
