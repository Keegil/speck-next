RULING: DO NOT LAND v5.0.0 — the hearing model is worth keeping, but this build can mis-land rejected work and its governing, contractual, public, and release surfaces do not yet say one thing.

# Hearing on v5 — the hearing

**Subject ruled:** the kernel surface at `586a87a943dde20989142d11973b09f0f4df2b35`. The current commit, `b4b3a0dce543c6101d2fd260ca44bc7fff350633`, adds the two records and opens this judgment; it does not alter the surface they lived.

**Judge boundary:** I built none of this and walked none of it. I heard the two records, challenged them, and traced their cited source text. I did not run the kernel or gather a third experience.

## 1. Hear and grounding

### The newcomer

The verdict has four claims, all grounded:

- The normal-piece path is usable. It points to the one-pass dispatcher and loop read, then the experiencer's reconstructed drive (`docs/reviews/v5-experiencer-newcomer.md:23-26`, `52-64`).
- The hearing's two stances and outputs are understandable. It points to the experience and judge reads (`docs/reviews/v5-experiencer-newcomer.md:78-93`, `107-120`).
- Two fresh conductors can diverge on transitions, destinations, and follow-up. It points to the missing sixth step, unnamed `Built` write, unconditional landing, unnamed grade/output destinations, and non-resumable-session gap (`docs/reviews/v5-experiencer-newcomer.md:36-48`, `115-120`, `260-264`).
- README's release status cannot be trusted alone. It points to the observed `At v4` text and the CLI output advertising `v5.0.0` (`docs/reviews/v5-experiencer-newcomer.md:143-146`, `152-168`, `262`).

**Grounding ruling:** no claim is struck. Each points at a read or run actually recorded. The favorable real-build claim is predictive, not runtime proof; that limits its weight but does not make it ungrounded, because this persona was experiencing the kernel as an instruction surface.

### The worst day

The split verdict is grounded:

- The installed governing surface is coherent on the v5 stance. It points to the source and installed stale-stance sweeps, the four-file hearing read, the reference-resolution probe, and the successful install/upgrade run (`docs/reviews/v5-experiencer-worstday.md:31-65`, `67-99`, `220-294`, `296-472`).
- The whole published surface is inconsistent. It points to the contracted persona being narrower than the governing persona, README omitting the second milestone judge, and README saying v4 while the executable says v5 (`docs/reviews/v5-experiencer-worstday.md:101-137`, `165-218`, `483-489`).

**Grounding ruling:** no claim is struck. Each claim points at an executed probe. The positive upgrade claim is limited to a fresh v5 repository and the specifically retired `independent-review` path; the record expressly says it did not test a real historical v4 repository (`docs/reviews/v5-experiencer-worstday.md:474-479`).

## 2. Challenge

### Questions to the newcomer

**Q1. You say you could carry a normal piece through a real hearing. Did you actually do that?**

**Answer from the record:** No. The experiencer did not drive a product piece, dispatch or resume real experiencers or judges, run a UI, run shape or map, or run upgrade (`docs/reviews/v5-experiencer-newcomer.md:245-252`). The claim therefore supports cold-read actionability only. It cannot support a ruling that the full path works in operation.

**Q2. Does the canonical piece template cure the alleged receipt and judgment destination gap?**

**Answer after tracing the cited surface:** Partly. `templates/piece.md:11-13` places both the hearing receipt and judgment in the piece work file, so the record's page-alone stumble must not be promoted into a claim that the whole installed surface lacks those destinations. It does not cure the missing destination for a milestone owner grade, and it does not say how a non-resumable experiencer's follow-up keeps its lineage. Those findings stand (`docs/reviews/v5-experiencer-newcomer.md:42-46`, `115-120`).

**Q3. Is unconditional “Land it” merely wording a competent agent will reconcile?**

**Answer from the record:** No. The experiencer did reconcile it, but had to invent the condition: its simulated step 9 says not to mark a failed piece done, while step 10 says the conditional is the experiencer's own reconciliation rather than wording the page provides (`docs/reviews/v5-experiencer-newcomer.md:62-64`). A kernel promise cannot rely on every fresh conductor making the same repair.

**Q4. Is the missing `Built` event cosmetic?**

**Answer from the record:** No. The page declares four states, calls the piece loop six steps, prints five, and moves from build directly into hearing without naming when `Built` is recorded (`docs/reviews/v5-experiencer-newcomer.md:36-40`, `58-61`). That is a state-truth defect at the exact transition the kernel governs.

**Q5. Does the record prove the judge-ordered follow-up works on the host-neutral path?**

**Answer from the record:** No. The judge skill says to return “that experiencer,” but the record did not run a follow-up and found no fallback for a one-shot session that cannot resume (`docs/reviews/v5-experiencer-newcomer.md:115-120`, `247-248`). This remains unjudged in behavior and defective as an instruction.

### Questions to the worst day

**Q1. Does a clean stale-phrase sweep prove the installed kernel can run a real hearing?**

**Answer from the record:** No. It proves that the named dead v4 phrases were absent and that the four hearing files can be read coherently. The experiencer did not build a downstream product, act as judge, or perform a judge-ordered follow-up (`docs/reviews/v5-experiencer-worstday.md:474-479`). The favorable verdict is true of the installed instruction surface, not yet of the full runtime process.

**Q2. Does the upgrade probe support the product's seamless-upgrade promise?**

**Answer from the record:** No. It supports fresh-v5 install, same-version upgrade, exact 19-file counting, and deliberate deletion of the retired skill path (`docs/reviews/v5-experiencer-worstday.md:346-472`). It did not exercise an actual historical v4 repository with live work (`docs/reviews/v5-experiencer-worstday.md:478`). Seamless historical upgrade remains outside this hearing.

**Q3. Is README simply stale, with the executable therefore authoritative?**

**Answer after tracing the standing release ruling:** The record proves inconsistency, not that README alone is wrong. At the subject commit, README said v4 while the package and CLI advertised v5 (`docs/reviews/v5-experiencer-worstday.md:198-218`), but `v5.0.0` is still the tag this hearing is being asked to permit. The release identity was exposed before the release judgment. The defect is non-atomic release truth across README, package, CLI, state, and tag; changing only README would merely choose a different premature claim.

**Q4. Can the persona and second-judge drifts be dismissed as outer-doc polish because AGENTS.md is clean?**

**Answer from the record:** No. The narrower persona can omit a same-install cross-user leakage probe, and the omitted second judge can remove an independent milestone ruling (`docs/reviews/v5-experiencer-worstday.md:120-137`, `165-196`, `487`). These alter behavior promised by the product.

### Re-run decision

No pre-ruling re-run is ordered. The grounded defects already decide whether this build may land; another run against the same text cannot rescue it. The two exact post-fix runs required for a new ruling are ordered under Close.

## 3. Hold the divergence

The verdicts are true of different things and remain side by side:

- **The newcomer's verdict is true of cold operational guidance.** A fresh agent can reconstruct the normal path and begin, but must invent state and landing semantics at the failure boundary (`docs/reviews/v5-experiencer-newcomer.md:52-64`, `254-264`).
- **The worst-day verdict is true of installed coherence versus published coherence.** The installed governing files consistently express the fused hearing, while contract and README can cause a different persona set, one judge instead of two at a milestone, and a false read of release status (`docs/reviews/v5-experiencer-worstday.md:483-489`).

There is no middle verdict to average into. “An agent can start” does not cancel “two agents can drive differently,” and “the installed governor is coherent” does not cancel “the product's authoritative and public surfaces prescribe different behavior.”

## 4. Rule

### The Outcome promises in `work/v5-hearing.md`

1. **Experiencers judge as users and return verdicts from lived records — KEPT.** Both records end in persona verdicts and cite their own reads and runs (`docs/reviews/v5-experiencer-newcomer.md:254-264`; `docs/reviews/v5-experiencer-worstday.md:481-489`). Both hearing-file reads found the experiencer and judge stances compatible (`docs/reviews/v5-experiencer-newcomer.md:78-93`, `107-113`; `docs/reviews/v5-experiencer-worstday.md:67-99`).
2. **Every verdict claim is grounded or void — KEPT in this bootstrap.** Every claim used above points to a recorded moment; none was struck. Challenge narrowed two favorable claims to what their runs actually establish.
3. **A separate non-builder, non-walker judge hears and challenges the verdicts — KEPT in this bootstrap.** This judgment does that, and the records show the installed role boundary without contradiction (`docs/reviews/v5-experiencer-newcomer.md:107-113`; `docs/reviews/v5-experiencer-worstday.md:86-99`).
4. **The judge closes evidence gaps by sending an experiencer back — NOT JUDGED in operation; BROKEN as host-neutral instruction.** Neither record ran the path, and the newcomer found no rule for the recommended one-shot session becoming unavailable (`docs/reviews/v5-experiencer-newcomer.md:115-120`, `247-248`).
5. **Diverging persona verdicts are held without averaging — KEPT in this judgment.** The newcomer rules on fresh-conductor actionability; the worst day rules separately on installed and published surfaces. Their scopes are preserved above.
6. **The judge rules the four from cited moments and routes insufficiency — KEPT in this judgment.** The four rulings and defect routes below cite the records instead of replacing them with a summary.
7. **A second judge hears milestones and risky pieces — BROKEN across the published surface; NOT JUDGED in operation.** AGENTS.md and the contract carry it, but README's milestone account omits it entirely (`docs/reviews/v5-experiencer-worstday.md:165-196`). This piece hearing is not itself the completed milestone hearing, so absence of a second judge here is not the defect.
8. **v4's verdict ban and paper-only judge die — KEPT on the installed surface.** Both source and installed payloads returned no named stale stance, and the four hearing files gave compatible roles (`docs/reviews/v5-experiencer-worstday.md:31-99`).
9. **Receipts, personas, and never-the-builder carry over — BROKEN as a whole-surface promise.** Receipts and never-the-builder are present, but CONTRACT.md and one README passage shrink “second user” to least-privileged user and can omit the second person's same-install trace (`docs/reviews/v5-experiencer-worstday.md:101-137`).
10. **The bootstrap stops arm's-length blindness — NOT JUDGED YET.** This hearing challenged and narrowed claims, but it needed no new product observation. The dispatch-back path that closes the distance has not run (`docs/reviews/v5-experiencer-newcomer.md:247-248`; `docs/reviews/v5-experiencer-worstday.md:477-479`). Odd's build also remains future evidence by the piece's own success test.

### The four

- **Works — BROKEN.** The central hearing can be conducted, but the governing loop says “six steps,” gives five, never lands `Built`, and unconditionally marks the piece done after a hearing that may reject it (`docs/reviews/v5-experiencer-newcomer.md:36-44`, `58-64`). A failed piece can therefore advance under the literal governor.
- **Delivers the promise — BROKEN.** The challenge and divergence model is working in this bootstrap, but judge-ordered return is not executable host-neutrally, the broader second-user persona is not conserved across the contract, and the public milestone path omits the second judge (`docs/reviews/v5-experiencer-newcomer.md:115-120`; `docs/reviews/v5-experiencer-worstday.md:120-137`, `165-196`).
- **Good to use — BROKEN.** The newcomer could proceed, but only after inventing when `Built` lands, when landing is allowed, where the owner grade lives, and how an unavailable experiencer returns (`docs/reviews/v5-experiencer-newcomer.md:254-264`). The product promises two fresh agents should not have to make up different kernel behavior.
- **Quality hangs together — BROKEN.** Governing, contractual, public, and executable version surfaces disagree on behavior and release truth (`docs/reviews/v5-experiencer-worstday.md:101-137`, `165-218`; `docs/reviews/v5-experiencer-newcomer.md:141-146`).

This piece is **not proven**.

### Structure

**FIGHTING — specifically at two seams.** The state ladder requires `Built` and forbids advancement after an insufficient judgment, while the build loop omits the `Built` event and says to land unconditionally. Separately, the same hearing is duplicated across AGENTS.md, CONTRACT.md, README.md, skills, and templates, and those copies now prescribe different personas and milestone judging. The hearing model itself is sound; its distribution and transition structure are fighting it.

Per the structural rule, this routes back into the current piece as structural build work. It does not open the next mapped product piece.

### The whole against the contract

- **Product excellence — BROKEN:** correctness and internal coherence fail at the transition and surface seams.
- **Owner attention and peace — BROKEN:** a reader must reconcile five-versus-six steps and v4-versus-v5 release truth.
- **Iterative with the owner — NOT JUDGED HERE:** neither record ran an owner steering loop.
- **Care where the risk is — BROKEN:** shrinking the second-user persona can remove the cross-user trace probe.
- **Smallness — KEPT in this evidence:** the fresh install measured 19 files and all named references resolved (`docs/reviews/v5-experiencer-worstday.md:220-278`, `346-377`).
- **Seamless upgrades — NOT JUDGED HERE:** only fresh v5 and same-version upgrade were run; no historical repository with work in flight was tested (`docs/reviews/v5-experiencer-worstday.md:474-479`).
- **Truth over theater — BROKEN:** the executable advertised a v5 pin before this hearing permitted the v5 tag, while README still said v4.
- **Fun and understandability — BROKEN:** the newcomer recorded load-bearing stumbles in the one-pass governing read and owner page (`docs/reviews/v5-experiencer-newcomer.md:28-48`, `141-146`).

## 5. Route every grounded defect

All defects below are mis-built kernel or publication surfaces. **Re-enter build** for `v5 the hearing`; neither the shaped outcome nor the mapped piece boundary needs reopening.

### Breaks a promise — fix first

1. **A rejected piece can be landed.** Change `AGENTS.md`: make the piece loop's count true; name the `Built` state event; make landing conditional on sufficient judgment; say what happens to map and state on backward routing; reconcile the one-unjudged-piece allowance with the listed order. This breaks state truth and product correctness. Grounds: `docs/reviews/v5-experiencer-newcomer.md:36-44`, `58-64`.
2. **The safety persona shrank in authoritative and public copy.** Change `CONTRACT.md:19` and `README.md:28` to conserve the complete second-user persona: least privilege plus a second person on the same install looking for traces. Grounds: `docs/reviews/v5-experiencer-worstday.md:101-137`.
3. **README omits the mandatory second judge at milestones and risky work.** Change `README.md:20` and the hearing explanation at `README.md:28` so the owner-facing path carries the standing ruling. Grounds: `docs/reviews/v5-experiencer-worstday.md:165-196`.
4. **Judge-ordered follow-up has no host-neutral unavailable-session path.** Change `.claude/skills/judge/SKILL.md`, `.claude/skills/experience/SKILL.md`, and `templates/piece.md` to say whether the original session resumes or a fresh context inherits the same persona, record, exact scenario, and receipt lineage; say how the returned observation rejoins the hearing. Grounds: `docs/reviews/v5-experiencer-newcomer.md:115-120`, `247-248`.
5. **Release truth is non-atomic.** Reconcile `README.md`, `package.json`, `bin/speck-next.js`, `state.md`, and the tag point so no surface advertises a released pin before it exists and all surfaces name v5 together when it lands. Grounds: `docs/reviews/v5-experiencer-newcomer.md:143-168`; `docs/reviews/v5-experiencer-worstday.md:198-218`.
6. **The owner page overclaims its own simplicity.** Change `README.md:24-26` so “five files” is honest about `work/` being a collection and “only words” means the four state labels, not every concept the page requires. Keep the plain status paragraph from collapsing into project-history vocabulary. This breaks the explicit fresh-reader promise, even though it does not block execution. Grounds: `docs/reviews/v5-experiencer-newcomer.md:141-146`.

### Real defects

7. **The owner grade and hearing outputs do not have one named home at every scope.** `templates/piece.md` already locates piece receipts and judgments; change `AGENTS.md`, `.claude/skills/judge/SKILL.md`, and the applicable template so piece rulings, milestone rulings, follow-ups, and the owner's felt grade each have one destination, with `state.md` remaining derived truth. Grounds: `docs/reviews/v5-experiencer-newcomer.md:42-48`, `61-64`, `115-120`.
8. **The UI experiencer's three verdicts have no explicit mapping to the judge's four.** Change `.claude/skills/experience/references/walk.md` or `.claude/skills/judge/SKILL.md` to state that the three are persona testimony and do not satisfy or replace any of the four governing rulings. Grounds: `docs/reviews/v5-experiencer-newcomer.md:93`.
9. **Piece and milestone hearings are distinguishable only after a careful read.** Tighten `AGENTS.md:31-33` so the piece hearing's selected personas and the milestone hearing's four personas plus second judge are unmistakable on first contact. Grounds: `docs/reviews/v5-experiencer-newcomer.md:44-48`.

### Polish

10. **Cold-start status wording is unspecified.** In `AGENTS.md:7`, permit “first look in this session” when no prior owner look is known. Grounds: `docs/reviews/v5-experiencer-newcomer.md:32-35`.
11. **Five phases versus one hearing causes an avoidable first-read pause.** Clarify in `AGENTS.md:3` that experience and judge remain two phases with one continuous hearing. Grounds: `docs/reviews/v5-experiencer-newcomer.md:32`.

## 6. Close

**May this piece land now? No.** Do not tag `v5.0.0` and do not upgrade the governed repository from this build.

Fix the promise-breaking items first, then the real defects. After the corrected installed surface exists, run these exact scenarios before a new judge rules:

**ORDER RE-RUN: failed-hearing transition.** In two separate fresh clones at the corrected commit, give two fresh conductor agents the same ratified product/map fixture with one live substantial piece already built. Supply an experiencer record with a grounded deal-breaker so the judge rules the piece insufficient and routes it to build. Each conductor must use only the installed kernel. Record whether both write `Built` before the hearing, preserve the piece as live after the failed ruling, do not mark it done or start the next piece, put the ruling and owner-facing state in the same named destinations, and then land only after a fixed build is re-experienced and re-judged sufficient.

**ORDER RE-RUN: unavailable experiencer follow-up.** In a fresh clone at the corrected commit, dispatch a one-shot experiencer whose record deliberately leaves one load-bearing question untested, then end that session. Have a separate judge order one exact product scenario whose answer is required. With the original experiencer unavailable, the conductor must use only the installed kernel to dispatch the permitted replacement, preserve the same persona and receipt lineage, append the new lived moment to the hearing, and return it to the same judge without the judge walking the product. Record every file destination and whether the gap closes without invented procedure.

Then a fresh judge must hear the corrected records and rule all four again. Tagging and the governed-repo upgrade may proceed only if that judgment permits landing.

What honestly stays open after this piece is repaired:

- Odd's real build remains the in-anger test of whether grounded hearings improve product work.
- The completed milestone still requires all four personas, two independent judges, and the owner's felt grade.
- Historical v4/old-Speck upgrade preservation was not tested by these records.
- Product UI, auth, network, persistence, shape, and map behavior were outside these two experiences.

