# Round 1 · Reviewer 1 — owner lens (Claude Opus, fresh context), as received

Target: CONTRACT.md v0 + fixtures.md v0. Instruction: refute through the owner/product lens. Findings F1–F15 (P0×4, P1×9, P2×2), abridged only where repetitive with the sibling reviews.

1. **F1 · P0 — Opponent mislabeled and frozen worse-than-current.** `58c8568` is the annotated tag object, not a commit; it dereferences to `c7303fb` — two table rows presenting one tree as two states. The manifest pre-declares four framework defects fixed-on-v11-later, so the successor would be scored against a v11 known to be inferior at decision time. Fix: one commit SHA; re-snapshot and re-baseline the opponent after the fixes land.
2. **F2 · P0 — Four of five field fixtures exist only on the owner's laptop.** Only odd's SHA has a remote; brightstance and speilet trees are dirty. An unreproducible fixture cannot be independently judged — the exact structure Promise 3 forbids. Fix: pushed tags or bundles before Lock 2; runs from `git worktree add <sha>`, never copies.
3. **F3 · P0 — Promise 1's acceptance measures the wrong thing, and it carries the owner's verbatim constraint.** "A runnable demo exists before any second planning document" — one 4,000-line plan defeats it; "exists" doesn't mean the owner saw it; one proxy on one task defends the whole constraint. Fix: cap minutes/tokens/files before first real-path execution; logged owner touchpoint (demo handed over); apply to T02/T03/T09 too.
4. **F4 · P0 — Trivial/substantive boundary undefined, self-assessed by the party that benefits, no trap in the benchmark.** Zero-artifact-minutes is the cheapest path, so agents classify down. Fix: ex-ante falsifiable definition; recorded classification; elevate-only; add the T15 trap (typo-shaped prompt on a planted privileged path).
5. **F5 · P1 — Promise 6 is false on the contract's own face.** "Zero methodology vocabulary" while `state.md` teaches Shaped/Built/Proven/Live — roughly a dozen concepts an owner must hold. The contract overclaims, which it calls a defect class. Fix: frozen owner vocabulary ≤ 5 words; measure comprehension (owner-proxy answers the five questions, no glossary).
6. **F6 · P1 — Promise 5 self-contradicts and is scoped past the customer base.** "Zero manual repair" vs "fails loudly with a report" — a loud failure is manual repair. Also: does conservative down-mapping silently bill a re-proof tax? Fix: total conversion with disclosed residue for real repos; state the evidence-import rule.
7. **F7 · P1 — Budgets bound files, not load.** 8 KB doctrine beside unbounded pulls replays v11 (small AGENTS.md, 70 skills behind it). No end-to-end unit-cost budget — the ~53k-token figure that motivates the project is unbudgeted. Fix: per-unit end-to-end ceilings in CI; capability budget = total reachable; paired rises.
8. **F8 · P1 — "≥ v11.2.0" has no measured baseline and the kernel is designed against the answer key.** Fix: baseline frozen at Lock 2 before any slice; ≥ 2 held-out classes planted by a non-designer; falsification keyed to held-out too.
9. **F9 · P1 — The independent evaluator is never required to run anything.** The 183-documents-vs-97-runtime failure, reproducible at 9 capabilities. Fix: bind decorrelation axes into the promise — different context/model · executes, not reads · judges against owner intent; "zero executed observations" is a named defect.
10. **F10 · P1 — Four states collapse the four axes the inherited doctrine forbids compensating.** "Proven" as one token restores implementation-green-hides-taste-failure. Fix: Proven carries one line per axis with evidence or "not judged".
11. **F11 · P1 — Three shared documents or four?** The exempt set is named differently in three files; if the packet is exempt, the per-unit budget bounds nothing. Fix: one identical exempt set everywhere; the packet counts.
12. **F12 · P1 — Round-trip-first is absolute where reality is irreversible, and rigor matching was deleted without replacement.** Fix: named-replica carve-out with disclosed fidelity gap; rigor packs get a register entry and an acceptance.
13. **F13 · P1 — The register omits what's most likely to be traded away:** owner attention/peace (the optimization target itself — "iterative-with-owner" pushes monotonically toward more contact), risk-proportionate rigor, axis separation, round-trip safety, durability. "Consequential" is self-assessed. Fix: add all five; define consequential by trigger.
14. **F14 · P2 — The re-bloat alarm fights the last war and the constitution has no bootstrap.** Fix: measured maintenance-share ratio with declared threshold; v0 mechanisms ship with predicted benefit and a measure-by date; name the release step.
15. **F15 · P2 — Falsification has no date, reader, or owner-experience trigger.** Fix: date + named check + a trigger keyed to owner attention per shipped increment.

**Verdict:** No — not as it stands, but the bones are sound and the P0s are fixable without redesign. The inversion is the thing to fix before code: the contract is most rigorous exactly where v11 already succeeded (honest state, small budgets) and vaguest where the owner said v11 failed him. Ship it as-is and the most likely outcome is a smaller, faster system that still shows him a plan before it shows him a running product.
