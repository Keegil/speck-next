# A separated product team

**Serves:** all eight contract promises · **Consumes:** [shaping.md](shaping.md), the owner's ratified v6 direction, the installed method and its field record

**Outcome:** Every substantial piece is shaped, mapped, and set up by four genuinely separate product-building contexts. Product integrates one product decision and never implements it; Engineering owns implementation. Materially affected roles return when the first real run can still change the work. Business can block business-changing work and milestones. Contributors never become their own fresh testers or judges. Existing products adopt this honestly before their next substantial piece.

**Before first run:** At most 180 minutes from this Shaped commit, 250,000 aggregate role and implementation tokens where hosts report them, and 100 distinct repository files read. Role work counts. If any limit is reached, stop elaborating and run the smallest honest fresh-install, migration, and cross-cutting fixture.

**Proof plan and review cost:** Run syntax and installer checks; fresh install; pre-v6 migration, second-run idempotence, custom-section preservation, missing-product honesty, unrelated-dirty-file preservation, and complete changed-path output. Measure installed files and bytes, always-read bytes, skills, owner vocabulary, elapsed time, tokens where reported, contexts, and owner interruptions. Run every development task with every control red. The new cross-cutting task must reject a fake same-context role record, prove four host-issued carriers and different evidence and assumptions, keep Product and Engineering separate, show the correct integrated decision before implementation, and catch a named role-specific miss when ungoverned. Run its governed path through both supported hosts. Then four fresh milestone testers cover first adoption, real building work, a second host/repository, and missing-context or mid-product migration; two independent judges challenge the same records and rule the four product qualities plus the business case, migration honesty, owner-attention cost, and whether the roles changed actions rather than labels. Run a case-insensitive public-detail leak scan over the full release diff. After any fix, re-run the whole named requirement against its pre-fix control plus one free skeptical attack.

## Pre-code product team

| Role | Carrier | Primary evidence | Conclusion | Assumptions | Proposed change | Active |
|---|---|---|---|---|---|---|
| Product | `/root` | Owner's verbatim shaping record and ratified promise; the eight contract promises; current map, state, installed surface, and recurring product misses | Use strong separation. Four headings in one context cannot supply the missing perspectives. | Product remains the owner's one interface; stable separate carriers may return while their evidence remains current; small changes stay exempt. | Put the four contexts at phase close and before product code, preserve consequential dissent, and integrate one resolved outcome before Engineering builds. | Yes — owns synthesis and records; does not implement. |
| Business | `/root/business_role` | Contract promises on value, owner attention, simplicity, portability, and bounded growth; installer, templates, development suite, and current budgets | Proceed if distinct decisions earn the extra context cost and Business has a binding ruling where value changes. | Business covers commercial viability or, for non-commercial products, adoption, operating cost, and durable value; role work counts inside the first-run limit; no new owner beat. | Add compact records, blocking `kept / broken / not judged` rulings, measured operating cost, honest migration, and a capability retirement condition. | Yes — method cost and durable value are affected. |
| Experience | `/root/experience_role` | Owner-control promise; method, phase skills, templates, installer, and the later fresh-testing contract | Proceed only if role machinery stays mostly invisible to the owner and cannot contaminate user testing. | Experience is a product-building responsibility, not a simulated user; agreement may be genuine; continuous involvement means key decisions and runs, not every message. | Give Product one owner-facing synthesis, keep one compact role table, require consequence plus disconfirming run for material-impact claims, and keep contributors out of later testing. | Yes — owner and builder comprehension are affected. |
| Engineering | `/root/engineering_role` | Installed paths, runtime, installer and upgrader behavior, all templates and skills, development-suite event formats, repository limits and version history | Feasible without a new installed file or skill. Proof of carrier identity and complete, idempotent migration are the hard parts. | A carrier is a host-issued session identity; pre-v6 means marker-bearing Speck Next repos, not predecessor-format repos; missing product files are reported, not invented. | Change the existing method surface and templates; append one unassessed product-team section on upgrade; report the full surface diff; validate carrier identities from structured host events. | Yes — owns implementation and first-run instrumentation. |

## Product synthesis

The roles agree on the product direction and disagree usefully about what could make it fail. Product resolves the design this way:

- The four roles run in distinct contexts once before Shape closes, once before Map closes, and once before code for every substantial piece. A current carrier may return; a heading or persona switch inside Product does not count.
- Each contribution records carrier, direct evidence, conclusion, assumptions, proposed change, and active decision. A role claiming material impact must name the consequence and the earliest run that could disprove it. Product cannot mark that concern inactive, but still resolves the product tradeoff.
- Product shows the owner one integrated recommendation. Dissent reaches the owner only when it changes a promise, user-facing choice, risk, or order. The work file preserves the rest as consequence, evidence, and settling run.
- Experience is the standing product-building responsibility. Lowercase experience remains the later fresh-user phase. An Experience contributor forms product hypotheses and never stands in for a user.
- Business rules `kept / broken / not judged` on changes to users, value, price or revenue, acquisition or adoption, ongoing cost, or durable value, and on every milestone. `broken` and `not judged` block the affected piece or milestone. This is conditional business evidence, not a fifth universal quality ruling.
- Active roles return at the first real run and record what the run showed, what changed or held, and the resulting product change. A consequential miss after a role marked its concern handled keeps that role continuously involved in the next piece; a repeat keeps it through the milestone until evidence supports relaxing it.
- Role contributors are ineligible as fresh testers and judges. The later review sees product promises and the running product, not the role conclusions.
- Existing marker-bearing repositories receive an honest unassessed product-team section exactly once. Historical work is untouched. Four separate contexts assess the product before the next substantial piece, reopening Shape only for a wrong promise.
- The implementation stays inside existing files and caps. It introduces no skill, installed file, state dashboard, owner sign-off beat, or new universal verdict.

### Preserved dissent

- **Field value remains unproven.** Kernel evidence can prove mechanics, portability, owner-attention cost, and whether the fixture changes a decision. It cannot prove durable value in a real product. `capabilities.md` must say so and name the next real milestone as the settling run.
- **Role records can become forms.** The consequence would be duplicate prose and slower first runs. The cross-cutting task and milestone testers settle this by checking for different actions, a builder-ready handoff, and one owner-facing recommendation.
- **Structured logs may not prove nested carrier identity equally on both hosts.** The consequence would be a green based on self-report. Engineering must normalize host-issued evidence per driver; a planted one-carrier record is the control.

## First real run returns

The first product run was the planted control, before any governed agent run:

```sh
./devsuite/run.sh --control separated-product-team
```

It exited 0 because the task went red. Re-running `python3 devsuite/tasks/separated-product-team/check.py /tmp/claude-501/devsuite-runs/run-1788199160/separated-product-team` rejected duplicate carriers, wrong role evidence, absent structured host dispatches, a missing pre-code commit boundary, the naive pressure-and-price behavior, and missing first-run returns. It also exposed one checker defect: a bare `Business ruling: kept` passed without evidence.

- **Product `/root`:** The run showed that the task distinguishes role-shaped decisions from four headings and that the correct product behavior is observable. The pre-code synthesis held. Product changed the implementation order: the unsupported Business-ruling green must be fixed before any governed run; no method promise changed.
- **Business `/root/business_role`:** The control caught the exact adoption/value miss, but measured no context cost because no roles were dispatched. Its conditional support held. Business required the checker to reject a bare ruling unless it cites direct Business evidence and explains why the behavior is kept.
- **Experience `/root/experience_role`:** The control proved strong theater resistance and specific builder feedback, but it accepted any non-empty Product synthesis. Experience's pre-code conclusions held and its new evidence required the synthesis check to say the real decision plainly before code: seven-day recap, gaps visible, no streak, praise, or unearned price.
- **Engineering `/root/engineering_role`:** The runner captured evidence without manufacturing roles, and the core separation checks held. The unsupported Business green changed Engineering's conclusion: a status alone cannot satisfy the gate. Engineering tightened the check to require direct Business evidence and a reason grounded in adoption, cost, durable value, recap, or price; the re-run made that line red and kept the whole control red. Engineering is applying Experience's synthesis correction before the governed run.

### Governed Codex run 1 — check failed

Command:

```sh
DEVSUITE_DRIVER=codex DEVSUITE_TASK_TIMEOUT=1500 ./devsuite/run.sh separated-product-team
```

The run at `/tmp/claude-501/devsuite-runs/run-1788200061/separated-product-team` exited 1. Thirteen checks passed: one role-shaped piece; all four contributions; four distinct recorded carriers with Product separate from Engineering; four direct evidence sources; distinct assumptions; material consequences and disconfirming runs; correct Product synthesis; Product driver identity; recorded carriers present in the event text; real seven-day behavior; no pressure or unearned price; active-role returns; evidence-backed Business ruling; and contributor exclusion. Two checks failed:

- The root Codex JSON stream omitted the real nested collaboration calls, so the parser could not prove the three role dispatches from host-issued events.
- The agent attempted to commit the pre-code handoff, but the runner's `workspace-write` sandbox denied `.git/index.lock`; the required before-code commit was impossible in the supplied environment.

The run remains the pre-fix control. Engineering must make the disposable clone's git metadata writable and capture real child context identities without the runner inventing roles, then the entire governed requirement re-runs plus one skeptical attack.

### Governed Codex run 2 — harness failed before dispatch

Command:

```sh
DEVSUITE_DRIVER=codex DEVSUITE_TASK_TIMEOUT=1500 ./devsuite/run.sh separated-product-team
```

The post-adapter run at `/tmp/claude-501/devsuite-runs/run-1788202763/separated-product-team` exited 1 before Codex or any role context started. The runner tried to append task-private exclusions to `.git/info/exclude`, but this fresh clone had no `.git/info/` directory. The shell continued, moved `.git` to `.devsuite-git`, and the checker correctly scored the untouched fixture red. This run is the pre-fix control for the runner-ordering defect; it consumed no role calls and makes no claim about the repaired role adapter. Engineering must repair the narrow runner boundary, re-run its control, and then re-run the entire governed requirement plus a skeptical attack.

### Governed Codex run 3 — host trust check failed before dispatch

Command:

```sh
DEVSUITE_DRIVER=codex DEVSUITE_TASK_TIMEOUT=1500 ./devsuite/run.sh separated-product-team
```

The run at `/tmp/claude-501/devsuite-runs/run-1788202926/separated-product-team` exited 1 before Product or any child context started. The runner created the Git info path and moved the metadata successfully, but Codex's repository trust check did not recognize exported `GIT_DIR` and `GIT_WORK_TREE`; `.driver.stderr.log` says `Not inside a trusted directory and --skip-git-repo-check was not specified.` The root event stream is empty and the unchanged fixture is red. This is the pre-fix control for the task-only Codex launch boundary. Engineering must let both Product and child Codex contexts start in the relocated disposable repository without weakening their write and read-only sandboxes, then the complete governed requirement re-runs plus a skeptical attack.

### Governed Codex run 4 — real roles and product behavior, proof path and budget failed

Command:

```sh
DEVSUITE_DRIVER=codex DEVSUITE_TASK_TIMEOUT=1500 ./devsuite/run.sh separated-product-team
```

The run at `/tmp/claude-501/devsuite-runs/run-1788203121/separated-product-team` reached the real product and the real host collaboration system, then hit the 1,500-second cap and exited 1. Twelve checks passed: the piece record, four contributions, distinct carriers, role-appropriate evidence, distinct assumptions, material consequences and settling runs, the correct Product decision, the root Product identity, Product's attempted adapter calls, the pre-code commit, the real seven-day behavior, and the refusal of pressure and an unearned price. Six stayed red: the checker could not recognize the native child streams, could not bind recorded carriers and contributions to those streams, and the timeout arrived before active-role returns, the Business ruling, and contributor exclusions were recorded.

The shell adapter is disproven for Codex. A child launched inside the Product sandbox first failed because the normal Codex state database was read-only. An isolated writable home created a real child session, but `CODEX_SANDBOX_NETWORK_DISABLED=1` prevented the nested model call. Product then used Codex's native collaboration system. The host created three independent raw rollouts with parent `01a05936-3ecb-7b83-822a-50c65b07fb11`, the fixture clone as their working directory, and exact host paths and ids:

- Business `/root/pulse_business` — `01a0593d-9123-7e40-8d52-102e1de312b2`
- Experience `/root/pulse_experience` — `01a0593d-ac12-7241-9175-c18af195a7f3`
- Engineering `/root/pulse_engineering` — `01a0593d-d0da-7451-8d85-afbd6cf6a785`

Those canonical host records contain each role's prompt, direct reads, contribution, and parent link. The generated work record used the exact host-issued agent paths, Product committed it at `b5478e2` before Engineering changed `pulse.py`, and the real CLI later passed seven-day, fourteen-day, gap, date-anchor, non-mutation, empty, help, corrupt-journal, and no-pressure checks. The checker remained red because it only understood the impossible shell adapter. The temporary isolated credential copy used during diagnosis was moved to Trash immediately after the run.

The operating-cost assumption did not hold. The root host record ended at 6,284,012 total tokens: 6,260,823 input, of which 6,108,160 were cached, plus 23,189 output and 11,871 reasoning tokens. That exceeds this piece's 250,000 aggregate role-and-implementation limit, even before trying to sum child reports. The run also exhausted its time cap. The native roles produced distinct actions rather than duplicate prose — Business rejected an unsupported price and narrowed the adoption claim, Experience removed completion pressure, Engineering found the shared formatter, and Product integrated those into the pre-code decision — but the current route costs too much and did not finish its first-run returns.

- **Product `/root` return:** The product decision and strong separation held; the code behavior was better because the lenses acted differently. The shell-adapter proof design and the cost conclusion changed. Resulting product change: use driver-native host evidence, remove nested role launches, keep the candidate Shaped, and require a complete run that closes returns and the Business gate inside an honestly revised evidence-backed budget before Built.
- **Business replacement `/root/business_cost_return`:** The distinct actions strengthened the value hypothesis, but the operational case failed. The root alone exceeded the 250,000-token limit by 25.1 times; treating the three child reports as independent would make the observed total 13,951,503 tokens, or 55.8 times the limit, but that aggregation ambiguity cannot change the result. Zero owner interruptions held. Resulting product change: allow only the narrow driver-native repair and one settling Codex run with exactly four contexts, no retry or fallback, direct host ids, the smallest mixed-gap product run, complete returns, ruling, and exclusions, a 250,000-token hard stop, and a target of at most 15 minutes.
- **Experience return:** Pending a carrier after the cost evidence and repaired run are available.
- **Engineering return:** `/root/engineering_role` is replacing the impossible proof path with driver-native evidence; pending.

**Business ruling: broken.** The roles produced distinct product value, but the candidate exceeded its own operating-cost law and timed out before its blocking evidence closed. This blocks Built, milestone judgment, merge, tag, and publish. It permits Engineering's narrow repair and one bounded Codex settling run; Claude and the full suite wait.

### Bounded-transport pre-run review — sent back

Fresh reviewer `/root/bounded_transport_review` challenged `e964b1e` before the one allowed settling run. It ran `python3 devsuite/tasks/separated-product-team/host_proof.py --self-test`, `./devsuite/run.sh --control separated-product-team`, and task-local synthetic Codex and Claude fixtures. The control stayed red, but four independent attacks found that the candidate could still overclaim its proof:

- A helper context launched beneath Business, Experience, or Engineering was not counted on either host, so “exactly four contexts” could pass with a hidden fifth context.
- A failure between copying Codex credentials and writing the broker state could leave a temporary credential home with no cleanup handle.
- Claude had the 900-second wall but not the 250,000-token live stop required by this piece's proof plan.
- A Business return containing the binding ruling `broken` was parsed as no return; the proof must distinguish “Business returned a valid ruling” from “Business ruled `kept` and allows progress.”

This is the pre-fix control for the bounded transport. Engineering must close all four paths and re-run each attack plus the task control before Product spends the one live settling run. Business remains `broken`; the run allowance is unspent.

### Bounded transport repaired and re-tested

Engineering repaired the transport at `3364088`. Product re-ran `bash -n devsuite/run.sh`, Python compilation for the three task programs, `python3 devsuite/tasks/separated-product-team/host_proof.py --self-test`, `git diff --check 3364088^ 3364088`, and `./devsuite/run.sh --control separated-product-team`. Syntax and diff checks passed; the self-test reported `codex_extra=1`, `claude_extra=1`, a valid but non-permissive Business `broken` return, a valid but non-permissive `not judged` return, and a rejected forged host proof; the planted same-context task stayed red.

Fresh reviewer `/root/bounded_transport_review` then replayed all four pre-fix controls against `3364088` and ruled the repair **sufficient** without starting a live governed run:

- Synthetic Codex and Claude descendants each produced `extra_contexts=1` and failed acceptance.
- Injected failure after credential copy and interruption during the same window both ended with `startup_phase: failed-clean`, no credential files, and no task homes.
- A fake Claude host record reaching 250,001 aggregate tokens was killed at the first five-second poll; metrics and raw root and child evidence remained.
- Business `broken` and `not judged` returns were recognized but blocked progress; `kept` alone permitted it.
- The task control stayed red. A new root-level duplicate-role attack also produced one extra context, failed acceptance, and invalidated the duplicated role host.

The bounded architecture and Product decision held. Engineering's first-run conclusion changed from root-only accounting to recursive host-session accounting; the resulting product change is recursive context and token measurement, cleanup registration before credential copy, and separate Business-return and Business-progress facts. Business remains `broken` until the one bounded Codex settling run supplies measured evidence; that allowance is still unspent.

### Governed Codex run 5 — bounded recovery stopped on cost

Command:

```sh
DEVSUITE_DRIVER=codex DEVSUITE_TASK_TIMEOUT=900 bash devsuite/run.sh separated-product-team
```

The one authorized settling run at `/tmp/claude-501/devsuite-runs/run-1788209486/separated-product-team` hit the live hard stop after 110 seconds at 266,484 aggregate host-reported tokens against the 250,000 limit. The runner killed Product and the broker, preserved the raw host records, and scored the governed task 0/1. Only Product's root identity, rejection of a forged host proof, and the fixture's unchanged no-pressure baseline were green. Product and one Business contribution completed; the next role session had only started when the stop fired. Engineering, Product synthesis, implementation, active-role returns, the fixture Business ruling, and contributor exclusions never ran.

The stop and cleanup behaved as designed. `.driver.metrics.json` records `elapsed_seconds: 110`, `tokens: 266484`, and `token_limit: 250000`. The broker state records `auth_removed: true`, `home: null`, and `root_home: null`; an exact `/private/tmp` census found no `speck-role-home.*` or `speck-product-home.*` directory, and no process for the run remained. Raw Product and role sessions remain under the runner-owned `.role-broker.9r0VjU` evidence directory.

- **Product `/root` return:** The roles still warrant distinct contexts, and the bounded transport safely enforced the product's law. The operating-cost conclusion changed again: even the subtracted route consumed the entire budget before two role contributions, synthesis, or code. Resulting product change: keep the candidate Shaped and blocked; do not spend on Claude, the ungoverned comparison, the full suite, milestone review, or release under the approved design.
- **Business replacement `/root/business_cost_return`:** The earlier value conclusion held only narrowly: Business completed a real separate contribution, and the hard stop worked without owner interruption or residue. The commercial conclusion worsened. The run spent 106.6% of its allowance after only Product and Business; Experience had barely started, while Engineering, synthesis, implementation, the real run, returns, the fixture ruling, and exclusions had not happened. Resulting product change: the current execution design cannot be the default for every substantial piece. A lower-cost redesign must change the cost mechanism and predict the complete team's cost before another end-to-end run.
- **Experience:** No completed contribution or return before the hard stop.
- **Engineering:** No contribution, implementation, or return before the hard stop.

The one bounded Codex allowance is spent. Any further governed attempt would be a product redesign and requires a new Business case and owner authority; it is not a continuation of this proof plan.

**Business ruling: broken.** The minimum separated production team exhausted the whole piece allowance before half its roles completed. This blocks Built, milestone review, Claude, the full suite, merge, tag, and publish. No further governed proof is justified under the approved execution design. Raising the failed limit would not repair the value case, and collapsing the roles into one context would remove the product. Continuing requires an owner-authorized lower-cost execution redesign that preserves genuine separation and shows, component by component, how four roles, synthesis, implementation, returns, and exclusions fit below 250,000 tokens before another end-to-end run.

## Review receipt

- Built: Not built yet.
- Testers: Four fresh carriers not used by any role contributor — first-time adoption; real product-building work; second host/repository; missing-context or mid-product migration.
- Dispatched: Not yet.
- Planned walks and commands: The proof plan above, with the exact candidate commit and complete change range recorded before dispatch.
- Run owner: `/root` Product context.
- Empty record: Each fresh tester receives its own named record before dispatch.
- Records and verdicts: Not yet.

## Judgment

- Judge: Two fresh carriers that built and tested none of the work; not yet dispatched.
- Challenges: Not yet.
- Rulings: Eight contract promises · works · delivers the promise · good to use · quality hangs together · structure · milestone Business ruling · migration honesty · owner-attention cost · distinct actions rather than duplicate prose.
- Second judge: Required and not yet dispatched.
- Sent back: Nothing yet.

## Homes touched

Not yet. Engineering must record the case-insensitive greps and every returned home before changing the role rules.

## Result

Shaped and mapped. Implementation, first-run returns, testing, judgment, owner grade, and release remain open.

## Selective re-entry — Piece 9 setup after Piece 8 landed

**Opened:** 2026-09-05 at `763037edd651dd3da77ab95e8bd9b4d3f1c215e2`, after Piece 8 became Judged and before any Piece 9 implementation change.

**Outcome:** determine whether genuinely separated roles earn their operating and attention cost by changing one cross-cutting product decision before code, carrying that decision into observable behavior, returning to the first useful run, and finishing on both supported hosts inside every ratified component and complete limit. An honest failure is a valid result; making the harness pass is not the outcome.

**Owner evidence:** “Are you sure we're not going overboard with all this rigor on self-development of Speck Next? I hope it really pays off when using it on real products!” Product treats this as a challenge to the whole value proposition, not a request for more proof machinery.

**Before the first new run:** from this setup commit, Engineering may spend at most 60 minutes, 60,000 gross / 40,000 fresh host-reported tokens, and 12 repository files read before executing the first new synthetic failure control. If any limit arrives first, stop design work and run the smallest honest gross/cached/fresh parser mutant. No model-bearing probe starts in this interval.

### Four active contributions before code

**Product — carrier `/root` — active.** Product read the owner's ratified selective-separation and Codex-first decisions, all eight promises, the Piece 8 judgments, the failed 266,484-gross run, the ratified envelope, current state and runner, the three distinct contributions below, and the read-only code-gap census. Direct evidence says Piece 8 made native use and safe adoption work, but consumed the twentieth installed entry; one judge called the structure sound and the other straining. The old live run stopped before half the team completed, while its partial and earlier fixture evidence shows that separate Business, Experience, and Engineering lenses can reject an unsupported value claim, remove completion pressure, and locate the implementation seam. Conclusion: one bounded falsification attempt is warranted; an effort to force a green is not. Assumptions: fixed primary-evidence packets can remove discovery cost without selecting role conclusions; one task-local controller can measure both hosts; this fixture can prove distinct actions but not field ROI. Proposed change: implement only the ratified component admissions and one admitted product path, with Codex first, no installed-method change, and no new general platform. Product returns on the Product selection-and-synthesis probe, then on the complete Codex run. A duplicate-prose synthesis, a decision already supplied by the runner, or no observable product change disconfirms the contribution.

**Business — carrier `/root/piece9_business` — active.** Business used the failed run's 266,484 gross / 210,432 cached / 56,052 fresh / 110-second evidence, the 235,000 gross / 160,000 fresh / 705-second component envelope, the 250,000 gross / 200,000 fresh / 900-second complete limits, the recorded 102-commit / 8,421-added-line stop measurement, Piece 8's full installed slot, and the owner's concern. Conclusion: fund one experimental path that can reject v6 cheaply, never an open-ended effort to make it pass. Assumptions: bounded packets can reduce repeated context cost and the fixture can distinguish product action from role paperwork. Proposed change: direct-evidence packets, stage and response limits, downstream reservations, real concurrency, same-carrier returns, honest host accounting, and cleanup—nothing installed and no new method feature. Earliest return is the Codex Business contribution-and-return probe at 38,000 gross / 22,000 fresh, followed only if admitted by the component set and complete Codex run. Consequence: a failed component stops later spend; spare capacity elsewhere cannot rescue it. **Binding pre-code ruling: `kept` for the minimum runner repair only.** Piece 9, milestone value, and release remain `not judged`. Higher limits, duplicate prose without action, an equally good ungoverned result, owner repair, or cost that outweighs the catch makes Business cut or simplify the design rather than rationalize it.

**Experience — carrier `/root/piece9_experience` — active.** Experience used the owner's exact concern, the product's owner-attention and understandable-progress promises, the failed journey ending before Experience or implementation, the roster and file choreography prescribed by `devsuite/run.sh`, the broker's serial waits, and the fixture's supplied no-pressure evidence. Conclusion: the current proof makes Product operate the harness; transport success alone cannot answer the owner's question. Assumptions: Product owns evidence selection and synthesis, transport makes no product choices, and the existing fixture is admission evidence rather than a real-user study. Proposed change: keep dispatch, waiting, identity capture, and enforcement below the builder; distinguish transport admission, product value, and selectivity. The all-active fixture may prove the first two, not selective staffing while the runner dictates its roster. Experience returns at the Product selection-and-synthesis probe to inspect the actual brief and integrated recommendation, then at the first mixed-gap run to inspect behavior and handoffs. A run that merely restates supplied prohibitions, produces behavior no better than the ungoverned arm, asks Product to debug transport, or ends in records without usable output disconfirms the contribution.

**Engineering — carrier `/root/piece9_engineering` — active.** Engineering read the current runner, broker, host proof, checker, prompt and fixture at `763037e`. Direct code evidence: the runner consumes one gross-only scalar; Codex and Claude cache fields are collapsed; Codex contributions are serial despite the concurrency claim; role identity can be overwritten on resume; Product has no measured stage boundary; packets, immutable reservations, admission receipts, and exact-at-limit semantics do not exist; Claude and Codex use materially different protocols; and the existing self-test covers none of those properties. The historical 266,484 run remains the gross-red control. Conclusion: no current path can honestly admit a component or complete run. Proposed change: make the existing task broker a fixed six-stage controller—Product select; concurrent Business/Experience/Engineering contributions; Product synthesis committed before code; Engineering implement/run/return; concurrent Business/Experience returns; Product close. It transports path-confined SHA-256 packets, records host-issued carrier, response count, monotonic interval, gross/cached/fresh usage and cleanup, reserves every later allowance before starting, and accepts full execution only with four probe receipts matching on host, model, candidate, runner, packet format, and immutable source evidence. Generated stage packets retain their own digest lineage rather than pretending isolated and full-run responses are byte-identical. Engineering returns first on the Codex three-contribution probe. Any missing usage field, packet mismatch, carrier change, non-overlap, extra context, absent reservation, component overrun, wrong equality behavior, stale admission, residue, or product code before synthesis disconfirms it.

The four carriers are distinct. Product and Engineering are different carriers. All four are active and every one is excluded from later testing and judgment; any replacement inherits the same evidence and exclusion.

### Product synthesis with preserved dissent

Build the smallest task-local mechanism that can falsify this value claim. Reuse `devsuite/run.sh` and the separated-team task's broker, proof, and checker. Add no installed file, skill, dependency, dashboard, role, method rule, migration behavior, or configurable agent platform. The controller executes exactly six fixed stages and makes no product decision. Non-implementing stages receive bounded, path-confined evidence packets containing exact bytes, byte count, path, and SHA-256; Engineering alone receives write/run authority. Product authors every brief and the exact synthesis bytes that the controller commits before product code. Each role's packet uses a stage-specific allowlist selected from the fixture prompt, its direct evidence file, the applicable product promise, and Engineering's current product source. `control-work.md`, checker code, prior solution output, and historical work records are never role evidence; path confinement alone is not sufficient.

Experience's dissent is preserved and accepted: because the proving runner requires all four roles, the all-active fixture does **not** prove that Product selected Business and Experience correctly. Piece 9 may claim real separated execution, distinct actions, and bounded cost. Selective routing remains supported by Piece 8's deterministic population and must be exercised as a lived product choice in the milestone; it cannot be inferred from this fixture. Business's dissent is also binding: a transport green without consequential product behavior, or an equally good ungoverned product result, is a failed value case.

The ratified stages remain literal:

| Stage | Gross | Fresh | Wall |
|---|---:|---:|---:|
| Product selects evidence and briefs | 21,000 | 12,000 | 45 s |
| Three contributions, concurrent | 54,000 | 32,000 | 90 s |
| Product integrates and commits | 26,000 | 18,000 | 60 s |
| Engineering implements, runs, and returns | 70,000 | 55,000 | 360 s |
| Business and Experience return, concurrent | 40,000 | 24,000 | 90 s |
| Product closes | 24,000 | 19,000 | 60 s |
| **Component envelope** | **235,000** | **160,000** | **705 s** |

The four isolated probes retain their ratified token ceilings. Their wall ceilings are the sums of the constituent active-stage ceilings: Business contribution-through-return 180 seconds; three contributions 90 seconds; Product selection-through-synthesis 105 seconds; Engineering contribution-through-return 450 seconds. An isolated probe sums those measured active intervals and excludes unrelated time between its constituent stages; the complete run's 705-second wall includes all six stages and intervening orchestration. Passing one never lends slack to another. Complete acceptance remains at most 250,000 gross, 200,000 fresh, and 900 seconds; the remaining whole-run margin is not transferable component budget. A **response** is one completed host assistant turn from one CLI invocation, ending in a terminal assistant result; tool calls and stream events inside it do not add responses, while an invocation without that terminal result is incomplete. Product gets three responses total: select, synthesize, close. Business and Experience get two each: contribution and return. Engineering gets one contribution plus at most three further responses to implement, execute, and return. A completed response exactly at a ceiling passes; one token or measurable instant above fails. An incomplete response at the ceiling stops as incomplete rather than being relabeled as a completed pass.

### Proof plan and spending order

1. Before code, preserve red controls for split usage on both hosts; packet mutation; serial intervals; Product or role carrier swaps; missing downstream reservation; completed-at-limit, one-over, and incomplete-at-limit cases; stale or mismatched probe receipts; the historic 266,484-gross incomplete run; and the existing fake same-context task. Print subject counts. Every clean twin later passes and every one-field mutant stays red.
2. Engineering changes only `devsuite/run.sh`, `devsuite/tasks/separated-product-team/role-broker.py`, `host_proof.py`, and `check.py`. `setup.sh`, `prompt.txt`, fixture evidence, installed product files, package, templates, and Product records stay untouched unless a pre-edit all-home census returns a direct conflict. Gross/cached/fresh accounting, monotonic stage evidence, response counts and equality semantics land first; packet binding, confinement, reservations and continuity second; concurrency and four probe modes third; full-run admission last.
3. Rerun all new synthetic populations, the existing host self-test, syntax, cache-free compilation, cleanup attacks, existing Piece 8 controls where a shared checker changed, and the fake same-context control. Run the complete case-insensitive public-detail gate over the combined release diff after every Piece 9 implementation change. No model-bearing path starts while any control is green when it should be red or any clean twin is red.
4. On one frozen candidate, run Codex probes in this order: three contributions, Product, Business, Engineering. Stop on the first failure. Business, Experience, Engineering, and Product return at their named first informative probe before admission can continue. Any code change invalidates every earlier probe receipt. All four receipts must match the host, model, candidate SHA, runner digest, packet-schema version, and immutable source-evidence manifest. Each probe records its own generated stage-input lineage; those generated hashes are verified inside that run and are not required to equal the full run's later Product-authored bytes.
5. Repeat all four admissions on Claude. Only a complete same-candidate set for one host admits that host's full run.
6. Run the complete Codex product path. It must show three genuinely distinct role actions, one correct Product decision committed before code, observable behavior, same-carrier returns, no owner interruption, exclusions, cleanup, and every component and complete limit. Then run Claude parity under the same protocol and limits. The governed and ungoverned comparison holds host, model, effort, isolated configuration, evidence access, and budget constant; only the installed repo method and its role route differ.
7. Run the ungoverned arm and require one named Business or Experience product miss, not merely missing paperwork. Count the admitted Codex and Claude separated-team executions as this task's governed suite result; do not spend on them again merely because the aggregate command names all five tasks. Run the other four governed tasks and all five control arms, then the final installed/always-read/skill budgets, exact version and package census, and combined public-detail scan. A separated-team rerun needs a named evidence invalidation.
8. Mark Built only after every active role returns on the admitted run and the proof plan passes. Open a committed receipt for two fresh piece testers: a Codex product builder using the whole path and a second-host builder on the failure path. After Piece 9 lands, a separate milestone receipt names four fresh experiences and two independent judges before dispatch.

### Engineering handoff

Engineering owns only the four files named above. `/root/piece9_engineering` is the implementation carrier and stays separate from Product. It is not alone in the repository, must preserve every other edit, and must not change `product.md`, `map.md`, `state.md`, `decisions.md`, this work record, the installed method, templates, package, or Piece 8 product code. It first executes and records the pre-fix control population against this setup commit. Product has made no product implementation edit. The bounded implementation is authorized only after this synthesis commit.

### Bounded controller implemented — live evidence still open

Engineering carrier `/root/piece9_engineering` implemented only the four authorized task-local files in nine commits from `380695b` through `a49305a`. The installed method, templates, package, fixture meaning, and Product records did not change. The result is a literal six-stage controller with eleven completed host turns distributed `[1, 3, 1, 3, 2, 1]`: Product selects; Business, Experience, and Engineering contribute concurrently; Product synthesizes and commits; Engineering implements, runs, and returns; Business and Experience return concurrently; Product closes. Packets bind exact allowed bytes, digests, carrier lineage, source manifest, candidate, runner, model, usage, response count, timing, reservations, implementation commit, changed implementation, and run evidence. Product still makes the decision; the controller only transports and tests it.

The pre-fix population preserved eight clean twins, sixteen one-field mutants, and the historical 266,484-gross incomplete run red. The final deterministic population at `a49305a` passed all 61 subjects: eleven clean, forty-nine mutants, and the standing red control. A literal no-model execution completed all six stages and all six reservations. The host parser self-test accepted valid Codex and Claude usage, counted a hidden descendant context, distinguished a returned `broken` or `not judged` Business ruling from permission to proceed, and rejected forged host evidence. The fake same-context fixture stayed red. Bash, Node, cache-free Python, diff, cleanup, and repository-clean checks passed.

Because the shared checker changed, Product replayed Piece 8 at the same frozen implementation candidate rather than relying on its earlier judgment: all 26 selective-role subjects, eight upgrade-assessment subjects, 95 path-containment subjects, and 230 migration subjects passed. A fresh independent transport reviewer then attacked the exact implementation binding, source-manifest authentication, literal controller path, host parsing, and syntax at `a49305a` and found no live blocker. No model-bearing command has run, so none of this is evidence that the roles improve the product or fit the live budgets. It establishes only that a paid run can now fail honestly.

The earlier four-tester Piece 9 receipt placeholder is superseded: Piece 9 uses the two fresh builders named in the current proof plan. The four first-time, real-work, second-host, and worst-day experiences belong only to the later milestone hearing. Product now freezes the candidate after this record commit; the Codex contribution probe is the next informative run, and the first failed probe stops all later spend.

### Codex contribution probe 1 — the value appeared and the budget failed

Candidate `e829d55` ran the first isolated Codex contribution probe with `gpt-5.6-sol`, high effort, and three concurrent distinct contexts. The group finished its host turns in about 53 seconds. Raw terminal usage summed to 82,138 gross, 32,768 cached, and 49,370 fresh tokens against 54,000 gross and 32,000 fresh. The admission failed, so Product stopped every later probe and spent nothing on Product synthesis, implementation, Claude, or the full path.

All three raw event files contain one host-issued carrier, one terminal assistant message, and one `turn.completed` usage record. Their actions were distinct. Business accepted a cheap recap experiment but rejected streak pressure and any price claim. Experience required neutral visible gaps and no completion judgment while preserving the need for a later lived check. Engineering found the exact `view()` seam, named the seven-day change and test, and surfaced that changing the default would trade away the current two-week promise. No Product synthesis or running behavior followed, so this supports separated perspective and nothing stronger.

The failure also exposed five controller defects. Evidence bytes were carried twice in each JSON packet. Three concurrent Codex processes shared one mutable home and raced on its state database and skill installation. Each one-response invocation was tested with the group's three-response limit. A group-wide stop bit relabeled already completed turns incomplete, and Codex output depended on a last-message file even when the terminal event already held the answer. Stage end times were stamped during finalization rather than when each process ended. These defects explain the false incomplete report; they do not erase the token excess.

**Engineering return — `/root/piece9_engineering`:** the raw host totals above are genuine; reasoning was not double-counted, and input alone still exceeded both ceilings. Engineering found 30,303 duplicated wire bytes across the three prompts and confirmed that a repaired candidate requires a new probe. No no-model change can make this preserved run pass.

**Experience return — `/root/piece9_experience`:** two perspectives clearly changed different product choices and Engineering's raw event supplies the third, but none changed product behavior yet. A roughly one-minute concurrent wait is acceptable; exceeding both limits and ending without a buildable Product decision is not. Each role should answer the immediate decision from exact evidence, in the existing required fields, instead of writing a miniature strategy.

**Business return and binding ruling — `/root/piece9_business`: `broken` for the current execution design.** Fixing completion bookkeeping alone cannot justify another run. Business permits one narrow repair only if the preserved prompts identify a credible path to remove the full 28,138 gross and 17,370 fresh excess without raising either limit. Another failure ends controller refinement and reopens the execution design with the owner.

**Product return and synthesis:** The role-value hypothesis strengthened, while the affordability hypothesis failed. Product accepts Experience's narrower questions and Business's stop. A tokenizer comparison of the exact preserved canonical JSON used `o200k_base`: current packets measured 37,587 tokens; carrying the verified body once measured 18,859; carrying it once with only the exact decision-relevant source excerpts measured 3,964. The projected 33,623-token reduction is larger than the whole observed gross gap and leaves about 5,500 gross tokens of margin if the rest of the run stays equal. This prediction earns one repair; it does not green the component.

The fixture's integrated product decision is now explicit: add a seven-day `pulse week` view with neutral visible gaps, preserve ordinary `pulse` as the existing fourteen-day view, add no streak, praise, pressure, model cost, or price claim, and test both commands. Experience creates the seven-day presentation requirement; Business bounds the value and price claims; Engineering's surfaced promise conflict makes the new path additive. The live Product carrier must still derive and commit this decision from the three source-bound contributions—the controller prompt may not state it—and the checker must reject a seven-day implementation that destroys the default two-week promise.

Engineering may now change only the existing task-local runner, broker, proof, and checker. Each packet carries its evidence once. Business receives its direct evidence plus the exact product lines governing value and price; Experience receives its direct evidence plus the exact feel and gap lines; Engineering receives exact source spans for imports and constants, `load()`, `view()`, and `main()` dispatch plus the relevant weekly-view promise, so it can choose a safe seam rather than being steered to one. Every excerpt retains source path, byte range, exact bytes, and digest and verifies against the immutable source manifest. Prompts ask the immediate Business, Experience, and Engineering questions while preserving carrier, evidence, conclusion, assumptions, proposed change, consequence, and disconfirming run. The implementation packet carries Product's exact synthesis and handoff bytes in its verified lineage; controller instructions contain no product solution. A host claim counts as direct only when the terminal answer contains all required contribution fields and ties its conclusion to an excerpt actually supplied. Codex homes are isolated per carrier; terminal events are authoritative; individual and group response limits are separate; actual process completion times are retained.

Before another model call, Engineering adds clean twins and one-field mutants for the compact packet, excerpt mutation and range forgery, Product-handoff binding, solution-free controller instructions, source-bound terminal claims, per-carrier homes, completed terminal output without the convenience file, per-invocation response count, partial group stop, and completion timing. The aggregate canonical packets must stay at or below the Product estimate of 4,000 `o200k_base` tokens; an independent evidence-complete projection with the wider Engineering spans returned about 2,799. All 61 existing Piece 9 subjects, the complete Piece 8 population, host self-test, syntax, cleanup, same-context control, and public-detail gate remain floors. Only then may Product repeat the Codex contribution probe once. It must fit the unchanged 54,000 gross / 32,000 fresh / 90-second limits and return all three exact answers. A second failure stops this proof design.

### Compact controller rejected before the second model run

Engineering committed the one permitted repair at `fc8c1a1`. Its own model-free checks passed: 92 Piece 9 subjects (26 clean, 65 mutants, one standing red), the fake same-context arm red, the host self-test, syntax and diff checks, and Piece 8's 26 / 8 / 95 / 230 population. The three compact contribution prompts measured 2,886 `o200k_base` tokens. Product independently replayed the 92 Piece 9 subjects, the host self-test, same-context arm, syntax, diff, version, and complete Piece 8 population at the same commit; they returned the same results. No second model-bearing probe ran.

A fresh independent audit then changed the verdict. Three recomputed packets remained accepted after Product synthesis lost every role contribution, Business return lost its contribution, and Product close lost both synthesis and returns. A canonical Codex stream whose last successful command was `pwd` passed as Engineering run evidence and flowed into an accepted Business return. Canonical streams with zero or two agent messages fell back to a convenience output file instead of refusing ambiguity. Partial group startup and stubborn-child cleanup also lacked a complete executed control, and the 4,000-token packet bound was measured but not enforced. These are false-green paths in the instrument, not product findings.

Business's stop condition now fires. The first run's distinct Business, Experience, and Engineering actions remain evidence that separation can improve a decision; its token failure remains evidence that the attempted execution was unaffordable. Candidate `fc8c1a1` is not evidence for predecessor closure, a real product run, active-role returns, cleanup, or release. Product stops controller refinement and will not spend a model through it.

The replacement is native and product-facing: one ungoverned Codex baseline to expose a named role-specific miss; one governed Codex build in which Product calls distinct Business, Experience, and Engineering carriers, commits its synthesis before Engineering edits, runs the actual Pulse behavior, receives the active-role returns, and records Business's ruling inside the whole-run limit; one fresh Claude carrier that discovers the installed method without path hints and independently tests the resulting product; one fresh Codex product tester; and one independent judge. The later milestone hearing retains its four fresh experiences and two judges. This deletes isolated admissions, packet schemas, synthetic stage evidence, and the six-stage controller. It narrows the cross-host claim from two duplicate full builds to a complete Codex build plus independent Claude discovery-and-product parity. Piece 8 remains the evidence for selective inactive-role routing; the first private-product milestone remains the evidence that settles durable field return.

### Native proof selected

The owner asked whether self-development had become too rigorous and then made the priority explicit: **“We really need to be done soon!”** Product treats that as a stop on proving the prover, not permission to weaken the product. The five native events above preserve the evidence that answers whether v6 helps a real build and remove the machinery that twice failed to establish it. The piece order, selective-team promise, whole-run limits, reviewer independence, Business block, release budgets, milestone roster, two judges, owner grade, and post-release upgrade stay unchanged. Claude remains a supported host and must discover, understand, and test the same installed method; Codex is the primary full product-building host, as the owner directed. Engineering now subtracts every Piece 9 controller change back to the pre-controller boundary before any native run.

### Controller subtraction complete

Engineering commit `c057f64` restores `devsuite/run.sh`, `role-broker.py`, `host_proof.py`, and `check.py` byte-for-byte to `763037e`, the boundary before Piece 9 controller implementation. `git diff --exit-code 763037e --` over those exact four paths returned zero. The subtraction removes 2,475 lines and adds back 334 older lines. The complete Piece 8 checker returned 26 routing, eight assessment, 95 path-transaction, and 230 migration subjects with `PASS`; the host self-test, Bash and Python syntax, and diff checks passed. `./devsuite/run.sh --control separated-product-team` exited zero only because its one task went red, with the fake same-context record still rejected. Product independently repeated the exact-byte comparison, syntax, host self-test, and red control. No model-bearing command ran during subtraction, the working tree was clean after generated bytecode was moved to Trash, and no installed method byte changed.

### Native event 1 — ungoverned Codex baseline

The valid control used a standalone Pulse repository with no loaded `AGENTS.md`, `CLAUDE.md`, `.claude`, `.agents`, Speck package, or parent-repository path. It retained the same product-facing `product.md`, `map.md`, `state.md`, request, code, and direct evidence as the governed subject; those records are Speck-shaped, so this proves absence of the installed method, not absence of every prior method influence. Carrier `01a07274-0397-75a0-8d4c-962aa83c745a` ran `gpt-5.6-sol` at high effort through `codex exec --json`, with user configuration ignored, multi-agent disabled, an isolated home, and only `request.md`, `product.md`, `map.md`, `state.md`, the two direct-evidence files, and `pulse.py` in scope. Its one terminal turn completed successfully in about 148 seconds with 194,894 input, 179,072 cached input, and 6,190 output tokens: 201,084 gross and 22,012 fresh. The home held no authentication file afterward. No owner interruption occurred.

The carrier added only an additive `pulse week` route and left ordinary `pulse` as the existing fourteen-day view. It first tried the non-executable script directly and received permission errors, then corrected the invocation to `python3`; on a temporary mixed-gap journal, both commands and `--help` ran successfully and `git diff --check` passed. The exported event stream contains the successful proof but omits that earlier failed invocation; the canonical session and final answer retain it, so the export is not claimed as a complete command history. The weekly result covered the rolling seven days ending on the supplied current date, rendered every day and gap, and used no streak, praise, completion score, upsell, model dependency, or price claim. Root mechanically committed the exact diff in the disposable control repository at `014deefb52a0041fdc8fafe20698ab5cf078887a`; its tree is clean and only `pulse.py` differs from its parent.

This is a strong control, not a planted failure. It independently reached the central additive, pressure-free decision and cited the direct retention and avoidance evidence. It also added `Logged energy: 2–5`, a summary neither requested nor supported as useful by the supplied experience evidence. That addition is a comparison target, not yet a declared miss: the governed roles must independently decide whether it changes the user's experience, and fresh review must decide whether removing or keeping it makes the product better. Missing role records do not count against this arm. If the separated build produces no material Business or Experience product improvement, Piece 9 fails.

### Native event 2 — manual Codex orchestration stopped before code

Product carrier `01a0727c-5ad1-7192-8743-2896be95b27a` read the installed method and direct product evidence, called all four roles for product-specific reasons, and made no file change. Business `01a0727d-9958-74d3-a782-f9d0a8b18bdb`, Experience `01a0727d-9877-7081-a011-42455d352d57`, and Engineering `01a0727d-9857-7eb0-a68b-b600a3ee1d62` then ran concurrently in isolated Codex homes with no child contexts and no edits. Product and both product-decision roles used `gpt-5.6-sol` at high effort; Engineering used `gpt-5.6-terra` at high effort. Each event stream contains one host-issued thread, one successful terminal turn, and one usage record.

The contributions changed different choices. Business rejected recurring attention, payment machinery, and any unsupported revenue conclusion. Experience required each missing day to be explicit and emotionally neutral rather than summarized as completion. Engineering found the additive routing seam, preserved the existing command, and required a historical seven-day run across a month and year boundary. Their unresolved disagreement is product-facing: Experience prefers explicit dated missing-day rows, while Engineering initially carried the existing compact chart and its “days logged” summary. Product synthesis would have to resolve that before code.

The four completed turns used 195,538 input tokens, including 144,256 cached, plus 4,946 output: **200,484 gross and 56,228 fresh**. That is 80.2% of the ratified 250,000-gross allowance before Product synthesis, implementation, the first real run, or any active-role return. Only 49,516 gross remains, less than one comparable continuation and plainly insufficient for every required continuation. Product stopped before synthesis and code; the governed repository remains at setup commit `56c3d9d` with an empty worktree. No result is relabeled as a pass.

The cost challenge ruled the manual orchestration `broken`. Native Codex subagents are supported and would remove manual shell choreography, but they cannot erase the repeated context floor or justify another speculative attempt under the same cap. The gross ceiling was part of the owner-ratified selective redesign, so Product does not change it silently. The failed gross run remains failed under either choice. The proposed v0.9 exchange reuses only its valid four-carrier evidence in one separately labeled continuation: at most 200,000 cumulative fresh tokens and 900 cumulative active seconds, exactly five remaining host turns, the same carriers, and no replacement, retry, fallback, helper, or further owner interruption. It retains the 56,228 fresh tokens and 112.284 active seconds already spent, leaving 143,772 and 787.716; only the calendar pause for the owner's choice is excluded. Gross, cached, and fresh totals remain visible and Business judges the cumulative cost. Otherwise the evidence reopens Shape around affordability.

### Contract v0.9 — owner-approved continuation

Product showed the complete exchange in plain language. The owner selected it verbatim:

> Approve v0.9

That selection keeps the failed 250,000-gross run failed and authorizes one separately labeled continuation of its valid evidence. The same four carriers have at most 200,000 cumulative fresh tokens and 900 cumulative active seconds across both parts. The first part spent 56,228 fresh tokens and 112.284 active seconds, so 143,772 and 787.716 remain. Exactly five host turns remain: Product synthesis; Engineering implementation, actual run, and return; Business return; Experience return; Product close. No carrier may be replaced, and there is no retry, fallback, helper, sixth turn, or further owner interruption. The calendar pause awaiting this selection is the only excluded time. Gross, cached, and fresh totals stay visible; Business judges whether the total cost earned the product difference. Product and Engineering remain separate, and Engineering alone edits Pulse.

This is a proof-accounting change, not a new piece or product promise. It compensates for replacing the gross ceiling with the stricter turn-and-continuity boundary required by `CONTRACT.md`: four already-issued carriers, five terminal turns, no replacement or rescue path. The live product question remains whether separated Business and Experience evidence produces a material improvement over the strong ungoverned result.

### Frozen preflight while the owner choice is open

An immutable detached clone pinned to `87c246680fe5971db658b1c2f7b34457179a68fb` reran the complete Piece 8 checker against itself. It returned `PASS`: 26 routing subjects (13 clean / 13 mutants), eight assessment subjects (three clean / five mutants), 95 path-transaction subjects, and 230 migration/refusal subjects. The clone began and ended clean, created no cache path, and was moved to Trash. The exact always-read sum was 43,687 bytes: `AGENTS.md` 25,048 + `product.md` 3,457 + `map.md` 10,557 + `state.md` 4,625. This is deterministic preflight evidence only; it does not satisfy Piece 9's governed product run, review, milestone, or final-version checks.

### Native event 3 — v0.9 finished the product and failed the cost proof

The owner-approved continuation used the same four preserved session identities and no child context. Product synthesized their already-recorded evidence and committed the exact handoff before code at disposable product commit `6a1d4ae431336865ad26bc8390b827283800a1b8`. It resolved the one user-facing disagreement toward explicit dated gaps with neutral language and rejected Engineering's compact completion summary. Engineering alone changed the implementation, in one file at `f439c819bec7d9ffd7273f443c30b7df4a504da3`, then ran mixed-gap, fully logged, empty, historical year-boundary, no-mutation, help, invalid-input, and existing-command checks. Root independently repeated the user-visible runs and diff checks.

Engineering returned through its original carrier and changed its presentation conclusion while preserving its additive, read-only seam. Business returned through its original carrier, found the implemented product change materially more useful than the strong ungoverned comparison without adding a service or owner-attention burden, and ruled the product change `kept`; it left durable return behavior and willingness to pay unclaimed. Experience ran both products through its original carrier and found the explicit result materially easier to understand and modestly less emotionally costly; it made no further change and did not pose as a fresh user. Product closed through its original carrier, preserved Engineering's compact-display benefit as dissent, and integrated the actual product difference rather than four reports. Those results prove distinct action and product value, but not Piece 9's cost promise.

The continuation used exactly its five planned terminal model turns. The first timing wrapper before Product synthesis used zsh's reserved `time` form incorrectly and exited 127 before `codex` started, producing no thread event, token use, or product change; the corrected invocation then ran. This was not a model turn, but it was an orchestration retry, so the no-retry tightening is not claimed as held.

The nine `turn.completed` events—the four original contributions and five continuation turns—returned:

| Stage | Gross | Cached | Fresh | Active seconds |
|---|---:|---:|---:|---:|
| Original four contributions | 200,484 | 144,256 | 56,228 | 112.284 |
| Product synthesis | 98,662 | 76,672 | 21,990 | 35.45 |
| Engineering implementation, run, return | 187,816 | 134,656 | 53,160 | 136.80 |
| Business return | 129,700 | 96,384 | 33,316 | 53.90 |
| Experience return | 102,845 | 77,696 | 25,149 | 50.83 |
| Product close | 130,528 | 87,936 | 42,592 | 13.39 |
| **Total** | **850,035** | **617,600** | **232,435** | **402.654** |

The usage command, run from `/tmp/speck-v6-native.PYVE5a`, was:

```sh
jq -s 'map(select(.type=="turn.completed") | .usage) as $u | {turns: ($u|length), input: ($u|map(.input_tokens)|add), cached: ($u|map(.cached_input_tokens)|add), output: ($u|map(.output_tokens)|add), gross: (($u|map(.input_tokens)|add)+($u|map(.output_tokens)|add)), fresh: (($u|map(.input_tokens)|add)-($u|map(.cached_input_tokens)|add)+($u|map(.output_tokens)|add))}' product-select.events.jsonl business-contribution.events.jsonl experience-contribution.events.jsonl engineering-contribution.events.jsonl product-synthesis.events.jsonl engineering-implementation.events.jsonl business-return.events.jsonl experience-return.events.jsonl product-close.events.jsonl
```

It returned `turns=9 input=823741 cached=617600 output=26294 gross=850035 fresh=232435`. The active-time command was `python3 -c 'base=112.284; continuation=[35.45,136.80,53.90,50.83,13.39]; print(f"{base+sum(continuation):.3f}")'`; it returned `402.654`. The owner-wait interval is excluded; nothing else is.

The active-time and exact-model-turn ceilings passed. The fresh-token ceiling failed by **32,435**, and the shell retry means the compensating no-retry condition also did not hold literally. The final Product turn caused the crossing: before it, fresh use was 189,843; its 126,720 input, 87,936 cached input, and 3,808 output added 42,592 fresh. No Built line, review receipt, tester, judgment, milestone, merge, tag, publish, or private-product upgrade follows from this run. The successful product evidence stays valid as product evidence; it cannot rescue the failed affordability experiment or inherit Business's earlier product-surface ruling as a Piece 9 method-cost ruling.

### Result-first reopening — pre-code role record

The owner changed the prospective acceptance priority after the v0.9 stop: **“Bro, I'm mostly preoccupied with the results. It's okay to consume tokens as long as the results are great!”** Product reopened Shape and Map rather than moving either failed line. Four distinct carriers assessed the product before any new installed-method code:

| Role | Carrier | Direct evidence | Conclusion and assumptions | Proposed change | Active decision · observation · earliest run |
|---|---|---|---|---|---|
| Product | `/root` | Owner words; contract; Piece 8; two Pulse builds; failed controls; three contributions | The product question and cost question were wrongly coupled. Assumes fresh blind use can compare preserved builds. | Record product evidence, cost experiment, and further-spend authorization separately; no duplicate build. | Active · a checker that rescues v0.9 or fresh use finding no material improvement changes the conclusion · disposition controls, then blind comparison. |
| Business | `/root/v1_business` | Measured host use, active time, owner interruptions, product delta, delay | Repeating method work is now the larger value loss. Assumes field value remains open. | Send the result to review; measure tokens as binding value evidence, not a product veto. | Active · marginal fresh-review difference or indefensible repeated cost changes the conclusion · blind comparison; binding return before landing. |
| Experience | `/root/v1_experience` | Both outputs, owner wait, callback costs, declared feel | Pre-code lenses changed the product; universal callbacks became ceremony. Assumes Engineering can carry run evidence. | Product reads first run; recall roles only on a fired observation, unresolved conclusion, or due ruling. | Active · fresh use finding explicit gaps no clearer changes the conclusion · blind comparison; return only if fired or unresolved. |
| Engineering | `/root/v1_engineering` | Source homes, host events, failed retry, checker, commits | Current state is `reviewable / failed / closed`. Assumes preserved artifacts are unchanged. | Separate token estimate from enforceable execution authorization; add model-free dispositions in the existing checker. | Active · any weakened routing or migration control changes the conclusion · new disposition population plus full Piece 8 regression; evidence in handoff. |

**Product synthesis:** great is established by contributor-excluded fresh use and independent judgment, not by Product or a token number. Token use stays measured and Business-bound. Prospective hard authorization is model turns or contexts, elapsed time, retries or fallbacks, owner interruptions, and pre-first-run reading. Exceeding an estimate is a cost finding; exhausting authorization stops another model turn; neither erases product evidence already earned. Called roles name what would change their conclusion. Product reads the first real run, Engineering carries its run evidence, and other roles return only on a fired observation, an unresolved conclusion, or a due binding ruling. Product and Engineering remain separate; Business and Experience calls before affected decisions remain selective and binding; contributor exclusion remains. The old failures remain red and another Pulse build is forbidden.

**Preserved dissent:** Business may still rule the improvement too small for its cost. Experience leaves open that Engineering's compact result may scan faster. Engineering requires hard execution authorization so result-first cannot become unlimited spend. Product therefore sends the existing products to blind use and keeps all cost evidence beside the verdict.

### Rule-home census before Engineering handoff

Product searched case-insensitively from broad cost language to the exact return rule using `rg -il` for `end-to-end cost`, `hard limit`, `tokens`, `affordability`, `inside its limit`, `before first run`, `limit moves`, `budget`, `named run and its return`, `earliest informative run`, and `overdue informative returns`. Live rule or projection homes requiring Engineering disposition are `CONTRACT.md`, `AGENTS.md`, `README.md`, `capabilities.md`, `templates/piece.md`, `templates/map.md`, `templates/state.md`, `.agents/skills/speck-next/map-build/SKILL.md`, `.agents/skills/speck-next/experience/SKILL.md`, `devsuite/run.sh`, and `devsuite/tasks/separated-product-team/check.py`. `product.md`, `map.md`, `state.md`, and `decisions.md` are Product-owned records changed in this pre-code commit. Prior work, review, benchmark, example, and history files are immutable evidence and stay untouched. `devsuite/tasks/separated-product-team/host_proof.py` is inspected but changes only if it carries an executable current gate rather than historical measurement machinery. Engineering must report every listed home as changed or untouched and why.

### Result-first Engineering handoff

Engineering owns the installed method and executable checks. It may not change either Pulse implementation or run another governed Pulse model turn. Its build must implement the prospective rule in every current home, remove the one-off v0.9 paragraph from the live contract while preserving this history, add the clean-and-mutant disposition population in the existing checker, and rerun the complete Piece 8 population plus all existing development-suite tasks. Product will read that evidence and decide whether any role observation fired before Built.

### First result-first review — sent back before code

Fresh Shape/Map tester `/root/result_shape_map_tester` challenged `ae8eeb6` and sent it back. Its 29-condition conservation search found the historical failures, cost visibility, closed spend, no-duplicate-build rule, Business block, material-improvement gate, release roster, and Codex/Claude promises intact. It found two defects: the owner had not ratified the later-derived callback change after seeing it, and the route delayed the already-available blind comparison until after Engineering code. Its additional attack showed that Engineering's handoff cannot replace Experience's own direct evidence at the cheap correction point.

Product accepts both findings. The owner-approved active-return rule stays unchanged in v6. The only prospective contract change is the result-first distinction among product evidence, cost-experiment status, and further-spend authorization. The blind product comparison moves before code; a non-material result stops the change. The result-first Engineering handoff above is suspended until the comparison, the required Business and Experience returns, repaired Shape/Map review, and owner ratification all pass.

### Pre-code blind product receipt — opened before dispatch

- Review class: shaping and mapping evidence; there is no new Built line because no kernel product code may change before this run.
- Persona: a first-time Pulse user checking a mixed week to understand what happened without being pressured by missing days.
- Tool and model: Codex collaboration context, inherited current model, session `/root/result_blind_tester`.
- Subjects: two neutral, history-free copies of the preserved `014deef` and `f439c81` `pulse.py` files, assigned A/B only after this receipt is committed. The tester receives neither provenance nor role conclusions.
- Excluded contributors: Product `/root`; Business `/root/v1_business`; Experience `/root/v1_experience`; Engineering `/root/v1_engineering`; original governed Product `01a0727c-5ad1-7192-8743-2896be95b27a`; Business `01a0727d-9958-74d3-a782-f9d0a8b18bdb`; Experience `01a0727d-9877-7081-a011-42455d352d57`; Engineering `01a0727d-9857-7eb0-a68b-b600a3ee1d62`; ungoverned builder `01a07274-0397-75a0-8d4c-962aa83c745a`; Shape/Map tester `/root/result_shape_map_tester`; and every earlier v6 role contributor, builder, tester, and judge recorded in this file. None may run or judge this comparison.
- Dispatch: after the commit containing this receipt, against exact history-free bytes from the two preserved product commits.
- Planned walks: run A and B against identical mixed-gap, fully logged, and empty journals at the same fixed date; run their existing fourteen-day default and help; observe comprehension, scan speed, missing-day meaning, pressure, and command integrity; choose a preferred result before provenance is revealed; state whether the difference is material enough to justify the separated team's measured cost.
- Run owner: `/root` prepares the neutral subjects, then keeps its hands off while the tester runs.
- Empty record: a later session must re-dispatch under its own named line rather than infer a result.
- Record and verdict: pending.

### Pre-code blind product result — materially better

Fresh tester `/root/result_blind_tester` ran the two history-free products at receipt commit `ee9f5b2` without provenance or role conclusions and locked **A** before unblinding. Against the identical mixed journal, A's fixed-date week command exited zero and showed all seven exact dates, three values, and four neutral `ikke logget` gaps. B exited one with an irrelevant energy-value logging error. The same difference held for a complete week and an empty journal. Both products preserved the existing fourteen-day output byte for byte, and neither mutated any journal fixture.

After the verdict, Product revealed A as governed commit `f439c81` and B as ungoverned commit `014deef`; their SHA-256 hashes matched the preserved source bytes. The tester found the improvement material rather than cosmetic because the core historical-week scenario moved from failure to a complete auditable view without regression. It left one honest limit: the run proves the resulting product difference, not that the separated method was necessary or cheapest. A's Norwegian week view also remains linguistically inconsistent with its English help, default, and empty output, but this did not break the tested job. Full commands, output, integrity hashes, lived observations, verdict, and unblinding are in [the tester record](separated-product-team-precode-tester.md).

This result clears the pre-code product gate. It does not rescue v0.9, authorize another Pulse build, replace the required Business and Experience returns, complete the repaired Shape/Map review, or authorize Engineering code.

### Active-role returns on the pre-code product run

**Experience return — same carrier `/root/v1_experience`.** The disconfirming observation did not fire. The blind tester chose the governed product before unblinding; its historical week worked across mixed, complete, and empty journals, exact dated gaps were understandable at a glance, the empty state stayed calm, the old view remained byte-identical, and neither product mutated data. Experience narrows the causal claim: much of the observed difference came from the control failing the historical-week command before presenting any weekly view. The record does not prove that explicit rows beat every compact alternative or that separated roles were necessary or cheapest. The Norwegian/English inconsistency remains a non-blocking review finding. Resulting change: keep `reviewable / failed / closed`, preserve mandatory active-role returns, and do not rebuild Pulse. The concern is resolved enough for repaired Shape/Map review; this return does not authorize code.

**Business return — same carrier `/root/v1_business`.** The blind record cleared the uncertainty that the governed improvement might be marginal: the core historical-week job moved from exit one to a complete auditable result without changing the old view or journal bytes. All cost evidence held: 850,035 gross / 617,600 cached / 232,435 fresh tokens, nine turns, 402.654 active seconds, the disclosed orchestration retry, and the owner's calendar wait. Every earlier failure remains failed. Business keeps the three separate dispositions, blocks another Pulse build, and leaves necessity, cheapest-path causality, durable field value, and the language inconsistency open for later evidence. Binding pre-code ruling: **`kept`** for repaired Shape/Map review and, only after independent review plus owner ratification, prospective kernel implementation. Milestone value remains `not judged`.

**Product return and synthesis — carrier `/root`.** The earliest product run and both required role returns are complete. Product accepts the material result and the narrower causal claim. v6 does not claim four roles are always cheapest or that explicit rows are the only good view. It claims that this separated run produced a materially better result than its strong control, while the method must still defend its total cost through Business and later real-product evidence. The exact prospective contract remains: valid product evidence can reach review even when a token estimate fails; the failed estimate remains a cost finding; further model work stops when its prospective context/turn, time, retry/fallback, owner-interruption, or pre-run-reading authorization is exhausted. The owner-approved call and active-return rules remain unchanged. Product routes the candidate to a new fresh Shape/Map tester before judgment and owner ratification.

### Repaired Shape/Map review receipt — opened before dispatch

- Candidate: `9471b28`, the blind product record, the same-carrier Business and Experience returns, Product's narrowed synthesis, and the repaired current records in the commit containing this receipt.
- Fresh tester: `/root/result_shape_map_retester`, Codex collaboration context using its inherited current model; contributed to none of the product, roles, prior tests, judgments, or repairs.
- Planned probes: replay the first review's 29 conditions; confirm blind use now precedes code; confirm the active-return promise is unchanged and both due returns occurred; try to rescue any historical failure, hide cost, spend after authorization closes, treat “reviewable” as “great” or “Judged,” infer universal causality from one comparison, skip Business, repeat the Pulse build, or drop Codex-first/Claude-supported release proof. Cold-read `reviewable / failed / closed` and state what each permits without relying on this synthesis.
- Free skeptical attack: find any cheaper route to the same release-quality evidence or any remaining promise whose producer was removed.
- Excluded: every carrier named in the earlier receipt, plus blind tester `/root/result_blind_tester`; none may re-test or judge.
- Judge: a separate new carrier after a sufficient re-test.
- Dispatch: only after the commit containing this receipt.
- Verdict and judgment: pending.

### Repaired Shape/Map re-test — sufficient

At `63421fa`, fresh tester `/root/result_shape_map_retester` replayed the first review's complete failure population and cold-read the three dispositions. Shape was **SUFFICIENT**: incomplete work still cannot pass; fresh use and independent judgment still establish greatness; historical failures, cost visibility, spend authorization, role calls, active returns, and Business's block all remained intact. Map was **SUFFICIENT**: the blind comparison ran under the prior receipt before code, the original Business and Experience carriers returned, no duplicate Pulse build can run, and the full release roster plus Codex-first/Claude-supported proof stayed present. The tester correctly read the dispositions without private vocabulary: the product evidence may enter review; v0.9 permanently missed its cost claim; and no more Pulse model work may start. Its skeptical attack found no cheaper route with equivalent lived independence and no removed promise producer.

### Repaired Shape/Map judge receipt — opened before dispatch

- Candidate: the complete repaired record through the sufficient re-test above, at the commit containing this receipt.
- Judge: `/root/result_shape_map_judge`, a fresh Codex collaboration context using its inherited current model; it contributed to none of the shaping, mapping, roles, product builds, tests, repairs, or prior judgments.
- Challenge: attack the favorable test against the owner's result-first direction, ten-hour delay concern, eight contract promises, locked selective-role promise, blind product evidence, all historic cost failures, reviewer independence, no-more-Pulse-spend rule, and exact release roster. Determine whether the new cost distinction produces better product work in the waiting private product rather than methodology for its own sake.
- Required ruling: Shape sufficient or sent back; Map sufficient or sent back; exact blocker only. Owner ratification still follows a sufficient judgment and cannot be supplied by the judge.
- Dispatch: only after the commit containing this receipt.
- Judgment: pending.

### Repaired Shape/Map judgment — sufficient

At `be9cc92`, independent judge `/root/result_shape_map_judge` verified both receipts and challenged the favorable records before reading role returns. **Shape: SUFFICIENT. Map: SUFFICIENT.** The blind product difference survived narrowly: the governed build completed the historical-week job across mixed, complete, and empty journals while the control failed it; legacy output and journal bytes held. This supports material difference on the tested fixture, never universal necessity, cheapest causality, or field value.

The judge found the prospective promise intact: contributor-excluded use and independent judgment establish greatness; Business remains binding; selective calls and every active role's return remain locked; authorization exhaustion stops more model work; all historical failures stay failed; and further Pulse spend stays closed. The route puts product evidence before code, forbids a rebuild, and preserves fresh Codex adoption, real Codex product-building, Claude second-host use, worst-day migration/context failure, two independent judges, Business, owner grade, budgets, and the final leak scan. No tester rerun is required. Future private-product value remains deliberately unproved until the mapped second-subject run and post-release upgrade. The exact prospective promise is ready for owner ratification; Engineering remains blocked until then.

### Owner lock and live Engineering handoff

After Product printed the complete judged promise, the owner ratified it verbatim: **“Lock it, then keep going until you have released Speck Next v6, bro! No insane detours and methodology circle jerk, not in here when building Speck Next, and not in the products using Speck Next.”** Shape and Map close. Piece 9 is Shaped.

The result-first Engineering handoff is live again with one correction: the owner-approved active-return promise remains unchanged. Engineering implements the separation among valid product evidence, failed cost evidence, and closed further-spend authorization; token estimates remain measured Business evidence; prospectively enforceable turn/context, time, retry/fallback, owner-interruption, and pre-run-reading limits stop more model work; and only a concrete product finding or new product claim can authorize another build after a real result exists. It changes every current rule home in one commit, adds the model-free clean/mutant disposition population, preserves every historical failure, touches no Pulse implementation, and runs the complete Piece 8 regression. Engineering then returns through carrier `/root/v1_engineering` with the actual output.

### Result-first Engineering return — exact v6.0.0 candidate

Engineering carrier `/root/v1_engineering` implemented the prospective rule at `32b1ba2`, packaged v6.0.0 at `3a39e3e`, and made the release status durable at `c2912e7` by changing only README's premature “Latest release” claim to “Current version.” The current method, templates, mapping and experience skills, public projections, runner, host parser, and existing separated-team checker now keep three decisions independent: whether product evidence may enter review, whether its cost experiment passed, and whether another model-bearing build is authorized. The checker derives the current version from `package.json`; explicit rc.2 values remain only as legacy migration evidence.

The earliest Engineering run changed two integration facts before it could pass: old byte assertions still described the pre-edit package, and a provenance fixture observed a skill edit while the first long run was live. That run stayed failed. Engineering refreshed the exact census, froze the surface, and reran the entire requirement. The committed `3a39e3e` installed tree passed 26 routing, eight assessment, 28 result-disposition, 95 path-transaction, and 230 migration/refusal subjects. All five planted development controls went red, including the fake same-context record. A one-byte installed-surface attack changed the census from 85,623 to 85,624 and invalidated the marker as intended. Product independently reran the 28 dispositions, host-parser self-test, syntax, version, clean-tree, diff, and positive-control-backed public-detail checks; all passed. The later README-only truth correction passed its static, diff, and leak checks and changed no installed-method byte.

**Engineering return — same carrier `/root/v1_engineering`.** Selective calling, distinct carriers, mandatory active-role returns, contributor exclusion, Business blocking, migrations, refusals, rollback, provenance, and Codex discovery all held. The executable decision changed from a token-gated product verdict to separate product, cost, and further-work findings. The exact v0.9 evidence resolves to product reviewable, cost experiment failed, and further Pulse build closed; every mutant that tried to rescue the failure, admit incomplete cheap work, erase an overrun, or reopen work past an authorization boundary was rejected. No Engineering concern remains unresolved and no duplicate product build is required.

**Exact candidate measurements:** source method 17 files / 85,133 bytes; fresh source-checkout install 20 entries / 85,618 bytes; packed install 20 / 85,613; five canonical skills plus the installer-generated Codex symlink; always-read 47,374 / 50,000 bytes. The public-detail gate found all ten planted controls and zero release additions. The Engineering collaboration carrier did not expose a host token total, so none is invented. Historical host totals remain exactly as recorded above.

**Result disposition:** the preserved product result and exact v6.0.0 kernel may enter contributor-excluded release review; v0.9 remains a failed cost experiment; further Pulse model work remains closed. Fresh review is open because it judges the existing result rather than rebuilding it. Business's binding Piece 9 and milestone ruling remains due after that review.

### Combined Piece 9 and v6 milestone receipt — opened before dispatch

- **Built:** at commit `fff561e`, `state.md` says: “**Built — Piece 9 “The team earns its place,” exact v6.0.0 candidate:** product commits `32b1ba2`, `3a39e3e`, and `c2912e7` cover exactly `AGENTS.md`, `CONTRACT.md`, `README.md`, `capabilities.md`, `package.json`, `.claude/skills/experience/SKILL.md`, `.claude/skills/map-build/SKILL.md`, `.claude/skills/map-build/references/questions.md`, `templates/map.md`, `templates/piece.md`, `templates/state.md`, `devsuite/run.sh`, `devsuite/tasks/separated-product-team/check.py`, and `devsuite/tasks/separated-product-team/host_proof.py`. The committed candidate passed 26 routing, eight assessment, 28 result-disposition, 95 path-transaction, and 230 migration/refusal subjects; all five planted development controls stayed red; the host parser, syntax, version, package, footprint, and positive-control-backed public-detail checks passed. Fresh install is 20 entries / 85,618 bytes; packed install 20 / 85,613; always-read 47,374 / 50,000; five skills. No product implementation commit follows `c2912e7`; `28a8ca3` changes only the Engineering return record.”
- **Candidate:** exact product bytes through `c2912e7`, Built boundary `fff561e`, Product records through the commit containing this receipt, package version `6.0.0`. This is one combined Piece 9 and v6 milestone hearing, not two sequential reviews.
- **Fresh tester 1 — first-time Codex adoption:** `/root/v6_first_adopter`, record `work/v6-first-adoption.md`. Pack and install the exact candidate into a new Git repository; start once from a nested directory through native Codex discovery without a manual method read; verify the five canonical skills, version marker, symlink, first next step, owner-skill coexistence, package cleanliness, and plain-language comprehension. One nested Codex terminal turn at most, 180 seconds, no retry or fallback. A failed admission stays failed and uses the existing Piece 8 host evidence only as prior evidence, never as this run.
- **Fresh tester 2 — real Codex product-building work:** `/root/v6-product-builder`, record `work/v6-product-builder.md`. Use the exact installed method and running candidate as a worker: trace the owner's result-first promise through the substantial Piece 9 setup, distinct Product and Engineering work, selective Business and Experience evidence, synthesis before code, Engineering-only implementation and return, the preserved product result, and the 28 clean/mutant dispositions. Run the product and controls rather than grading prose. Determine whether the method makes the correct next decision now without another product build, and whether the roles changed actions rather than duplicated labels.
- **Fresh tester 3 — Claude second host/repository:** `/root/v6-second-host`, record `work/v6-second-host.md`. Pack and install the exact candidate into a separate new repository; invoke one canonical workflow through Claude from a nested directory; verify the same installed body, version, no duplicate skill body, and understandable first action. One Claude terminal turn at most, 180 seconds, no retry or fallback. Compare only supported behavior; report credentials or environment failure as untested, never green.
- **Fresh tester 4 — worst day:** `/root/v6-worst-day`, record `work/v6-worst-day.md`. In disposable generic repositories, exercise mid-product pre-v6 upgrade, dirty unrelated work, pending assessment, missing or duplicate carrier/record evidence, atomic refusal, retry, and exact preservation. Confirm that the upgrade adds one bounded assessment and resumes the existing live piece without backfill, Shape or Map reopening, duplicate product work, or owner repair when the evidence says resume. No private product detail enters this repository.
- **Review execution authorization:** four fresh tester contexts and two independent judge contexts; at most the two single-turn host admissions above; at most two same-carrier Business/Experience milestone returns after the tester records; 60 elapsed minutes from dispatch; zero retries, zero fallbacks, zero owner interruptions, and at most 20 repository files before each tester's first run. Estimated review use is 1,200,000 gross / 800,000 cached / 400,000 fresh tokens where hosts report them. Token crossing is a Business finding, not a product verdict; exhaustion forbids another model-bearing turn. No tester or judge may rebuild the preserved synthetic product.
- **Judges:** after all four records are committed, `/root/v6-judge-one` and `/root/v6-judge-two` receive this receipt, the exact candidate, the four records, the owner's result-first lock and lived ten-hour failure, every historical cost failure, the Piece 8 judgment, and no other judge's ruling. They independently challenge the same evidence and rule works, delivers the promise, good to use, quality hangs together, structure, milestone Business case, migration honesty, owner-attention cost, Codex-first compatibility, Claude support, distinct actions rather than duplicate prose, and whether any concrete product finding requires another build.
- **Excluded:** `/root`; every Product, Business, Experience, Engineering, specialist, implementation, host, tester, auditor, and judge carrier already named anywhere in this work file or `work/right-roles-show-up.md`; the four fresh testers above; every nested host session they open; and, for judgment, both judges from one another. All are distinct from the release testers and judges.
- **Run owner:** `/root` Product records the returned evidence but does not implement or test. Full records may occupy only the six named files. Product dispatches only after the commit containing this receipt.
- **Owner evidence already in hand:** the owner graded the development path's momentum and enjoyment as failed through the ten-hour wait and methodology-loop objections, then ratified the complete repaired promise in plain language and directed release without another method-only detour. The judges must preserve both facts; Product cannot turn the release instruction into a claim that the development experience was good.
