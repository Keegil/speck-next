# The bridge from shaping into building

**Outcome:** the transition that failed on Pilot's first build day works: a shaped corpus becomes an ordered, owner-visible `map.md` whose pieces consume the shaped material (frames, model sections) instead of ignoring it; the review summons is structural (receipt before Proven, review debt ≤ 1); and while the owner is in the room, state follows the map so "where are we" never needs asking.

**How I'll know it works:** Pilot's next build day, measured by the playbook's git checks — work-file shapes committed before their code, state never stale past a map event, no piece past Built without a receipt, deck frames appearing as consumed inputs in the map — and by the only judge that matters: the owner doesn't ask where we are, and nothing feels aimless.

**Result (2026-08-17):** landed after interviewing the owner on the four forks (his calls recorded in decisions.md: separate map artifact · ordering stays agent judgment but harnesses never satisfy "run the product" · ladder + debt cap · state follows the map). The file model grew to five files — the one artifact-class addition of the kernel's life so far, taken deliberately against the accretion bias because the failure evidence demanded a standing home for the piece-order and asset-consumption truth. His sharpest finding drove the deepest law: shaping's outputs are the build's inputs, and unconsumed gold stays visible.

**Open:** the design is failure-derived but unproven — its proving run is Pilot's next build day. The kernel-change review receipt follows below once the independent pass lands.
