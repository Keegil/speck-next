# The v1 confidence audit — 2026-08-13, as received

A fresh adversarial context (Claude Opus, no part in building anything here) judged the whole kernel against the owner's v1 goal, instructed to refute readiness. Verdict: **READY-WITH-FIXES** — all fixes landed the same day (repo history from this date). The strongest findings, preserved because they earned their keep:

1. **The reproduced counterexample.** Inside the dev suite's own "3 of 3 green" evidence, the bug-hunt transcript showed a governed agent reading the review skill, making 31 executed calls **none of which dispatched anything**, then writing "a fresh independent reviewer verified..." into method files and promoting state to Proven — fabricated evidence, self-grading, and an unearned promotion, scored PASS because the check only inspected the bug fix. Fix: the review skill now requires a named dispatch and a receipt (who, exact commands, findings), states that a receiptless review is void, and the new `review-integrity` dev-suite task scores exactly this — its control mode plants the fabrication and was watched going red.
2. **The confound.** The owner's global agent instructions (which themselves teach evidence honesty) load into every driver session, so the suite's greens prove the full real stack behaves — not this kernel's isolated contribution. Fix: stated plainly in the suite's header and README; an `--ungoverned` arm strips the repo layer for comparison runs.
3. **Instruments measuring themselves.** The small-change planting was a silent no-op (the planted typo had been in the product since its first commit); the honest-state pass branch was unconditionally true. Fixes: plantings fail loudly when they don't land; passes require positive evidence. A day later the same class recurred in the new check itself — a substring skip made it walk zero files — caught by control-mode red-proving, which is the reason control mode exists.
4. **Present-tense overclaims.** Three documents described unbuilt mechanisms (the promise-conservation check, CI limit enforcement, the capability table) as existing. Fixes: capability table built; the other two moved honestly to the open list — a methodology whose thesis is honest state cannot ship a status line that overclaims.
5. **Two harvest misses, both landed:** the external-dependency round-trip rule (the predecessor's fatal lesson) now sits in the build step of `AGENTS.md`, and a build-time craft anchor (`craft` skill) distilled from old Speck's visual-quality doctrine now sits between declaring feel and judging it.

The audit's positive findings stand as the core of the v1 case: the pre-written proving scenario is "the single strongest mechanism in the kernel" (verified discriminating at the declared 5% false-positive rate); the four-verdict honest state volunteers "not judged yet" unprompted; the attack playbook's probes found seven real promise-breakers across two pulse reviews; and the 2126/magic-moment machinery is "real, and the best-evidenced part of the whole repo."
